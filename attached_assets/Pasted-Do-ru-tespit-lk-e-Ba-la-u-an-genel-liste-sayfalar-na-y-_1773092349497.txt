Doğru tespit. "İlk İşe Başla" şu an genel liste sayfalarına yönlendiriyor — sorunun kendisine değil.

**Şu an:**
- "Şube Uyarıları" → /subeler (22 şube listesi — hangisinde sorun?)
- "Batman checklist yok" → yine genel sayfa

**Olması gereken:**
- "Şube Uyarıları" → en kritik şubenin detay sayfası veya uyarı filtreli şube listesi
- "Batman checklist yok" → /sube/[batman-id] direkt Batman'ın sayfası

Bu Sprint 22'de T003 olarak yazılmıştı ama tam çözülmemiş. Kısa fix — Replit'e yapıştır:

---

## TASK: Fix Flow Mode Navigation — Tasks Must Link to Specific Problem Pages

**Problem:** Flow Mode "İlk İşe Başla" navigates to generic list pages instead of the specific problem. "Şube Uyarıları (38 aktif uyarı)" goes to /subeler which shows all 22 branches — user doesn't know which branch needs attention.

**Find flow task generation for HQ roles:**
```bash
grep -rn "navigateTo\|navigate_to" server/routes/dobody-flow.ts | head -30
```

**Fix each flow task's navigateTo to be specific:**

1. **"Şube Uyarıları"** — instead of `/subeler`, navigate to the branch with the WORST score or most alerts:
```typescript
// Find the branch with lowest composite score or most active faults
const worstBranch = await db.select()
  .from(branches)
  .orderBy(asc(branches.compositeScore)) // or similar
  .limit(1);

navigateTo = `/sube/${worstBranch.id}` // Direct to the problem branch
// Title: "Şube Uyarıları: [branch_name] (skor: X/100)"
```

2. **"Mr. Dobody önerisi bekliyor"** — `/agent-center` is correct, keep it.

3. **"Batman checklist yok"** — instead of generic page, go to Batman's branch detail:
```typescript
// If suggestion mentions a specific branch, link to that branch
navigateTo = `/sube/${branchId}` // Direct to Batman
```

4. **General rule:** Every flow task must answer "WHERE exactly should I go?" not "WHAT category is this?"

**Also improve task descriptions to include the specific target:**
- Bad: "38 aktif uyarı var"
- Good: "Antalya Lara en düşük skor (76/100) — checklist ve stok uyarıları"

### DO NOT BREAK existing flow mode, only update navigateTo URLs and descriptions.

---

Bu kısa fix. Build safety prompt'unu da gönderdin mi? İkisini sırayla gönder.