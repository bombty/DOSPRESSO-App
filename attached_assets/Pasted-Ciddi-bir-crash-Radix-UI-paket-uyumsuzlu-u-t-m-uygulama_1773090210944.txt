Ciddi bir crash — Radix UI paket uyumsuzluğu tüm uygulamayı çökertti. Audit raporu düzeltildiğini söylüyor ama screenshot hâlâ hata gösteriyor. Replit'e yapıştır:

---

## TASK: EMERGENCY — App Completely Broken, Cannot Load

The app shows `[plugin:runtime-error-plugin] Importing a module script failed` on ALL pages for ALL users. Nothing loads. This started after a Radix UI package was added.

### STEP 1: Check if the previous fix actually worked

```bash
# Is the server running?
curl -s http://localhost:5000/api/health | head -5

# Does Vite build succeed?
cd /home/runner/workspace && npx vite build 2>&1 | tail -20

# Check for nested Radix packages (the original cause)
ls node_modules/@radix-ui/react-collapsible/node_modules/ 2>/dev/null
# If this directory EXISTS, the fix didn't work

# Check which version is installed
cat node_modules/@radix-ui/react-collapsible/package.json | grep version
```

### STEP 2: If nested packages still exist, do a clean install

```bash
# Remove the problematic package
npm uninstall @radix-ui/react-collapsible

# Clean node_modules cache
rm -rf node_modules/.vite
rm -rf node_modules/.cache

# Reinstall with exact compatible version
npm install @radix-ui/react-collapsible@1.1.4 --save-exact

# Verify no nested packages
ls node_modules/@radix-ui/react-collapsible/node_modules/ 2>/dev/null && echo "STILL NESTED - PROBLEM" || echo "CLEAN - OK"
```

### STEP 3: If the package is clean but still failing, check for build errors

```bash
# Full build check
npx vite build 2>&1 | grep -i "error\|fail\|cannot" | head -20

# Check for TypeScript errors
npx tsc --noEmit 2>&1 | grep "error" | head -20

# Check for duplicate variable declarations (found 12 in audit)
npx vite build 2>&1 | grep "duplicate\|redeclare\|already been declared" | head -20
```

### STEP 4: If build errors exist, fix them

The audit found 12 duplicate variable declarations. Verify ALL were fixed:

```bash
# Check each file mentioned in audit
for f in "admin/fabrika-pin-yonetimi.tsx" "employee-of-month.tsx" "ik.tsx" "kayip-esya.tsx" "muhasebe-raporlama.tsx" "my-performance.tsx" "satinalma/satinalma-dashboard.tsx" "satinalma/stok-yonetimi.tsx" "sube/dashboard.tsx" "yonetim/checklist-takip.tsx" "yonetim/kullanicilar.tsx" "yonetim/servis-talepleri.tsx"; do
  DUPES=$(grep -c "isLoading }" "client/src/pages/$f" 2>/dev/null)
  if [ "$DUPES" -gt "1" ]; then
    echo "DUPLICATE isLoading in $f ($DUPES occurrences)"
  fi
done
```

### STEP 5: Nuclear option if nothing works

If steps 1-4 don't resolve the issue:

```bash
# Complete clean rebuild
rm -rf node_modules
rm -rf client/dist
rm package-lock.json

# Fresh install
npm install

# Pin the problematic package
npm install @radix-ui/react-collapsible@1.1.4 --save-exact

# Rebuild
npx vite build 2>&1 | tail -20

# Restart server
npm run dev
```

### STEP 6: Verify the app loads

```bash
# Wait for server to start
sleep 10

# Check server health
curl -s http://localhost:5000/api/health

# Check frontend loads (should return HTML, not error)
curl -s http://localhost:5000/ | head -5
# Should contain <!DOCTYPE html> or similar

# Check browser console simulation
curl -s http://localhost:5000/ | grep -i "error\|fail" | head -5
# Should return nothing
```

### IMPORTANT:
- Do NOT add any new packages until the app is stable
- Do NOT continue with Sprint 25 until this is resolved
- Pin ALL Radix UI packages to exact versions in package.json after fix
- Test that login works: curl POST to /api/login with valid credentials

### After fix, report:
1. What was the actual cause (nested packages? build error? something else?)
2. What exact fix was applied
3. Vite build output (success/failure)
4. Can users login and see their dashboard?

---

Bu en acil fix — uygulama tamamen çökmüş. Sprint 25'i bırak, önce bunu çöz. Sonucu getir.