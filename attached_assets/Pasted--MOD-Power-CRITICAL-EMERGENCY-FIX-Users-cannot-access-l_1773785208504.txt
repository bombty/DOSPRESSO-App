🔧 **MOD: Power**

```
CRITICAL EMERGENCY FIX — Users cannot access login page.

The KioskRoleGuard is still blocking /login. We need to completely 
remove KioskRoleGuard from wrapping the entire app and replace with 
a simpler approach.

## STEP 1 — Check exact current state
grep -n -A 35 "function KioskRoleGuard" client/src/App.tsx
grep -n "KioskRoleGuard" client/src/App.tsx

## STEP 2 — Nuclear fix: Remove KioskRoleGuard wrapper

In App.tsx, find where KioskRoleGuard wraps AppContent:
```tsx
// REMOVE THIS:
<KioskRoleGuard>
  <AppContent />
</KioskRoleGuard>

// REPLACE WITH:
<AppContent />
```

Delete the KioskRoleGuard component entirely.

## STEP 3 — Instead, add redirect in role-routes.ts

The kiosk redirect should happen AFTER login, not by wrapping the whole app.

In client/src/lib/role-routes.ts, the getRoleHomePath function already 
returns '/fabrika/kiosk' for fabrika_operator.

The login page uses this function to redirect after login.
That's enough — no need for a global guard.

## STEP 4 — Add simple protection in kiosk pages only

In fabrika/kiosk.tsx and sube/kiosk.tsx, add a check at the top:
If authenticated user is NOT a kiosk role, redirect to their home page.
This is simpler and safer than a global guard.

```tsx
// At top of FabrikaKiosk component:
const { user } = useAuth();
const [, setLocation] = useLocation();

useEffect(() => {
  if (!user) return; // not logged in = show device password (fine)
  // If logged in user is NOT a kiosk role, redirect them away
  const kioskRoles = ['fabrika_operator', 'fabrika_personel', 
                       'fabrika_sorumlu', 'sube_kiosk', 'admin'];
  if (!kioskRoles.includes(user.role)) {
    setLocation('/'); // redirect non-kiosk users away
  }
}, [user]);
```

## STEP 5 — Verify /login is in PUBLIC_PATH_PREFIXES

grep -n "PUBLIC_PATH_PREFIXES\|publicPath" client/src/App.tsx | head -10

Make sure '/login' is in the list. If not, add it.

## After fix, verify:
- [ ] Visiting the site shows /login (not kiosk)
- [ ] fabrika + 0000 login → /fabrika/kiosk
- [ ] isiklar + 0000 login → /sube/kiosk/5  
- [ ] admin login → /hq-ozet
- [ ] TypeScript no new errors
```

Gönderin — KioskRoleGuard'ı tamamen kaldırıyoruz, daha basit yaklaşım kullanıyoruz.