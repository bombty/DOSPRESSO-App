## Şu an 3 paralel iş var — öncelik sırasıyla:

---

### ⚡ HEMEN — Şimdi yapılacak

**Adım 1:** Daha önce yazdığım Academy V3 Critical Fixes promptunu henüz göndermediyseniz, **göndermeden önce durdurun.** Yeni audit 3 ek kritik sorun ortaya çıkardı. İkisini birleştirip tek seferde göndermeliyiz — iki ayrı prompt Replit'i karıştırır.

Yeni eklenecek 3 bulgu:

**S1 — Bildirim linki yanlış (kritik):** `academy-v3.ts` satır 408 ve 688'de `"/akademi/webinarlar"` yerine `"/akademi-v3?tab=webinar"` olmalı.

**S3 — seedDefaultWebinars side effect:** Her server restart'ında çalışıyor, tarihler kayıyor. `if (process.env.NODE_ENV !== 'production' || webinarCount === 0)` gibi bir guard eklenecek.

**S6 — isPending paylaşımlı:** Bir webinara kayıt olunca tüm butonlar donuyor. Her webinar kartının kendi `isPending` state'i olmalı.

Hemen yazıyorum — tek birleşik prompt:

---

**Replit'e gönderilecek — Academy V3 Consolidated Fixes:**

```
Fix 6 issues in Academy V3. Apply minimal targeted fixes only.
Do NOT restructure logic. Do NOT touch academy.ts, academy-v2.ts, 
factory.ts, crm-routes.ts, or any non-Academy-V3 file.

---

FIX 1 — isRequired → isMandatory migration
FILE: shared/schema.ts + any file using isRequired outside of academy-v3.ts

Step 1: Run this SQL once on startup (wrap in try/catch, skip if already done):
  UPDATE training_modules 
  SET is_mandatory = true 
  WHERE is_required = true AND is_mandatory = false;

Step 2: In shared/schema.ts, add comment on isRequired:
  // @deprecated — use isMandatory. Do not write to this column.

Step 3: Search all client and server files (excluding shared/schema.ts 
and server/routes/academy-v3.ts) for "isRequired" or "is_required".
Replace each occurrence with isMandatory / is_mandatory.
List every file and line number you changed.

Do NOT drop the is_required column.

---

FIX 2 — HQ_ROLES frontend-backend sync
FILE: client/src/pages/akademi-v3/WebinarTab.tsx (~line 32)

Change the HQ_ROLES constant to exactly match backend (academy-v3.ts):
  const HQ_ROLES = ["admin", "ceo", "cgo", "coach", "trainer"];

Remove: mudur, supervisor, muhasebe_ik, fabrika_mudur
Backend is the source of truth. Do NOT change the backend.

---

FIX 3 — Webinar target roles multi-select
FILE: client/src/pages/akademi-v3/WebinarTab.tsx (CreateWebinarDialog)

Add targetRoles multi-select to the webinar creation form.
Add state: const [targetRoles, setTargetRoles] = useState<string[]>([])

Add a section labeled "Hedef Roller" with Shadcn Checkbox for each:
  stajyer → "Stajyer"
  bar_buddy → "Bar Buddy"  
  barista → "Barista"
  supervisor_buddy → "Supervisor Buddy"
  supervisor → "Supervisor"
  mudur → "Müdür"
  (add a "Tüm Roller" option that clears the array → sends [])

Include targetRoles in the POST request body.
If "Tüm Roller" is checked, send targetRoles as [].

---

FIX 4 — Category constants unified
Create NEW file: client/src/pages/akademi-v3/categoryConfig.ts

export const ACADEMY_CATEGORIES = [
  { key: "barista_temelleri", label: "Barista Temelleri", emoji: "☕" },
  { key: "musteri_iliskileri", label: "Müşteri Hizmetleri", emoji: "🤝" },
  { key: "urun_bilgisi", label: "Ürün Bilgisi", emoji: "📦" },
  { key: "hijyen_guvenlik", label: "Hijyen & Güvenlik", emoji: "🧼" },
  { key: "sube_operasyonu", label: "Şube Operasyonu", emoji: "🏪" },
  { key: "liderlik", label: "Liderlik", emoji: "🌟" },
  { key: "onboarding", label: "Onboarding", emoji: "🎯" },
] as const;

Then:
- In HomeTab.tsx: remove local CATEGORY_CONFIG, import ACADEMY_CATEGORIES.
  Show first 6 in grid (exclude "onboarding" from homepage).
  On click: navigate(`/akademi-v3?tab=egitimler&category=${key}`)

- In TrainingTab.tsx: remove local CATEGORY_LABELS and FILTER_CATEGORIES,
  import ACADEMY_CATEGORIES. Use it for filter chips.

- In AkademiV3 index.tsx (shell): on mount, read ?tab and ?category 
  from window.location.search. Set active tab and pass initialCategory 
  to TrainingTab as a prop.

---

FIX 5 — Notification links pointing to wrong route
FILE: server/routes/academy-v3.ts, lines ~408 and ~688

Change all occurrences of:
  link: "/akademi/webinarlar"
to:
  link: "/akademi-v3?tab=webinar"

Search the entire file for "/akademi/webinarlar" and replace all.

---

FIX 6 — seedDefaultWebinars side effect on every restart
FILE: server/routes/academy-v3.ts (~line 838)

The current call seedDefaultWebinars() runs on every module load 
and uses Date.now() — webinar dates drift on each restart.

Replace the auto-call with a guard:
  async function seedDefaultWebinars() {
    const count = await db.select({ count: sql<number>`count(*)` })
      .from(webinars);
    if (count[0].count >= 3) return; // already seeded, skip
    
    // Use FIXED future dates, not Date.now()
    const baseDate = new Date("2026-04-01T10:00:00");
    // ... rest of seed logic with fixed dates
  }

The webinar titles and content stay the same, only dates become 
fixed (not relative to now()).

---

BONUS FIX (small) — isPending shared state in WebinarTab
FILE: client/src/pages/akademi-v3/WebinarTab.tsx (~line 305)

Currently: const isPending = registerMutation.isPending || unregisterMutation.isPending
This disables ALL webinar register buttons when any one is loading.

Fix: Pass the webinar id into each button's disabled check:
  disabled={registeringId === webinar.id || unregisteringId === webinar.id}

Add state:
  const [registeringId, setRegisteringId] = useState<number | null>(null)
  const [unregisteringId, setUnregisteringId] = useState<number | null>(null)

Set the id before mutation, clear it in onSuccess/onError/onSettled.

---

After ALL fixes, confirm with:
1. List of every file modified with line numbers
2. Confirm seedDefaultWebinars no longer uses Date.now()
3. Confirm notification links now point to /akademi-v3?tab=webinar
4. Confirm ACADEMY_CATEGORIES is the single source of truth for both tabs
```

---

### ⏳ BEKLEYEN — Yanıt bekliyorum

**FAB-0A doğrulama sonuçları** henüz gelmedi. O 4 sorgunun çıktısını paylaşın — görmem gereken:
- Kategori bazlı ürün sayısı
- NULL product_id kayıt sayısı
- Benchmark tablosunda 7 satır var mı
- Tedarikçi sayısı

Bu gelince FAB-0B (Kiosk Timer UI) hemen yazıyorum.

---

### 📋 Özet: Şu an ne yapacaksınız

| Adım | İş | Durum |
|---|---|---|
| 1 | Yukarıdaki Academy V3 Consolidated Fixes → Replit'e gönder | **Şimdi** |
| 2 | FAB-0A doğrulama sonuçlarını buraya yapıştır | **Bekliyor** |
| 3 | FAB-0B promptu | Adım 2 gelince |