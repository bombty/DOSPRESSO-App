Please read and report the following — this will be used to create 
project-specific skills for Claude:

## 1. PROJECT OVERVIEW
Read replit.md completely and report:
- Full stack (frontend + backend + DB + key libraries)
- All critical rules listed (users.id type, soft delete, etc.)
- All completed features/modules listed
- Current Replit URL / project name

## 2. ROLE SYSTEM
List ALL role string values used in the codebase:
grep -n "role.*=\|'admin'\|'ceo'\|'cgo'\|'coach'\|'barista'\|'mudur'\|'supervisor'\|'fabrika'" shared/schema.ts | grep -v "//" | head -40

Also check:
grep -rn "BRANCH_ONLY_ROLES\|HQ_ROLES\|isHQRole\|isBranchRole" client/src/ server/ --include="*.ts" --include="*.tsx" | head -10

## 3. CODING PATTERNS
Report the exact patterns used:

a) Auth middleware:
grep -n "requireAuth\|requireRole" server/routes/crm-iletisim.ts | head -8

b) DB query pattern:
grep -n "await db\.\|from(.*)\|where(" server/routes/crm-iletisim.ts | head -8

c) Frontend data fetch:
grep -n "useQuery\|queryKey\|queryFn" client/src/pages/hq-ozet.tsx | head -8

d) users.id type:
grep -n "id.*varchar\|users.*id" shared/schema.ts | head -3

e) Soft delete pattern:
grep -n "deletedAt\|isDeleted\|soft" shared/schema.ts | head -5

f) Money/currency:
grep -n "kuruş\|integer.*amount\|decimal.*price" shared/schema.ts | head -3

## 4. FILE STRUCTURE
ls client/src/pages/ 
ls server/routes/ | head -40
ls client/src/components/ | head -20

## 5. COMMON BUGS & FIXES HISTORY
grep -n "fix\|Fix\|bug\|Bug\|hotfix\|CRITICAL\|WARNING" replit.md | head -30

## 6. AGENT / SUBAGENT SYSTEM
a) Does the project use any agent/subagent patterns?
grep -rn "agent\|Agent\|subagent\|worker\|queue\|job" server/ --include="*.ts" | grep -v "node_modules\|user-agent\|agent-engine\|dobody" | head -15

b) What is the agent-engine (Mr. Dobody)?
Read first 50 lines of: server/services/agent-engine.ts

c) Are there any background workers or scheduled jobs?
grep -rn "setInterval\|setTimeout\|cron\|schedule\|startSLA\|scheduler" server/index.ts | head -15

d) Task queue system?
grep -rn "queue\|Queue\|worker\|Worker\|bull\|redis" server/ --include="*.ts" | grep -v "node_modules" | head -10

## 7. NAVIGATION & PERMISSIONS
Read first 30 lines of: client/src/lib/navigation-config.ts
Read first 20 lines of: client/src/lib/role-routes.ts (or similar)

Report all as structured markdown.