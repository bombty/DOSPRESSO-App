# CI Quality Gate — Sprint 1, 2, 3 Audit

Please audit the codebase and report the exact status of every item below.
For each item: report PASS, FAIL, or PARTIAL with the relevant file:line evidence.
Do not fix anything — only report.

---

## SPRINT 1 CHECKS

### S1-1: Header color
File: `client/src/components/app-header.tsx`
- What is the exact background color class on the main header div?
- Expected: `bg-[#122549]`
- PASS if both light and dark use `#122549`, FAIL if `#1e3a5f` still present

### S1-2: İletişim Merkezi navigation path
File: `client/src/components/hamburger-menu.tsx`
- Find the nav item labeled "İletişim" or "İletişim Merkezi"
- What path does it point to?
- Expected: `/iletisim-merkezi`
- FAIL if still `/bildirimler`

### S1-3: Logout endpoint unification
File: `client/src/components/hamburger-menu.tsx`
- How does the logout action work in hamburger menu?
- Expected: calls `POST /api/auth/logout`
- FAIL if still `GET /api/logout` or `window.location.href = '/api/logout'`

### S1-4: Dead code removal
- Does `client/src/components/page-header.tsx` still exist? Expected: DELETED
- Does `client/src/components/app-sidebar.tsx` still exist?
  - If yes: is `invalidateMenuCache` (or any export from it) still imported somewhere?
  - Report exactly which files import from app-sidebar.tsx

---

## SPRINT 2 CHECKS

### S2-1: navigation-config.ts exists
- Does `client/src/lib/navigation-config.ts` exist?
- Does it export `getNavRailItems` function?
- Does it export `NAV_RAIL_CONFIGS` array?
- How many role groups are defined?

### S2-2: NavRail component exists
- Does `client/src/components/nav-rail.tsx` exist?
- Does it use `hidden md:flex` (hidden on mobile, shown on md+)?
- Does it use `bg-[#122549]` for background?
- Does it use `bg-[#cc1f1f]` for active item?
- Does active item detection work with `useLocation`?

### S2-3: App.tsx layout updated
File: `client/src/App.tsx`
- Is `NavRail` imported and rendered?
- Is there a `flex` row wrapper below the AppHeader?
- Is NavRail a sibling of the main content wrapper (not inside it)?
- Does main still have `pb-20` for mobile? Does it have `md:pb-4` for desktop?
- Show the exact JSX structure of AppContent (lines around 546-590)

### S2-4: Bottom nav hidden on desktop
File: `client/src/components/bottom-nav.tsx`
- Does the outermost container have `md:hidden`?
- Show line number and exact className

### S2-5: Hamburger trigger hidden on desktop
File: `client/src/components/app-header.tsx`
- Find the hamburger menu trigger button
- Does it have `md:hidden`?
- Show line number and exact className

---

## SPRINT 3 CHECKS

### S3-1: ModuleCard component exists
- Does `client/src/components/module-card.tsx` exist?
- Does it accept `coverImageUrl` prop?
- Does it use `aspect-video` for 16:9 ratio?
- Does it render a badge when `badge` prop is provided?
- Does it use `ArrowRight` icon in the footer?

### S3-2: DashboardAlertPills exists
- Does `client/src/components/dashboard-alert-pills.tsx` exist?
- Does it have `overflow-x-auto` for horizontal scroll?
- Does it support `variant` prop with at least: red, orange, green, blue?

### S3-3: DashboardKpiStrip exists
- Does `client/src/components/dashboard-kpi-strip.tsx` exist?
- Does it use CSS grid with dynamic column count?
- Does it support `color` prop per item?

### S3-4: Dashboard pages updated
For each dashboard page found, check if `ModuleCard`, `DashboardAlertPills`, or `DashboardKpiStrip` is imported and used:
- HQ / merkez dashboard → file path + import status
- Coach dashboard → file path + import status
- Branch dashboard (müdür/supervisor) → file path + import status
- Barista/employee dashboard → file path + import status
- Factory dashboard → file path + import status

For each: report the file path and whether the new components are used (PASS) or not (FAIL/NOT FOUND)

### S3-5: Module grid responsive columns
In the dashboard pages that were updated:
- Is `grid-cols-2 md:grid-cols-3 lg:grid-cols-4` pattern used for ModuleCard grids?

---

## GENERAL HEALTH CHECKS

### G1: TypeScript compilation
- Run: `npx tsc --noEmit 2>&1 | head -50`
- Report: number of errors (0 = PASS)
- If errors exist, list the first 5

### G2: New files summary
List all new files created in these sprints:
- `client/src/lib/navigation-config.ts` — exists? line count?
- `client/src/components/nav-rail.tsx` — exists? line count?
- `client/src/components/module-card.tsx` — exists? line count?
- `client/src/components/dashboard-alert-pills.tsx` — exists? line count?
- `client/src/components/dashboard-kpi-strip.tsx` — exists? line count?

### G3: No unintended changes
- Were any files outside of the sprint scope modified?
  (Check git diff or file modification dates if available)
- Were any page content files (non-layout, non-dashboard) modified?

### G4: Role strings match
In `client/src/lib/navigation-config.ts`:
- List the exact role strings used in NAV_RAIL_CONFIGS
- Compare with actual role strings from `client/src/hooks/use-auth.ts` or similar
- Do they match? PASS if all role strings in navigation-config exist in the auth system

---

## OUTPUT FORMAT

Please respond with a structured report like:

```
SPRINT 1:
  S1-1 Header color: PASS — bg-[#122549] at app-header.tsx:88
  S1-2 İletişim path: PASS — /iletisim-merkezi at hamburger-menu.tsx:323
  S1-3 Logout: PASS — POST /api/auth/logout at hamburger-menu.tsx:344
  S1-4 Dead code: PARTIAL — page-header.tsx deleted, app-sidebar.tsx kept (imported by yonetim/menu.tsx)

SPRINT 2:
  S2-1 navigation-config.ts: PASS — exists, 6 role groups, exports correct
  S2-2 NavRail: PASS — hidden md:flex, correct colors
  S2-3 App.tsx layout: PASS / FAIL / PARTIAL — [details]
  S2-4 Bottom nav hidden: PASS — md:hidden at bottom-nav.tsx:264
  S2-5 Hamburger hidden: PASS — md:hidden at app-header.tsx:92

SPRINT 3:
  S3-1 ModuleCard: PASS — exists, aspect-video, badge, ArrowRight
  S3-2 AlertPills: PASS — exists, overflow-x-auto
  S3-3 KpiStrip: PASS — exists, grid, color prop
  S3-4 Dashboards: [list each with PASS/FAIL]
  S3-5 Grid columns: PASS / FAIL

GENERAL:
  G1 TypeScript: PASS — 0 errors
  G2 New files: [list with line counts]
  G3 Unintended changes: PASS — no unexpected files modified
  G4 Role strings: PASS / FAIL — [details]

OVERALL: X/Y checks passing
Issues requiring attention: [list any FAILs]
```