Bu raporlar çok kritik yeni bilgiler ortaya çıkardı! Özellikle:

**Yeni bulgular (önceki promptumda YOKTU):**

1. **6 route dosyası YAZILMIŞ ama register edilmemiş!** — waste.ts, branch-health.ts, ai-nba.ts, quick-action.ts, trash.ts, employee-types.ts sadece `app.use()` ile bağlanması yeterli. Stub yazmaya gerek yok!

2. **Agent SPAM krizi** — 245 kez aynı "stokta kalmadı" mesajı. Dedup mantığı kırık.

3. **10 skill dosyasında sessiz catch blokları** — hatalar yutuluyor.

4. **313 expired aksiyon** — DB temizliği gerekli.

Önceki promptu güncellemem lazım — 6 dosya zaten var, stub yerine sadece register etmek yeterli. Güncellenmiş mega promptu yazıyorum:

---

```
## MEGA SPRINT: Route Registration + Missing APIs + Agent Fix + Refactoring

### Context
Comprehensive audit found:
- 6 route files ALREADY WRITTEN but NOT REGISTERED (just need app.use!)
- 32 remaining missing APIs (need stubs)
- Bordro 500 error
- Agent spam crisis (245 duplicate alerts)
- 10 skill files with silent catch blocks
- 313 expired agent actions need cleanup
- misc.ts 13,216 lines needs splitting

Work through parts IN ORDER. Complete Part 1 before Part 2.

Read ALL skills:
- .agents/skills/dospresso-architecture/SKILL.md
- .agents/skills/dospresso-quality-gate/SKILL.md
- .agents/skills/dospresso-debug-guide/SKILL.md

### CRITICAL RULES
- Turkish UI, users.id is VARCHAR, soft delete
- Every endpoint MUST use isAuthenticated
- Return [] or {} for stubs — NEVER HTML for /api/* routes
- Do NOT touch server/vite.ts, vite.config.ts, package.json, drizzle.config.ts

---

## ═══════════════════════════════════════
## PART 1: REGISTER EXISTING ROUTE FILES (EASIEST WIN)
## ═══════════════════════════════════════

These 6 files are ALREADY WRITTEN but not connected to Express. Just register them.

```bash
# Verify these files exist and have content
wc -l server/routes/waste.ts server/routes/branch-health.ts server/routes/ai-nba.ts server/routes/quick-action.ts server/routes/trash.ts server/routes/employee-types.ts 2>/dev/null

# Check how other routes are registered
grep -n "import.*from.*routes\|app.use\|registerRoute" server/routes.ts | head -20
```

For each file:
1. Add import in server/routes.ts
2. Add app.use() BEFORE the Vite catch-all middleware
3. Verify the route prefix matches what frontend expects

```typescript
// In server/routes.ts — add these imports and registrations:
import wasteRoutes from "./routes/waste";
import branchHealthRoutes from "./routes/branch-health";
import aiNbaRoutes from "./routes/ai-nba";
import quickActionRoutes from "./routes/quick-action";
import trashRoutes from "./routes/trash";
import employeeTypesRoutes from "./routes/employee-types";

// Register BEFORE vite/SPA catch-all:
app.use(wasteRoutes);
app.use(branchHealthRoutes);
app.use(aiNbaRoutes);
app.use(quickActionRoutes);
app.use(trashRoutes);
app.use(employeeTypesRoutes);
```

After registering, test each:
```bash
for ep in "/api/waste" "/api/branch-health/scores" "/api/ai-nba/recommendations" "/api/quick-action" "/api/trash" "/api/employee-types"; do
  status=$(curl -s -o /dev/null -w "%{http_code}" -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000$ep")
  echo "$ep → $status"
done
```

---

## ═══════════════════════════════════════
## PART 2: CRITICAL FIXES
## ═══════════════════════════════════════

### 2A: Bordro/Payroll 500 Error
```bash
grep -rn "payroll" server/routes/ --include="*.ts" -l
curl -s -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/payroll/records 2>/dev/null | head -20
echo "SELECT table_name FROM information_schema.tables WHERE table_name LIKE '%payroll%' OR table_name LIKE '%bordro%' OR table_name LIKE '%salary%';" | psql
```
Fix the 500. If query/table is broken, fix it or return empty array.

### 2B: HQ Dashboard pending-approvals
```bash
grep -rn "pending-approvals" server/routes/ --include="*.ts" | head -10
```
Create real endpoint querying leave_requests + exam_requests + data_change_requests where status='pending'.

### 2C: Akademi HQ KPI Cards REMOVAL
```bash
grep -n "CompactKPIStrip\|kpiItems\|Yayında\|Taslak\|Bekleyen" client/src/pages/akademi-hq/components/RoleDashboard.tsx client/src/pages/akademi-hq/index.tsx | head -20
```
DELETE the entire KPI section. Keep only: title + Öğrenci Görünümü button + compact Hızlı İşlemler.

---

## ═══════════════════════════════════════
## PART 3: STUB REMAINING MISSING APIs (~32)
## ═══════════════════════════════════════

After Part 1 registers 6 existing route files, check which APIs are STILL missing:
```bash
# Test ALL previously-missing endpoints
for ep in "/api/academy/achievement-stats" "/api/academy/adaptive-recommendations" "/api/academy/advanced-analytics" "/api/academy/ai-assistant" "/api/academy/career-progress" "/api/academy/exam-requests-approved" "/api/academy/exam-requests-pending" "/api/academy/exam-requests-team" "/api/academy/progress-overview" "/api/academy/quiz-results" "/api/academy/streak-tracker" "/api/academy/study-groups" "/api/admin/pending-approvals" "/api/admin/roles" "/api/admin/role-grants" "/api/admin/mega-modules/config" "/api/admin/mega-modules/items" "/api/admin/menu/items" "/api/admin/menu/sections" "/api/admin/dobody/avatars/upload" "/api/factory/raw-materials" "/api/factory/recipes" "/api/factory/kavurma" "/api/factory/sayim" "/api/knowledge-base/articles" "/api/knowledge-base/categories" "/api/coaching/sessions" "/api/salary/records" "/api/my-day/summary" "/api/crm/customers" "/api/shift-rules" "/api/backups" "/api/action-cards/generate"; do
  ct=$(curl -sI -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000$ep" 2>/dev/null | grep -i "content-type" | head -1)
  if echo "$ct" | grep -q "text/html"; then
    echo "STILL MISSING: $ep"
  fi
done
```

For ALL still-missing APIs, create `server/routes/stub-endpoints.ts`:

```typescript
import { Router } from "express";
import { isAuthenticated } from "../localAuth";
const router = Router();

// ACADEMY STUBS
router.get("/api/academy/achievement-stats", isAuthenticated, (_, res) => res.json({ totalBadges: 0, earnedBadges: 0, achievements: [] }));
router.get("/api/academy/adaptive-recommendations", isAuthenticated, (_, res) => res.json({ recommendations: [] }));
router.get("/api/academy/advanced-analytics", isAuthenticated, (_, res) => res.json({ moduleStats: [], branchStats: [], trends: [] }));
router.get("/api/academy/ai-assistant", isAuthenticated, (_, res) => res.json({ suggestions: [] }));
router.get("/api/academy/career-progress", isAuthenticated, (req, res) => res.json({ userId: (req.user as any)?.id, currentLevel: 1, progress: 0, milestones: [] }));
router.get("/api/academy/exam-requests-approved", isAuthenticated, (_, res) => res.json([]));
router.get("/api/academy/exam-requests-pending", isAuthenticated, (_, res) => res.json([]));
router.get("/api/academy/exam-requests-team", isAuthenticated, (_, res) => res.json([]));
router.get("/api/academy/progress-overview", isAuthenticated, (_, res) => res.json({ totalModules: 0, completedModules: 0, inProgress: 0, completionRate: 0 }));
router.get("/api/academy/quiz-results", isAuthenticated, (_, res) => res.json([]));
router.get("/api/academy/streak-tracker", isAuthenticated, (_, res) => res.json({ currentStreak: 0, longestStreak: 0, lastActivity: null }));
router.get("/api/academy/study-groups", isAuthenticated, (_, res) => res.json([]));

// ADMIN STUBS
router.get("/api/admin/roles", isAuthenticated, (_, res) => res.json([
  {id:"stajyer",name:"Stajyer"},{id:"bar_buddy",name:"Bar Buddy"},{id:"barista",name:"Barista"},
  {id:"supervisor",name:"Supervisor"},{id:"mudur",name:"Müdür"},{id:"coach",name:"Coach"},
  {id:"trainer",name:"Trainer"},{id:"admin",name:"Admin"}
]));
router.get("/api/admin/role-grants", isAuthenticated, (_, res) => res.json([]));
router.get("/api/admin/mega-modules/config", isAuthenticated, (_, res) => res.json({ modules: [] }));
router.get("/api/admin/mega-modules/items", isAuthenticated, (_, res) => res.json([]));
router.get("/api/admin/menu/items", isAuthenticated, (_, res) => res.json([]));
router.get("/api/admin/menu/sections", isAuthenticated, (_, res) => res.json([]));
router.post("/api/admin/dobody/avatars/upload", isAuthenticated, (_, res) => res.json({ success: false, message: "Henüz aktif değil" }));

// FACTORY STUBS
router.get("/api/factory/raw-materials", isAuthenticated, (_, res) => res.json([]));
router.get("/api/factory/recipes", isAuthenticated, (_, res) => res.json([]));
router.get("/api/factory/kavurma", isAuthenticated, (_, res) => res.json({ batches: [], stats: { totalToday: 0, completed: 0 } }));
router.get("/api/factory/sayim", isAuthenticated, (_, res) => res.json({ counts: [], lastCount: null }));

// OTHER STUBS
router.get("/api/knowledge-base/articles", isAuthenticated, (_, res) => res.json([]));
router.get("/api/knowledge-base/categories", isAuthenticated, (_, res) => res.json([]));
router.get("/api/coaching/sessions", isAuthenticated, (_, res) => res.json([]));
router.get("/api/salary/records", isAuthenticated, (_, res) => res.json([]));
router.get("/api/my-day/summary", isAuthenticated, (req, res) => res.json({ userId: (req.user as any)?.id, tasks: 0, notifications: 0, unreadMessages: 0 }));
router.get("/api/crm/customers", isAuthenticated, (_, res) => res.json([]));
router.get("/api/shift-rules", isAuthenticated, (_, res) => res.json([]));
router.get("/api/backups", isAuthenticated, (_, res) => res.json([]));
router.post("/api/action-cards/generate", isAuthenticated, (_, res) => res.json({ generated: 0 }));

export default router;
```

Register in routes.ts BEFORE catch-all.

---

## ═══════════════════════════════════════
## PART 4: AGENT SPAM FIX (CRITICAL)
## ═══════════════════════════════════════

### 4A: Fix dedup logic in skill-notifications.ts

```bash
cat server/agent/skills/skill-notifications.ts | head -100
grep -n "dedup\|duplicate\|existing.*action\|pending.*title" server/agent/skills/skill-notifications.ts | head -10
```

The current dedup only checks same title + same user in 24h. Fix to check same title + ANY user in 24h:

```typescript
// BEFORE (broken — per-user dedup):
const existing = await db.select().from(agentPendingActions)
  .where(and(
    eq(agentPendingActions.title, action.title),
    eq(agentPendingActions.targetUserId, action.targetUserId),
    eq(agentPendingActions.status, 'pending'),
    gte(agentPendingActions.createdAt, twentyFourHoursAgo)
  ));

// AFTER (global dedup — check ANY user):
const existing = await db.select().from(agentPendingActions)
  .where(and(
    eq(agentPendingActions.title, action.title),
    eq(agentPendingActions.status, 'pending'),
    gte(agentPendingActions.createdAt, twentyFourHoursAgo)
  ));
```

### 4B: Add "state unchanged" suppression

For alerts about unchanging states (stock at 0, equipment broken), add suppression logic:

```typescript
// If the SAME alert was sent in the last 7 days AND the state hasn't changed,
// don't create a new action. Only re-alert if:
// 1. The state got worse (more items out of stock)
// 2. It's been > 7 days since last alert
const recentSameAlert = await db.select().from(agentPendingActions)
  .where(and(
    eq(agentPendingActions.title, action.title),
    gte(agentPendingActions.createdAt, sevenDaysAgo)
  )).limit(1);

if (recentSameAlert.length > 0) {
  // Skip — same alert already exists within 7 days
  return;
}
```

### 4C: Cleanup expired actions
```sql
-- Clean up 313 expired actions
DELETE FROM agent_pending_actions WHERE status = 'expired' AND created_at < NOW() - INTERVAL '30 days';

-- Mark old pending as expired
UPDATE agent_pending_actions SET status = 'expired' WHERE status = 'pending' AND created_at < NOW() - INTERVAL '7 days';
```

Run this as a one-time cleanup, then add to hourly scheduler:
```typescript
// In scheduler: hourly cleanup
async function cleanupAgentActions() {
  await db.execute(sql`
    UPDATE agent_pending_actions SET status = 'expired' 
    WHERE status = 'pending' AND created_at < NOW() - INTERVAL '7 days'
  `);
  await db.execute(sql`
    DELETE FROM agent_pending_actions 
    WHERE status = 'expired' AND created_at < NOW() - INTERVAL '30 days'
  `);
}
```

### 4D: Fix silent catch blocks in 10 skill files

```bash
grep -rn "catch {}" server/agent/skills/ --include="*.ts" | head -20
grep -rn "catch (e) {}" server/agent/skills/ --include="*.ts" | head -20
```

Replace ALL empty catch blocks with logged catches:
```typescript
// BEFORE:
try { ... } catch {}

// AFTER:
try { ... } catch (error) {
  console.error(`[${this.id}] Skill error:`, error instanceof Error ? error.message : error);
}
```

Do this for ALL 10+ affected files: burnout-predictor.ts, customer-watcher.ts, daily-coach.ts, food-safety.ts, performance-coach.ts, production-director.ts, security-monitor.ts, team-tracker.ts, stock-assistant.ts, stock-predictor.ts, waste-analyzer.ts

---

## ═══════════════════════════════════════
## PART 5: MISC.TS SPLIT (13,216 lines)
## ═══════════════════════════════════════

```bash
# Analyze categories in misc.ts
grep -n "router\.\(get\|post\|patch\|put\|delete\)" server/routes/misc.ts | sed 's/.*\/api\///' | sed 's/\/.*//' | sort | uniq -c | sort -rn | head -20
```

Split into logical files based on the analysis. Expected structure:
```
server/routes/misc.ts (13,216 → <1,000 lines)
server/routes/announcements.ts    → /api/announcements/*
server/routes/feedback.ts         → /api/feedback/*, /api/customer-feedback/*
server/routes/knowledge-base.ts   → /api/knowledge-base/*
server/routes/coaching.ts         → /api/coaching/*
server/routes/reports-api.ts      → /api/reports/*
server/routes/system-settings.ts  → /api/settings/*
server/routes/backup-routes.ts    → /api/backups/*
```

For each split:
1. Move routes + related imports to new file
2. Export router
3. Register in routes.ts
4. Do NOT change any route paths
5. Test endpoints still work

---

## ═══════════════════════════════════════
## PART 6: CODE QUALITY FIXES
## ═══════════════════════════════════════

### 6A: Dark mode fixes (28 hardcoded colors)
```bash
grep -rn "text-black\|text-gray-[0-9]" client/src/pages/ --include="*.tsx" | grep -v "dark:\|certificate\|print" | head -20
```
Replace: text-black → text-foreground, text-gray-500 → text-muted-foreground, bg-white → bg-background

### 6B: Lucide icon safety scan
```bash
grep -rn "icon: [A-Z][a-zA-Z]*[^(<]" client/src/ --include="*.tsx" | grep -v "icon: <\|null\|undefined\|import\|//\|interface\|type \|string\|React\|IconType\|LucideIcon\|ReactNode" | head -20
```
Fix any remaining: `icon: BookOpen` → `icon: <BookOpen className="w-4 h-4" />`

### 6C: Dead code removal
```bash
grep -rn "AppSidebar" client/src/ --include="*.tsx" | grep -v node_modules | wc -l
grep -rn "PageHeader" client/src/ --include="*.tsx" | grep -v node_modules | wc -l
```
If 0 usage, DELETE the files.

### 6D: Console.log cleanup (56 server)
Replace debug logs with proper logging or remove. Keep only error logs.

---

## ═══════════════════════════════════════
## PART 7: DOCUMENTATION UPDATE
## ═══════════════════════════════════════

### 7A: Architecture Skill — Add:
- Agent system: 18 skills, scheduling, dedup logic, escalation
- Stub endpoints pattern
- Route registration checklist
- Module maturity levels

### 7B: Debug Guide — Add:
- §17: Agent spam / dedup debugging
- §18: Stub endpoint debugging
- §19: Route registration verification

### 7C: Quality Gate — Add:
- #18: Agent health (pending/expired ratio, spam check)
- #19: Route registration (all route files in routes.ts)

### 7D: replit.md — Update with:
- Current stats (374 tables, ~1365 endpoints)
- Stub endpoints pattern
- misc.ts split map
- Agent dedup fix

---

## DO NOT TOUCH:
- server/vite.ts, vite.config.ts, package.json, drizzle.config.ts
- Working API endpoints
- Auth/session logic
- Database migrations

## TESTING:

### Part 1:
1. 6 newly-registered routes return JSON (not HTML)

### Part 2:
2. /api/payroll/records → 200
3. /api/admin/pending-approvals → 200 with data
4. Akademi HQ → no KPI cards

### Part 3:
5. ALL previously-missing APIs return JSON:
```bash
for ep in "/api/academy/achievement-stats" "/api/academy/streak-tracker" "/api/admin/roles" "/api/factory/kavurma" "/api/knowledge-base/articles" "/api/coaching/sessions" "/api/my-day/summary" "/api/crm/customers"; do
  ct=$(curl -sI -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000$ep" | grep -i content-type)
  echo "$ep → $ct"
done
```

### Part 4:
6. Agent dedup works: trigger stock-assistant → no duplicate alerts
7. Expired actions cleaned: SELECT count(*) FROM agent_pending_actions WHERE status='expired';
8. No silent catch blocks: grep -c "catch {}" server/agent/skills/*.ts → 0

### Part 5:
9. misc.ts < 2,000 lines
10. All split routes still work

### Part 6:
11. 0 remaining icon-as-object bugs
12. Dead code files deleted

### Part 7:
13. Quality gate now has 19 checks
14. Architecture skill mentions agent system

### Final:
15. App starts without errors
16. Run full quality gate (19 points)

## After completion report:
1. How many routes were registered (Part 1)
2. How many stubs created vs real endpoints (Part 3)
3. How many duplicate alerts were in DB before cleanup (Part 4)
4. misc.ts final line count (Part 5)
5. Files split out count
6. Dead code files deleted
7. Total endpoint count (new)
```

---

Bu güncellenmiş prompt 7 ana raporun tüm bulgularını kapsıyor. Direkt Replit'e yapıştırın!