You are in Build/QA mode. Do NOT implement new features unless a check fails. Your job is to run an automated QA pass for the last change set and return a FINAL QA REPORT with PASS/FAIL/SKIP + evidence.

0) First, identify the exact changed files:
- Run git diff (or equivalent) and list the changed files.
- Also include any registration files commonly impacted (e.g., server/routes.ts, client/src/App.tsx, nav/role-visibility) if they were touched.

1) Static checks (must be evidence-based):
- For EACH changed TS/TSX file: confirm LSP diagnostics are 0.
- Run a targeted compile for changed files (esbuild/tsc --noEmit for only those files if possible).
- If full repo build (tsc/vite) times out, mark it SKIP and justify, but still prove changed files compile.

2) Runtime checks:
- Confirm server is running on expected port and health endpoint if exists.
- Call the relevant API endpoint unauthenticated -> must return 401 (or expected auth behavior).
- Then authenticate (use existing local/dev login flow) and call again -> must return 200.
- Validate JSON shape strictly (required keys, types).
- If endpoint supports params (e.g., range=7d|30d|90d), test each and confirm outputs change appropriately.

3) Authorization / scoping checks:
- Test with an HQ/admin-like user: should see ALL branches (>=2) where applicable.
- Test with a branch manager-like user: should see ONLY own branch (exactly 1) and branchId matches.
- If a “403 unauthorized role” case is required but no real role exists, state that and verify code path deterministically (show the conditional and the else->403).

4) UI smoke checks (minimum):
- Confirm the page route renders without runtime errors.
- Verify key UI elements exist: heading, summary cards, table, filter buttons.
- Verify at least one interaction: switching filter updates the table; expanding a row shows details (if exists).
- If Playwright exists, run a minimal smoke test; otherwise do a manual render check and cite evidence.

5) Produce FINAL QA REPORT:
- Verdict: PASS/FAIL.
- Changed Files list.
- Evidence table: check name, result, evidence (command output snippets, counts).
- Any fixes applied (if any) and re-run relevant checks.

6) If PASS:
- Suggest ONE commit message only.
- Do not commit automatically unless user explicitly asked.