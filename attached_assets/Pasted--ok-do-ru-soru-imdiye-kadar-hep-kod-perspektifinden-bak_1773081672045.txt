Çok doğru soru. Şimdiye kadar hep **kod perspektifinden** baktık — crash var mı, auth var mı, veri var mı. Ama hiç **kullanıcı perspektifinden** tam bir gün simülasyonu yapmadık.

## Eksik Olan: Uçtan Uca Senaryo Testi

Şimdiye kadar yaptıklarımız:

| Test Tipi | Yapıldı mı? |
|-----------|-------------|
| Kod kalite kontrol (6 perspektif) | ✅ Her sprint sonrası |
| Crash tarama (.toFixed, null) | ✅ Bug scan |
| Güvenlik (auth, RBAC) | ✅ Audit |
| Veri hazırlık (seed) | ✅ Sprint 23 |
| **Gerçek kullanıcı senaryosu** | ❌ HİÇ YAPILMADI |
| **İş mantığı doğrulama** | ❌ Zincirler var ama sonuç doğru mu? |
| **Çapraz rol etkileşimi** | ❌ A'nın yaptığı B'de görünüyor mu? |
| **Edge case'ler** | ❌ Yanlış veri girince ne olur? |
| **Mobil cihaz testi** | ❌ Sadece screenshot'lar |

**En kritik eksik:** Hiçbir zaman "Işıklar şubesinde bir Pazartesi günü" simülasyonu yapmadık. Barista giriş yapıyor → checklist tamamlıyor → supervisor dashboard'da görünüyor mu? → müşteri feedback geliyor → composite score güncelleniyor mu? → ay sonu maaş doğru mu?

Replit'e yapıştır — bu en kapsamlı ve en değerli test:

---

## TASK: End-to-End Scenario Test — "A Day at Işıklar Branch" (NO CODE CHANGES)

Simulate a full working day at Işıklar branch (branch_id=5) by calling actual API endpoints as different users. Verify that EVERY action by one role is correctly reflected in other roles' views. Report ALL failures.

Do NOT modify any code. Only test and report.

### SETUP: Get Test User Credentials

```sql
SELECT id, username, role, branch_id, is_active FROM users 
WHERE branch_id = 5 AND is_active = true
ORDER BY role;
```

Identify: 1 barista, 1 supervisor, 1 mudur from Işıklar branch.
Also get: CEO (ali), CGO (utku), Coach (yavuz), Muhasebe (mahmut) credentials.

For each test, login as the specified user and call the API with their session cookie.

### SCENARIO 1: Morning Opening (08:00)

**Step 1.1 — Supervisor logs in:**
```bash
# Login as supervisor
curl -c /tmp/sup.cookie -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"[supervisor_username]","password":"dospresso123"}'

# Check Flow Mode tasks
curl -b /tmp/sup.cookie http://localhost:5000/api/dobody/flow-tasks
```
**VERIFY:** Flow tasks returned? Greeting correct for morning? Tasks include checklist?

**Step 1.2 — Supervisor views branch dashboard:**
```bash
curl -b /tmp/sup.cookie http://localhost:5000/api/branch-summary/5
```
**VERIFY:** Returns branch data with staff list, KPIs, warnings?

**Step 1.3 — Barista logs in:**
```bash
curl -c /tmp/bar.cookie -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"[barista_username]","password":"dospresso123"}'

# Check Flow Mode
curl -b /tmp/bar.cookie http://localhost:5000/api/dobody/flow-tasks
```
**VERIFY:** Barista gets different tasks than supervisor? Includes training recommendation?

### SCENARIO 2: Daily Checklist (08:30)

**Step 2.1 — Get today's checklist assignments for barista:**
```bash
curl -b /tmp/bar.cookie "http://localhost:5000/api/checklists/my-assignments?date=$(date +%Y-%m-%d)"
```
**VERIFY:** Returns assigned checklists? Opening checklist present?

**Step 2.2 — Complete a checklist:**
```bash
# Get the checklist items
curl -b /tmp/bar.cookie http://localhost:5000/api/checklists/[assignment_id]

# Complete each item (mark as done)
curl -b /tmp/bar.cookie -X POST http://localhost:5000/api/checklists/[assignment_id]/complete \
  -H "Content-Type: application/json" \
  -d '{"items": [checklist_item_ids], "completedAt": "2026-03-09T08:30:00"}'
```
**VERIFY:** Checklist marked as completed? Response includes completion timestamp?

**Step 2.3 — Supervisor sees checklist completion:**
```bash
curl -b /tmp/sup.cookie http://localhost:5000/api/branch-summary/5
```
**VERIFY:** Checklist completion percentage updated? Barista shows as "completed" in team status?

### SCENARIO 3: Training Module (09:00)

**Step 3.1 — Barista views training modules:**
```bash
curl -b /tmp/bar.cookie http://localhost:5000/api/academy/modules
```
**VERIFY:** Returns module list? Modules have content?

**Step 3.2 — Start a quiz:**
```bash
# Get quiz for a module
curl -b /tmp/bar.cookie http://localhost:5000/api/academy/quiz/[module_id]
```
**VERIFY:** Returns questions with options? Anti-cheat timer info included?

**Step 3.3 — Submit quiz answers:**
```bash
curl -b /tmp/bar.cookie -X POST http://localhost:5000/api/academy/quiz/[quiz_id]/submit \
  -H "Content-Type: application/json" \
  -d '{"answers": [{"questionId": 1, "selectedOption": 0}, ...], "duration": 120}'
```
**VERIFY:** Returns score? Pass/fail determined? Training completion recorded?

**Step 3.4 — Check if badge unlocked:**
```bash
curl -b /tmp/bar.cookie http://localhost:5000/api/academy/badges/my
```
**VERIFY:** If first quiz passed, "İlk Sınav" badge should unlock.

### SCENARIO 4: Fault Reporting (10:00)

**Step 4.1 — Barista reports equipment fault:**
```bash
curl -b /tmp/bar.cookie -X POST http://localhost:5000/api/faults \
  -H "Content-Type: application/json" \
  -d '{"equipmentId": [equipment_id], "description": "Blender garip ses çıkarıyor", "priority": "medium", "branchId": 5}'
```
**VERIFY:** Fault created? Returns fault ID?

**Step 4.2 — Supervisor gets notification:**
```bash
curl -b /tmp/sup.cookie http://localhost:5000/api/notifications?unread=true
```
**VERIFY:** Notification about new fault exists? Contains equipment name and barista name?

### SCENARIO 5: Customer Feedback (12:00)

**Step 5.1 — Submit customer feedback (public endpoint):**
```bash
curl -X POST http://localhost:5000/api/feedback/public \
  -H "Content-Type: application/json" \
  -d '{"branchId": 5, "rating": 4, "comment": "Kahve harika ama bekleme süresi uzundu", "customerName": "Test Müşteri"}'
```
**VERIFY:** Feedback created? Returns ID?

**Step 5.2 — Supervisor sees feedback:**
```bash
curl -b /tmp/sup.cookie "http://localhost:5000/api/feedback?branchId=5&limit=5"
```
**VERIFY:** New feedback appears? Rating and comment correct?

**Step 5.3 — CGO sees branch satisfaction score:**
```bash
curl -c /tmp/cgo.cookie -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"utku","password":"dospresso123"}'

curl -b /tmp/cgo.cookie http://localhost:5000/api/hq-summary
```
**VERIFY:** Branch satisfaction reflects the new feedback?

### SCENARIO 6: Task Assignment (14:00)

**Step 6.1 — Supervisor creates a task for barista:**
```bash
curl -b /tmp/sup.cookie -X POST http://localhost:5000/api/tasks \
  -H "Content-Type: application/json" \
  -d '{"title": "Depo düzenleme", "description": "Arka depoyu düzenle", "assignedTo": [barista_user_id], "priority": "medium", "dueDate": "2026-03-09"}'
```
**VERIFY:** Task created? Assigned to correct user?

**Step 6.2 — Barista sees the task:**
```bash
curl -b /tmp/bar.cookie http://localhost:5000/api/tasks/my
```
**VERIFY:** New task appears in barista's task list?

**Step 6.3 — Barista completes the task:**
```bash
curl -b /tmp/bar.cookie -X PATCH http://localhost:5000/api/tasks/[task_id] \
  -H "Content-Type: application/json" \
  -d '{"status": "completed"}'
```
**VERIFY:** Status updated? Supervisor notified?

### SCENARIO 7: PDKS Check-in/Check-out (08:00 / 16:00)

**Step 7.1 — Check PDKS endpoint exists:**
```bash
curl -b /tmp/bar.cookie -X POST http://localhost:5000/api/pdks/check-in \
  -H "Content-Type: application/json" \
  -d '{"userId": [barista_id], "branchId": 5, "checkInTime": "2026-03-09T08:05:00"}'
```
**VERIFY:** PDKS record created?

**Step 7.2 — Check-out:**
```bash
curl -b /tmp/bar.cookie -X POST http://localhost:5000/api/pdks/check-out \
  -H "Content-Type: application/json" \
  -d '{"userId": [barista_id], "checkOutTime": "2026-03-09T16:10:00"}'
```
**VERIFY:** Record updated with check-out time? Hours calculated?

### SCENARIO 8: Payroll Calculation (Month End)

**Step 8.1 — Calculate payroll for Işıklar branch:**
```bash
curl -c /tmp/muh.cookie -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"mahmut","password":"dospresso123"}'

curl -b /tmp/muh.cookie -X POST http://localhost:5000/api/pdks-payroll/calculate \
  -H "Content-Type: application/json" \
  -d '{"branchId": 5, "month": 3, "year": 2026}'
```
**VERIFY:** Returns payroll data? Salary amounts correct? Deductions applied for absences?

### SCENARIO 9: Factory → Branch Stock Chain

**Step 9.1 — Create a branch order from Işıklar:**
```bash
curl -b /tmp/sup.cookie -X POST http://localhost:5000/api/branch-orders \
  -H "Content-Type: application/json" \
  -d '{"branchId": 5, "items": [{"productId": 1, "quantity": 10}], "requestedDeliveryDate": "2026-03-10"}'
```
**VERIFY:** Order created with status "pending"?

**Step 9.2 — Check branch inventory before and after:**
```bash
curl -b /tmp/sup.cookie "http://localhost:5000/api/branch-inventory?branchId=5"
```
**VERIFY:** Inventory records exist? Stock counts reasonable?

### SCENARIO 10: Cross-Role Visibility (End of Day)

**Step 10.1 — CGO Komuta Merkezi:**
```bash
curl -b /tmp/cgo.cookie http://localhost:5000/api/ceo-command-center/overview
```
**VERIFY:** Shows branch count, personnel count, active faults, satisfaction scores? Işıklar data reflects today's activities?

**Step 10.2 — Coach sees Işıklar staff:**
```bash
curl -c /tmp/coach.cookie -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"yavuz","password":"dospresso123"}'

curl -b /tmp/coach.cookie http://localhost:5000/api/coaching/branch/5/staff
```
**VERIFY:** Shows barista with today's activity? Score updated?

**Step 10.3 — Mr. Dobody agent actions reflect today:**
```bash
curl -b /tmp/cgo.cookie http://localhost:5000/api/agent/actions/summary
```
**VERIFY:** Agent noticed today's activities? Generated relevant suggestions?

### SCENARIO 11: Security Boundary Tests

**Step 11.1 — Barista tries admin endpoint:**
```bash
curl -b /tmp/bar.cookie http://localhost:5000/api/admin/users
```
**VERIFY:** Returns 403 Forbidden?

**Step 11.2 — Barista tries different branch data:**
```bash
curl -b /tmp/bar.cookie http://localhost:5000/api/branch-summary/10
```
**VERIFY:** Returns 403 or scoped data (not branch 10 details)?

**Step 11.3 — Supervisor tries payroll:**
```bash
curl -b /tmp/sup.cookie http://localhost:5000/api/pdks-payroll/calculate \
  -H "Content-Type: application/json" \
  -d '{"branchId": 5, "month": 3, "year": 2026}'
```
**VERIFY:** Returns 403? (only muhasebe should calculate payroll)

### REPORT FORMAT

For each scenario step:
```
STEP: [step number]
ACTION: [what was done]
EXPECTED: [what should happen]
ACTUAL: [what actually happened]
STATUS: ✅ PASS / ❌ FAIL / ⚠️ PARTIAL
DETAILS: [response data or error message]
```

### FINAL SUMMARY

1. **Total steps tested:** ?
2. **Passed:** ?
3. **Failed:** ?
4. **Partial:** ?
5. **Critical failures** (data loss or security breach): ?
6. **Business logic failures** (wrong calculation or missing connection): ?
7. **UX failures** (data not showing where expected): ?

**OVERALL E2E SCORE: ?/100**

Do NOT fix anything. Only test and report all findings.

---

Bu test tam olarak "Işıklar şubesinde bir gün" simülasyonu — 11 senaryo, 30+ adım:

| Senaryo | Ne Test Ediyor |
|---------|---------------|
| 1. Sabah giriş | Login + Flow Mode |
| 2. Checklist | Tamamlama → supervisor'da görünme |
| 3. Eğitim | Quiz → badge → score |
| 4. Arıza | Bildirim zinciri |
| 5. Müşteri feedback | Public form → dashboard |
| 6. Görev atama | Supervisor → barista → tamamlama |
| 7. PDKS | Check-in/out → saat hesaplama |
| 8. Maaş | PDKS → bordro hesaplama |
| 9. Fabrika zinciri | Sipariş → stok |
| 10. Çapraz rol | CGO, coach, agent tümü görüyor mu? |
| 11. Güvenlik | Yetkisiz erişim engelleniyor mu? |

Replit'e yapıştır. Sonucu gelince gerçek pilot hazırlığına geçeriz.