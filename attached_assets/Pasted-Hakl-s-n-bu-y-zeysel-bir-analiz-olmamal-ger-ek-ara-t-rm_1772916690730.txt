Haklısın — bu yüzeysel bir analiz olmamalı, gerçek araştırmaya dayalı profesyonel bir çalışma olmalı. Adım adım gidelim:

**Adım 1:** Dışarıdan araştırma (en iyi uygulamalar, benchmark'lar)
**Adım 2:** Replit'ten detaylı sistem auditi
**Adım 3:** Tüm bulguları birleştirip final tasarım dokümanı

Önce Adım 1 — araştırma yapıyorum:Araştırma tamamlandı. Şimdi Adım 2 — Replit'ten detaylı audit. Bu çok önemli — mevcut sistemin gerçek durumunu görmemiz lazım.

Replit'e yapıştır:

---

## TASK: DOSPRESSO UX Mükemmellik Auditi — Tüm Sistem Analizi

Kod değişikliği YAPMA. Sadece analiz et ve ÇOK DETAYLI rapor ver.

Bu audit, AI destekli bir sistemin tüm kullanıcı rollerinde mükemmel uyumla çalışması için gerekli bilgiyi toplayacak.

---

### BÖLÜM A: Sidebar — Her Rol Ne Görüyor?

```bash
# 1. SIDEBAR_ALLOWED_ITEMS tam haritası — her rol kaç ve hangi öğeleri görüyor?
grep -A 500 "SIDEBAR_ALLOWED_ITEMS" server/menu-service.ts | head -600

# 2. MENU_BLUEPRINT tam listesi (tüm tanımlı öğeler)
grep -A 300 "MENU_BLUEPRINT\|menuBlueprint" server/menu-service.ts | head -400

# 3. Her rol için sidebar öğe sayısı
# (Bu kısmı manuel say — her rolün SIDEBAR_ALLOWED_ITEMS array'indeki eleman sayısı)
```

**Raporla:** Her 26 rol için:
- Sidebar'da kaç öğe görüyor?
- Hangi öğeler? (tam liste)
- 6'dan fazla olan roller hangisi?

---

### BÖLÜM B: Mega-Module Tab Sayıları

```bash
# Her mega-module'de kaç tab var?
for f in $(grep -rl "TabsTrigger" client/src/pages/ --include="*.tsx"); do
  count=$(grep -c "TabsTrigger" "$f")
  echo "$f: $count tabs"
done | sort -t: -k2 -rn | head -20
```

**Raporla:**
- 8+ tab olan sayfalar hangileri?
- Her birinde hangi tab'lar var?
- Bu tab'ların hepsi her role mi gösteriliyor yoksa rol bazlı filtreleme var mı?

---

### BÖLÜM C: Login Sonrası Yönlendirme

```bash
# 1. ROLE_HOME_ROUTES haritası
grep -A 40 "ROLE_HOME_ROUTES\|roleHome\|home.*route" client/src/ --include="*.tsx" --include="*.ts" -r | head -60

# 2. Login sonrası redirect mantığı
grep -rn "navigate.*dashboard\|redirect.*home\|location.*replace" client/src/pages/login* --include="*.tsx" | head -10

# 3. Dashboard.tsx redirect mantığı
grep -rn "redirect\|navigate\|replace" client/src/pages/dashboard.tsx | head -15
```

**Raporla:** Her rol giriş yaptığında hangi sayfaya düşüyor?

---

### BÖLÜM D: Bottom Navigation

```bash
# 1. Bottom nav tam içeriği
cat $(grep -rl "BottomNav\|bottom-nav\|bottomNav" client/src/components/ --include="*.tsx" | head -1)

# 2. Rol bazlı bottom nav farklılıkları
grep -rn "role\|isHQ\|isAdmin\|roleGroup" client/src/components/bottom* client/src/components/Bottom* --include="*.tsx" 2>/dev/null | head -20
```

**Raporla:**
- Bottom nav'da hangi butonlar var?
- Rol bazlı farklılık var mı?

---

### BÖLÜM E: Agent Skill'ler — Gerçek Çalışma Durumu

```bash
# 1. Skill registry
cat server/agent/skills/skill-registry.ts | head -80

# 2. Her skill dosyası var mı ve içeriği
ls server/agent/skills/*.ts 2>/dev/null

# 3. Scheduler — skill'ler ne zaman çalışıyor?
grep -rn "skill\|Skill\|schedule.*skill\|runSkill" server/ --include="*.ts" | grep -i "interval\|schedule\|cron\|hourly\|daily\|weekly" | head -20

# 4. Son skill çalışma logları
grep -rn "skillRun\|skill_run\|agentSkill" server/ --include="*.ts" | head -10
```

```sql
-- 5. Agent pending actions sayısı
SELECT COUNT(*) FROM agent_pending_actions;
SELECT action_type, COUNT(*) FROM agent_pending_actions GROUP BY action_type;

-- 6. Agent logs
SELECT COUNT(*) FROM ai_agent_logs;
SELECT action_type, COUNT(*) FROM ai_agent_logs GROUP BY action_type ORDER BY count DESC LIMIT 10;
```

**Raporla:**
- Kaç skill tanımlı ve aktif?
- Scheduler çalışıyor mu?
- Gerçek pending action var mı?
- AI agent log'larında ne var?

---

### BÖLÜM F: Dobody Suggestions — Dashboard Entegrasyonu

```bash
# 1. dobody-suggestions.ts kural sayısı
grep -c "suggestions.push\|insights.push" server/lib/dobody-suggestions.ts 2>/dev/null

# 2. Her summary endpoint'te suggestion entegre mi?
grep -rn "suggestions\|dobody\|Dobody" server/routes/my-day.ts server/routes/branch-summary.ts server/routes/hq-summary.ts server/routes/coach-summary.ts server/routes/franchise-summary.ts 2>/dev/null | head -20

# 3. Quick action API
grep -rn "quick-action\|quickAction" server/routes/ --include="*.ts" | head -10
```

**Raporla:**
- Kaç kural bazlı öneri tanımlı?
- Her dashboard'da öneriler görünüyor mu?
- Tek tıkla onay çalışıyor mu?

---

### BÖLÜM G: Fabrika Zinciri Bağlantı Durumu

```bash
# 1. Fabrika mega-module tab listesi
grep -rn "TabsTrigger" client/src/pages/fabrika* --include="*.tsx" | head -20

# 2. Kiosk 3 buton sistemi
grep -rn "quick-start\|quick-complete\|quick-end" server/routes/factory* --include="*.ts" | head -10

# 3. LOT sistemi
grep -rn "production_lots\|productionLots\|lot_number" server/ --include="*.ts" | wc -l

# 4. Sevkiyat → şube stok
grep -rn "branch_inventory\|branchInventory" server/ --include="*.ts" | wc -l

# 5. Sipariş sistemi
grep -rn "branch_order\|branchOrder" server/ --include="*.ts" | wc -l
```

```sql
-- 6. Fabrika veri durumu
SELECT 'factory_products' as tablo, COUNT(*) FROM factory_products
UNION ALL SELECT 'factory_stations', COUNT(*) FROM factory_stations
UNION ALL SELECT 'production_lots', COUNT(*) FROM production_lots
UNION ALL SELECT 'factory_shipments', COUNT(*) FROM factory_shipments
UNION ALL SELECT 'branch_inventory', COUNT(*) FROM branch_inventory
UNION ALL SELECT 'branch_orders', COUNT(*) FROM branch_orders
UNION ALL SELECT 'coffee_roasting_logs', COUNT(*) FROM coffee_roasting_logs
UNION ALL SELECT 'haccp_check_records', COUNT(*) FROM haccp_check_records;
```

**Raporla:**
- Fabrika tab sayısı ve listesi
- Her zincir adımı bağlı mı? (hammadde → üretim → LOT → kalite → stok → sevkiyat → şube)
- Kiosk 3 buton çalışıyor mu?
- Gerçek veri var mı yoksa hepsi 0 mı?

---

### BÖLÜM H: PDKS + Maaş Modülü

```bash
# 1. PDKS endpoint'leri
grep -rn "router\.\(get\|post\|put\|patch\)" server/routes/pdks.ts 2>/dev/null | head -15

# 2. Payroll endpoint'leri
grep -rn "router\.\(get\|post\|put\|patch\)" server/routes/payroll.ts 2>/dev/null | head -15

# 3. PDKS engine
wc -l server/lib/pdks-engine.ts 2>/dev/null
wc -l server/lib/payroll-engine.ts 2>/dev/null
```

```sql
-- 4. PDKS veri durumu
SELECT 'pdks_records' as tablo, COUNT(*) FROM pdks_records
UNION ALL SELECT 'position_salaries', COUNT(*) FROM position_salaries
UNION ALL SELECT 'monthly_payroll', COUNT(*) FROM monthly_payroll
UNION ALL SELECT 'scheduled_offs', COUNT(*) FROM scheduled_offs;
```

**Raporla:**
- PDKS kayıt/sorgulama çalışıyor mu?
- Maaş hesaplama motoru çalışıyor mu?
- 8 iş kuralı (günlük ücret ÷30, eksik+1 ceza, prim kesinti, FM ×1.5) implement edilmiş mi?

---

### BÖLÜM I: Push Notification & Güvenlik

```bash
# 1. Push subscription durumu
grep -rn "push.*subscribe\|vapid\|VAPID" server/ --include="*.ts" | head -10

# 2. CSP header
grep -rn "Content-Security-Policy\|helmet\|csp" server/ --include="*.ts" | head -10

# 3. Rate limiting
grep -rn "rateLimit\|rateLimiter\|rate.*limit" server/ --include="*.ts" | head -10

# 4. Audit logging
grep -rn "audit_log\|auditLog\|logAudit" server/ --include="*.ts" | wc -l
```

```sql
-- 5. Push subscription sayısı
SELECT COUNT(*) FROM push_subscriptions;

-- 6. Audit log sayısı
SELECT COUNT(*) FROM audit_logs;
```

**Raporla:**
- Push notification altyapısı aktif mi?
- Güvenlik header'ları mevcut mu?
- Audit logging çalışıyor mu?

---

### BÖLÜM J: Genel Sayfa & Route Envanteri

```bash
# 1. Toplam sayfa dosyası
ls client/src/pages/*.tsx 2>/dev/null | wc -l

# 2. Toplam route
grep -c "path=" client/src/App.tsx 2>/dev/null

# 3. Lazy load sayısı
grep -c "lazy(" client/src/App.tsx 2>/dev/null

# 4. Toplam API endpoint
grep -rn "router\.\(get\|post\|put\|patch\|delete\)" server/routes/ --include="*.ts" | wc -l

# 5. Toplam tablo
SELECT COUNT(*) FROM information_schema.tables WHERE table_schema = 'public';

# 6. Toplam background job
grep -rn "setInterval\|cron\|schedule" server/index.ts server/ --include="*.ts" | grep -i "start\|init\|begin\|register" | wc -l
```

**Raporla:** Tam sistem istatistikleri — sayfa, route, endpoint, tablo, job sayıları

---

### BÖLÜM K: Her Rolün "Ne Eksik?" Analizi

Her 26 rol için şu soruları cevapla:
1. Bu rol sisteme giriş yapınca ne görüyor?
2. Bu rolün günlük işi ne? Sistem buna uygun mu?
3. Bu rolün sidebar'da görmemesi gereken ama gördüğü şeyler var mı?
4. Bu rolün göremediği ama görmesi gereken şeyler var mı?
5. Mr. Dobody bu role proaktif öneri sunuyor mu?
6. Bu rol mobil/tablet/masaüstü hangisinden giriyor?

**Tüm 26 rolü listele ve her biri için bu 6 soruyu cevapla.**

---

### FORMAT:

Her bölümü (A-K) ayrı raporla. Toplam rapor en az 200 satır olsun — detay önemli.

### DO NOT:
- Kod değişikliği yapma
- Yeni dosya oluşturma
- DB'de INSERT/UPDATE/DELETE çalıştırma

---

Sonucu getir — Sprint 20 UX tasarım dokümanını bu audit'e + dış araştırmaya göre yazacağım.