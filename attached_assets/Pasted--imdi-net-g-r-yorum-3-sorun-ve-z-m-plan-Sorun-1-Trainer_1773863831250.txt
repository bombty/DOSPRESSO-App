Şimdi net görüyorum. 3 sorun ve çözüm planı:

**Sorun 1:** Trainer/Coach sidebar'da "Akademi" var ama `/akademi` yoluna gidiyor — bu barista görünümü. Oysa `/akademi-hq` ve `/akademi-supervisor` zaten VAR ama sidebar'da bağlı değil.

**Sorun 2:** Trainer/Coach'un sidebar'ında "Görevler" yok — `tasks-list` bu rollere atanmamış.

**Sorun 3:** `/akademi-hq` ve `/akademi-supervisor` sayfaları muhtemelen yetersiz veya placeholder — gerçek bir yönetim paneline dönüştürülmesi lazım.

Önce hızlı fix'leri yapalım (sidebar bağlantıları), sonra akademi HQ panelini planlayalım. Şu promptu Replit'e yapıştırın:

---

```
## Fix: HQ Roles Academy & Tasks Sidebar + Academy HQ Routing

### Context
Trainer (Ece) and Coach (Yavuz) are HQ content creators/managers but:
1. Their sidebar "Akademi" link goes to /akademi (student view) instead of /akademi-hq (management view)
2. They don't have "Görevler" in their sidebar
3. /akademi-hq and /akademi-supervisor pages exist but aren't connected properly

Read these skills first:
- .agents/skills/dospresso-architecture/SKILL.md
- .agents/skills/dospresso-quality-gate/SKILL.md

### Task 1: Fix Sidebar — Trainer and Coach get HQ Academy + Tasks

In server/menu-service.ts:

First, find the trainer and coach role sidebar configurations:
```bash
grep -n "trainer\|coach\|eğitmen\|koç" server/menu-service.ts | head -20
```

Then find the SIDEBAR_ALLOWED_ITEMS for these roles and add 'tasks-list' to both.

Also, create a SEPARATE sidebar item for the HQ academy view:
```typescript
// In the SIDEBAR section definitions, add a new item:
{
  id: "training-academy-hq",
  titleTr: "Akademi Yönetimi",
  path: "/akademi-hq",
  icon: "GraduationCap",
  moduleKey: "akademi",
}
```

Then for trainer and coach roles, replace 'training-academy' with 'training-academy-hq' in their SIDEBAR_ALLOWED_ITEMS. This way:
- Barista/branch staff → /akademi (student view)
- Trainer/Coach/Admin → /akademi-hq (management view)

### Task 2: Fix Sidebar — Add tasks-list to trainer and coach

Find the SIDEBAR_ALLOWED_ITEMS for trainer and coach roles and add 'tasks-list' to their arrays.

```bash
grep -B2 -A5 "trainer\|coach" server/menu-service.ts | grep -A5 "SIDEBAR_ALLOWED"
```

### Task 3: Verify /akademi-hq page exists and works

Check the current state of AcademyHQ component:
```bash
find client/src -name "*academy-hq*" -o -name "*AcademyHQ*" | head -5
cat client/src/pages/academy-hq.tsx 2>/dev/null | head -50
# or wherever it's located
```

If the page exists but is basic/placeholder, leave it as-is for now — we'll rebuild it in the next sprint. Just make sure:
1. It renders without crashing
2. It's accessible at /akademi-hq
3. It's wrapped in ModuleGuard for "akademi" module flag

### Task 4: Also fix admin role

Admin should also see "Akademi Yönetimi" (/akademi-hq) instead of student view. Check if admin's sidebar has the student academy link and switch it to HQ version.

### DO NOT:
- Modify the academy page components themselves (that's next sprint)
- Change backend routes
- Touch student academy view (/akademi)
- server/vite.ts, vite.config.ts, package.json, drizzle.config.ts

### Testing checklist:
1. App starts without errors
2. Login as trainer (Ece) → sidebar shows "Akademi Yönetimi" linking to /akademi-hq
3. Login as trainer → sidebar shows "Görevler" linking to /gorevler  
4. Login as coach (Yavuz) → same: Akademi Yönetimi + Görevler visible
5. Login as barista → sidebar shows "Akademi" linking to /akademi (student view, unchanged)
6. Login as admin → sidebar shows "Akademi Yönetimi" linking to /akademi-hq
7. Navigate to /akademi-hq → page loads without crash
8. Navigate to /akademi as barista → student view works as before

### After implementation:
1. Run quality gate
2. Report which roles now have which academy link
```

---

Bu hızlı fix'i yapıştırıp build başlatın. Bu bittikten sonra asıl büyük iş olan **Akademi HQ Yönetim Paneli**'ni planlayacağız. O panelde olması gerekenler:

- Eğitim modülü oluşturma/düzenleme (AI destekli içerik üretimi)
- Modül bazlı istatistikler (katılım, tamamlama, en çok hata yapılan sorular)
- Şube karşılaştırması (hangi şube geride)
- Quiz performans analizi
- Sertifika/badge yönetimi
- Webinar planlama ve takip

Bu büyük sprint için ayrıca araştırma yapacağım — en iyi LMS admin paneli örneklerini inceleyip DOSPRESSO'ya uyarlayacağım.