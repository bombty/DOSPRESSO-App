Mevcut kodu inceledim. Temel altyapı sağlam — tablo, servis, route'lar, hook, seed, menu entegrasyonu hepsi çalışıyor. Sprint 1B'de mevcut yapıyı kırmadan **3 kolon ekleyip, seed'i genişletip, servis mantığını güncelleyeceğiz.**

İşte Replit promptu:

---

**Replit'e yapıştırılacak prompt:**

```
## Module Feature Flag System — Sprint 1B: Enhanced Architecture

### Context
Sprint 1 created the basic module_flags system (16 global flags, simple on/off). Now we need to add 3 critical features:
1. Flag behavior types (fully_hidden vs ui_hidden_data_continues vs always_on)
2. Hierarchical sub-modules (factory sub-modules like fabrika.sevkiyat)
3. Parent-child relationships (if parent is off, children are off too)

IMPORTANT: Do NOT delete or recreate the existing module_flags table. We are ADDING columns and UPDATING existing rows. The table already has 16 rows in production.

Read these skills first:
- .agents/skills/dospresso-architecture/SKILL.md
- .agents/skills/dospresso-quality-gate/SKILL.md

### Task 1: Add 3 new columns to module_flags table

In shared/schema.ts, add these columns to the existing moduleFlags table definition:

```typescript
flagLevel: varchar("flag_level", { length: 20 }).notNull().default("module"),
// Values: "module" | "submodule" | "widget" | "function"

flagBehavior: varchar("flag_behavior", { length: 30 }).notNull().default("fully_hidden"),
// Values: "fully_hidden" | "ui_hidden_data_continues" | "always_on"

parentKey: varchar("parent_key", { length: 100 }),
// NULL for top-level modules, parent moduleKey for sub-modules
```

Then run the ALTER TABLE migration via SQL (NOT drizzle push):
```sql
ALTER TABLE module_flags ADD COLUMN IF NOT EXISTS flag_level VARCHAR(20) NOT NULL DEFAULT 'module';
ALTER TABLE module_flags ADD COLUMN IF NOT EXISTS flag_behavior VARCHAR(30) NOT NULL DEFAULT 'fully_hidden';
ALTER TABLE module_flags ADD COLUMN IF NOT EXISTS parent_key VARCHAR(100);
```

### Task 2: Update existing 16 rows + add new sub-modules

Update server/seed-module-flags.ts. Replace the flat MODULE_KEYS array with a structured definition:

```typescript
const MODULE_DEFINITIONS = [
  // === ALWAYS ON (cannot be disabled) ===
  { key: "admin", level: "module", behavior: "always_on", parent: null },
  { key: "dashboard", level: "module", behavior: "always_on", parent: null },
  { key: "bordro", level: "module", behavior: "always_on", parent: null },
  { key: "dobody", level: "module", behavior: "always_on", parent: null },
  { key: "fabrika", level: "module", behavior: "always_on", parent: null },
  { key: "satinalma", level: "module", behavior: "always_on", parent: null },

  // === UI HIDDEN BUT DATA CONTINUES ===
  { key: "pdks", level: "module", behavior: "ui_hidden_data_continues", parent: null },
  { key: "vardiya", level: "module", behavior: "ui_hidden_data_continues", parent: null },

  // === FULLY TOGGLEABLE (branch modules) ===
  { key: "checklist", level: "module", behavior: "fully_hidden", parent: null },
  { key: "gorevler", level: "module", behavior: "fully_hidden", parent: null },
  { key: "akademi", level: "module", behavior: "fully_hidden", parent: null },
  { key: "crm", level: "module", behavior: "fully_hidden", parent: null },
  { key: "stok", level: "module", behavior: "fully_hidden", parent: null },
  { key: "ekipman", level: "module", behavior: "fully_hidden", parent: null },
  { key: "denetim", level: "module", behavior: "fully_hidden", parent: null },
  { key: "iletisim_merkezi", level: "module", behavior: "fully_hidden", parent: null },
  { key: "raporlar", level: "module", behavior: "fully_hidden", parent: null },
  { key: "finans", level: "module", behavior: "fully_hidden", parent: null },
  { key: "delegasyon", level: "module", behavior: "fully_hidden", parent: null },
  { key: "franchise", level: "module", behavior: "fully_hidden", parent: null },

  // === FACTORY SUB-MODULES (parent = fabrika, always_on parent) ===
  { key: "fabrika.sevkiyat", level: "submodule", behavior: "fully_hidden", parent: "fabrika" },
  { key: "fabrika.sayim", level: "submodule", behavior: "fully_hidden", parent: "fabrika" },
  { key: "fabrika.hammadde", level: "submodule", behavior: "fully_hidden", parent: "fabrika" },
  { key: "fabrika.siparis", level: "submodule", behavior: "fully_hidden", parent: "fabrika" },
  { key: "fabrika.vardiya", level: "submodule", behavior: "ui_hidden_data_continues", parent: "fabrika" },
  { key: "fabrika.kalite", level: "submodule", behavior: "fully_hidden", parent: "fabrika" },
  { key: "fabrika.kavurma", level: "submodule", behavior: "fully_hidden", parent: "fabrika" },
  { key: "fabrika.stok", level: "submodule", behavior: "fully_hidden", parent: "fabrika" },
];
```

The seed function should:
- For existing rows (16 module keys): UPDATE flag_level, flag_behavior, parent_key (use ON CONFLICT ... DO UPDATE)
- For new rows (8 factory sub-modules): INSERT with ON CONFLICT DO NOTHING
- Log count of updated + inserted rows

### Task 3: Update isModuleEnabled in module-flag-service.ts

The function must now handle 3 behaviors:

```typescript
export async function isModuleEnabled(
  moduleKey: string, 
  branchId?: number | null,
  context?: "ui" | "api" | "data"  // NEW parameter
): Promise<boolean> {
  // 1. Check parent first (if sub-module)
  //    Get parentKey from the flag record
  //    If parent is disabled → child is also disabled (regardless of child's own flag)
  //    Exception: parent with behavior "always_on" is never disabled
  
  // 2. Get the flag record for this moduleKey
  //    Branch override > global fallback (existing logic — keep it)
  
  // 3. Apply behavior rules:
  //    - "always_on" → always return true, ignore isEnabled field
  //    - "fully_hidden" → return isEnabled value (existing behavior)
  //    - "ui_hidden_data_continues":
  //        if context === "data" → always return true (data collection continues)
  //        if context === "ui" or "api" → return isEnabled value
  //    - Default (no context) → return isEnabled value (backward compatible)
}
```

Also update `requireModuleEnabled` middleware to pass context="api":
```typescript
const enabled = await isModuleEnabled(moduleKey, branchId, "api");
```

### Task 4: Update menu-service.ts module flag filtering

In the buildMenuForUser function, pass context="ui" when checking flags:
```typescript
moduleFlagChecks.set(flagKey, await isModuleEnabled(flagKey, branchId, "ui"));
```

Also add factory sub-module paths to PATH_TO_MODULE_KEY_MAP in module-flag-service.ts:
```typescript
// Factory sub-modules
"/fabrika/sevkiyat": "fabrika.sevkiyat",
"/fabrika/sayim": "fabrika.sayim",
"/fabrika/hammadde": "fabrika.hammadde",
"/fabrika/siparis": "fabrika.siparis",
"/fabrika/vardiya-planlama": "fabrika.vardiya",
"/fabrika/kalite": "fabrika.kalite",
"/fabrika/kavurma": "fabrika.kavurma",
"/fabrika/stok": "fabrika.stok",
```

IMPORTANT: Check the actual factory route paths in the codebase before mapping. Use `grep -rn "path:" client/src/pages/fabrika/ --include="*.tsx" | head -20` and check App.tsx for lazy route paths.

### Task 5: Update GET /api/module-flags/branch/:branchId

The response should now include flagLevel, flagBehavior, parentKey for each flag:
```typescript
return {
  moduleKey: gf.moduleKey,
  flagLevel: gf.flagLevel,
  flagBehavior: gf.flagBehavior,
  parentKey: gf.parentKey,
  globalEnabled: gf.isEnabled,
  branchOverride: branchOverride ? branchOverride.isEnabled : null,
  effectiveEnabled: branchOverride ? branchOverride.isEnabled : gf.isEnabled,
  globalFlagId: gf.id,
  branchFlagId: branchOverride?.id ?? null,
};
```

### Task 6: Update frontend hook

Add context parameter to useModuleEnabled:
```typescript
export function useModuleEnabled(
  moduleKey: string, 
  context: "ui" | "api" = "ui"
): { isEnabled: boolean; isLoading: boolean; isError: boolean } {
  // Add context to query: /api/module-flags/check?moduleKey=X&context=ui
}
```

Update the /api/module-flags/check endpoint to accept and pass context parameter.

### Task 7: Add isModuleFlagAlwaysOn helper

In module-flag-service.ts, add a helper that returns the behavior type:
```typescript
export async function getModuleFlagBehavior(moduleKey: string): Promise<string> {
  // Returns "fully_hidden" | "ui_hidden_data_continues" | "always_on"
  // Used by admin UI to show lock icon on always_on modules
}
```

### DO NOT TOUCH these files:
- server/vite.ts
- vite.config.ts  
- package.json
- drizzle.config.ts
- Any kiosk-related files
- Any existing page components

### Testing checklist (run after implementation):
1. Check app starts without errors
2. Verify seed ran: SELECT module_key, flag_level, flag_behavior, parent_key FROM module_flags ORDER BY flag_level, module_key;
3. Verify 28 rows total (20 modules + 8 factory sub-modules)
4. Test admin API: GET /api/module-flags should return all 28 with new columns
5. Test behavior: PATCH a "always_on" module to isEnabled=false — isModuleEnabled should still return true
6. Test parent chain: Disable "checklist" globally, verify composite score calculation still works (it should — score recalculates without checklist)

### After implementation:
1. Run quality gate (read .agents/skills/dospresso-quality-gate/SKILL.md)
2. Update replit.md — add Module Feature Flags section
3. Update .agents/skills/dospresso-architecture/SKILL.md — add Module Flag System with:
   - Table structure (module_flags with flagLevel, flagBehavior, parentKey)
   - 3 behavior types explained
   - Factory sub-modules list
   - PATH_TO_MODULE_KEY_MAP reference
   - Graceful degradation rule: scores recalculate without disabled modules
```

---

Bu promptu Replit'e yapıştırın. Bitince sonucu paylaşın — kontrol ederiz.

Önemli noktalar:
- Mevcut 16 satır korunuyor, sadece 3 kolon ekleniyor
- 8 fabrika alt-modülü yeni ekleniyor (toplam 28 satır)
- `isModuleEnabled` geriye uyumlu — eski çağrılar context olmadan çalışmaya devam eder
- Skill dosyaları da güncelleniyor (son adım)