Replit yeni commit push etmiş. Pull + rebase yapıyorum:​​​​​​​​​​​​​​​​

Push başarılı: 64c2c950
İşte Replit test talimatı:

TASK: Sidebar Rol Fix — Pull + Build + Test
Step 1: Pull

git pull origin main


Step 2: Build

npm run build


Must complete with zero TypeScript errors.
Step 3: Server Start

npm run dev


Step 4: Verify — Login as branch role, check module sidebar

# Login as supervisor (branch role)
COOKIE_SUP=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"supervisor1","password":"TestPass123!"}' | grep connect.sid | awk '{print $NF}')

# Login as admin (HQ role)  
COOKIE_ADM=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"adminhq","password":"TestPass123!"}' | grep connect.sid | awk '{print $NF}')


Frontend visual test (open browser):
	1.	Login as barista/supervisor → Go to /gorevler → Module sidebar should NOT show “Duyurular”
	2.	Login as admin → Go to /gorevler → Module sidebar SHOULD show “Duyurular”
	3.	Login as barista/supervisor → Go to any /subeler page → Module sidebar should NOT show “Canlı Takip”
	4.	Login as admin → Go to any /subeler page → Module sidebar SHOULD show “Canlı Takip”
DO NOT BREAK: Login, dashboard, all existing sidebar items, factory kiosk.
Report:

PULL: ✅/❌
BUILD: ✅/❌
SIDEBAR TEST: ✅/❌ (branch roles can't see restricted items / HQ roles can)


Bu oturumun özeti:



|#|İş                        |Durum        |Commit                  |
|-|--------------------------|:-----------:|------------------------|
|1|Motor birleştirme         |✅            |`d3ff92bd`              |
|2|Sidebar rol çakışmaları   |✅            |`64c2c950`              |
|3|sql.raw taraması          |✅ Rapor hazır|23 kullanım, ayrı sprint|
|4|Skill dosyaları güncelleme|✅            |`64c2c950`              |
|5|product_recipes migrasyon |⏳            |Replit DB işi           |
|6|Sidebar active highlight  |⏳            |?tab= query param       |