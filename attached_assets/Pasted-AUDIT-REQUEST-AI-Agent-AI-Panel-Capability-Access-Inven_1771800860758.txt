AUDIT REQUEST — AI Agent / AI Panel Capability & Access Inventory (NO new features)

Context:
We have an AI Panel + ai_agent_logs + related endpoints. I want a precise, code-backed inventory:
- what the “AI agent” can do today,
- what data it can access,
- what security boundaries exist (RBAC + tenant isolation),
- where risks exist (PII, retention, leakage),
- and what “next safe target” should be.

Rules:
- Do NOT build new features.
- Do NOT add new UI screens.
- Do NOT add new DB tables (schema changes only if absolutely required to fix a critical security issue; otherwise propose).
- Output must reference actual code files/paths and endpoint names. No theory.

Deliverables (must provide all):

1) Where is the AI Panel in the UI?
- Exact route(s) and entry point(s) (which page imports it, tab name, URL path).
- File paths (e.g., client/src/pages/..., akademi-mega.tsx changes).
- How to reach it with each role (employee/supervisor/coach).

2) Endpoint Map (AI-related)
For each endpoint, list:
- method + path
- handler file path + function name (or route registration location)
- required role/permission (reference shared/permissions.ts and any guards)
- tenant/branch filtering logic (where applied in code)
Endpoints to include at minimum:
- GET /api/academy/ai-panel
- GET /api/academy/ai-logs
- POST /api/academy/ai-logs
- any admin/cleanup/related endpoints if present

3) Data Access Inventory (AI Panel + Logs)
- Which tables are queried (including ai_agent_logs).
- For each query: which columns are read/written.
- Identify whether any fields could contain PII (email, phone, TCKN, IBAN, address, names, free-text).

4) What “AI agent” actually does today
Be explicit:
- Is it only reusing deterministic NBA data?
- Any LLM call / external API call?
- Any write actions besides logging?
- Trigger sources: what events call POST /api/academy/ai-logs and when?
Provide code references.

5) RBAC & Tenant Isolation Proof Checklist
- Provide 5 concrete “proof points” in code:
  a) deny-by-default behavior,
  b) coach-only restriction to logs,
  c) supervisor branch filtering,
  d) employee isolation,
  e) protection against cross-branch queries.
If any are missing, list them as gaps.

6) Risk Gaps & Fix Plan (NO build, just plan)
- PII redaction gap: does inputSummary/outputSummary get sanitized before insert? If not, mark as P0.
- Retention gap: any cleanup job/policy? If not, mark as P0.
- Negative tests gap: any automated tests for 403? If not, mark as P1.
Provide a minimal follow-up sprint plan (max 4 items) to close P0/P1 gaps.

7) “North Star” alignment check
- Confirm the AI Panel follows docs/04-ai-surface-rules.md patterns:
  rationale + data used + confidence + human approval for critical actions.
- If not fully aligned, list what’s missing.

Output format:
- Start with a 1-page Executive Summary (bullets).
- Then sections 1–7 with file paths and short code pointers.
- End with “What I should do next” as 5 manual checks for the owner (me).