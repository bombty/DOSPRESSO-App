Replit'e yapıştır:

---

## TASK: Sprint 3 Hazırlık — Misafir Geri Bildirim Sistemi Güncel Audit

Kod değişikliği YAPMA. Sadece analiz et ve rapor ver.

---

### BÖLÜM A: Public Müşteri Formu Detayları

```bash
# 1. Public form route'u ve sayfası
grep -rn "public\|guest\|feedback.*form\|misafir.*form\|customer.*form" client/src/ --include="*.tsx" -l
grep -rn "public\|guest\|feedback" server/routes/ --include="*.ts" | head -20

# 2. QR kod sistemi
grep -rn "qr\|QR\|qrcode\|qr.*code\|qr.*generate" server/ client/src/ --include="*.ts" --include="*.tsx" | head -20

# 3. Public form URL yapısı
grep -rn "feedback.*form\|guest.*form\|review.*form\|rate\|rating.*page" server/routes/ --include="*.ts" | head -15

# 4. Form alanları
grep -rn "rating\|puan\|score\|category\|temizlik\|hizmet\|kalite\|ürün\|personel\|staff" client/src/ --include="*.tsx" | grep -i "feedback\|guest\|misafir\|customer" | head -20

# 5. Lokasyon doğrulama
grep -rn "geolocation\|navigator.geo\|latitude\|longitude\|location.*verify\|konum" client/src/ --include="*.tsx" | head -10

# 6. Çoklu dil desteği
grep -rn "language\|dil\|locale\|i18n\|translation\|lang.*select" client/src/ --include="*.tsx" | grep -i "feedback\|guest\|misafir" | head -10
```

Raporla:
- Public form hangi URL'de?
- Şubeye özel mi? URL'de şube kodu var mı?
- Hangi alanlar mevcut? (puan, kategori, yorum, fotoğraf, iletişim)
- Lokasyon doğrulama var mı?
- 7 dil desteği nasıl implemente edilmiş?
- Form anonim mi yoksa bilgi zorunlu mu?

---

### BÖLÜM B: customer_feedback Tablo Detayları

```sql
-- 1. Tablo yapısı
SELECT column_name, data_type, column_default, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'customer_feedback' 
ORDER BY ordinal_position;

-- 2. Mevcut veriler
SELECT * FROM customer_feedback ORDER BY created_at DESC LIMIT 10;

-- 3. Kategoriler
SELECT DISTINCT category FROM customer_feedback;
SELECT DISTINCT feedback_type FROM customer_feedback;
SELECT DISTINCT source FROM customer_feedback;

-- 4. İlişkili tablolar
SELECT column_name, data_type FROM information_schema.columns 
WHERE table_name = 'feedback_responses' ORDER BY ordinal_position;
SELECT COUNT(*) FROM feedback_responses;

SELECT column_name, data_type FROM information_schema.columns 
WHERE table_name = 'feedback_form_settings' ORDER BY ordinal_position;
SELECT COUNT(*) FROM feedback_form_settings;
SELECT * FROM feedback_form_settings LIMIT 5;

SELECT column_name, data_type FROM information_schema.columns 
WHERE table_name = 'staff_qr_ratings' ORDER BY ordinal_position;
SELECT COUNT(*) FROM staff_qr_ratings;
```

Raporla:
- customer_feedback tablosu hangi kolonlara sahip?
- Şube bağlantısı var mı? (branch_id)
- Personel bağlantısı var mı? (staff_id, rated_staff)
- Puan alanları: genel mi kategori bazlı mı?
- feedback_form_settings ne işe yarıyor?
- staff_qr_ratings: personel bazlı QR puanlama var mı?
- feedback_responses: müşteriye geri dönüş kayıtları mı?

---

### BÖLÜM C: guest_complaints Detayları

```sql
-- 1. Tablo yapısı
SELECT column_name, data_type, column_default, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'guest_complaints' 
ORDER BY ordinal_position;

-- 2. Mevcut veriler
SELECT * FROM guest_complaints LIMIT 5;

-- 3. Status değerleri
SELECT DISTINCT status FROM guest_complaints;
```

```bash
# 4. Şikayet bildirimleri
grep -rn "complaint.*notif\|şikayet.*bildir\|new.*complaint\|complaint.*alert" server/ --include="*.ts" | head -10

# 5. SLA kontrolü
grep -rn "sla\|SLA\|response.*time\|yanıt.*süre\|deadline.*complaint" server/ --include="*.ts" | head -10
```

Raporla:
- Şikayet lifecycle statüleri neler?
- Bildirim gidiyor mu? Kime?
- SLA takibi var mı?
- Müşteriye otomatik yanıt gidiyor mu?

---

### BÖLÜM D: Şube Sağlık Skoru Bağlantısı

```bash
# 1. Branch health score hesaplama
grep -rn "health.*score\|saglik.*skor\|branch.*score\|sube.*skor" server/ --include="*.ts" | head -15

# 2. Feedback'in skora etkisi
grep -rn "feedback.*score\|customer.*score\|satisfaction.*score\|memnuniyet.*skor" server/ --include="*.ts" | head -10

# 3. Şube sağlık dashboard'u
grep -rn "branch.*health\|sube.*saglik\|health.*dashboard" client/src/ --include="*.tsx" -l
```

```sql
-- 4. Branch health score tablosu
SELECT table_name FROM information_schema.tables 
WHERE table_name ILIKE '%branch_health%' OR table_name ILIKE '%sube_saglik%' 
OR table_name ILIKE '%health_score%';
```

Raporla:
- Şube sağlık skoru nasıl hesaplanıyor?
- Müşteri memnuniyeti dahil mi?
- Dashboard'da gösteriliyor mu?

---

### BÖLÜM E: Bildirim & Otomasyon Altyapısı

```bash
# 1. Feedback bildirimleri
grep -rn "feedback.*notification\|complaint.*notification\|misafir.*bildirim\|şikayet.*bildirim" server/ --include="*.ts" | head -10

# 2. Background job'larda feedback
grep -rn "feedback\|complaint\|customer\|misafir\|guest" server/ --include="*.ts" | grep -i "job\|cron\|schedule\|interval\|background" | head -10

# 3. Email/SMS altyapısı
grep -rn "sendEmail\|sendSms\|smtp\|twilio\|nodemailer\|email.*send\|sms.*send" server/ --include="*.ts" | head -10

# 4. Bildirim oluşturma fonksiyonu
grep -rn "createNotification\|sendNotification\|notification.*create" server/ --include="*.ts" | head -10
```

Raporla:
- Yeni feedback gelince bildirim gidiyor mu?
- Email/SMS altyapısı var mı?
- Background job'da feedback otomasyonu var mı?

---

### BÖLÜM F: Anti-Suistimal & Vardiya Bağlantısı

```bash
# 1. Rate limiting
grep -rn "rate.*limit\|throttle\|spam.*protect\|abuse.*prevent" server/ --include="*.ts" | grep -i "feedback\|guest\|customer" | head -10

# 2. Device fingerprint
grep -rn "fingerprint\|device.*id\|browser.*id\|unique.*device" server/ client/src/ --include="*.ts" --include="*.tsx" | head -10

# 3. Vardiya sistemi (feedback zamanı ile çaprazlama için)
grep -rn "shift.*active\|active.*shift\|vardiya.*aktif\|getCurrentShift\|onDuty" server/ --include="*.ts" | head -10
```

```sql
-- 4. Vardiya tablosu yapısı (o an çalışan personeli bulmak için)
SELECT column_name, data_type FROM information_schema.columns 
WHERE table_name = 'shifts' OR table_name = 'shift_assignments'
ORDER BY table_name, ordinal_position;

-- 5. Aktif vardiya örneği
SELECT * FROM shifts WHERE start_time <= NOW() AND end_time >= NOW() LIMIT 5;
```

Raporla:
- Aynı cihazdan tekrar değerlendirme engeli var mı?
- Device fingerprint kullanılıyor mu?
- Vardiya verisiyle feedback zamanı çaprazlanabilir mi?
- O an şubede çalışan personel listesi alınabiliyor mu?

---

### FORMAT:

Her bölümü (A-F) ayrı raporla:
1. **Mevcut durum** — ne var, ne çalışıyor
2. **Eksikler** — ne yok
3. **Veri durumu** — kayıt sayıları
4. **Sprint 3 iş tahmini** — küçük/orta/büyük

### DO NOT:
- Kod değişikliği yapma
- Yeni dosya oluşturma
- DB'de INSERT/UPDATE/DELETE çalıştırma

---

Bunu Replit'e yapıştır, sonucu getir.