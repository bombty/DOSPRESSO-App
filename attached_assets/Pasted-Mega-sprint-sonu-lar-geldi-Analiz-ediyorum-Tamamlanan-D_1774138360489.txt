Mega sprint sonuçları geldi. Analiz ediyorum:

**Tamamlanan:**

| İş | Durum | Detay |
|-----|-------|-------|
| QC auto-lot oluşturma | ✅ | 3 retry mekanizması ile |
| QC stats endpoint | ✅ | Günlük/haftalık istatistik |
| QC Mr. Dobody skill | ✅ | qc-tracker.ts (122 satır) |
| Payroll hesaplama servisi | ✅ | 196 satır, SGK/vergi |
| PDF generator | ✅ | pdf-lib ile 187 satır |
| PDF export endpoint | ✅ | Bordro + PDKS |
| İK Dashboard KPI | ✅ | 4 kart |
| İK Document CRUD | ✅ | Upload/verify/delete |
| İK Disciplinary CRUD | ✅ | Create/respond/status |
| Branch scope fix | ✅ | İK yetki kontrolü |
| AuthUser type | ✅ | Merkezi tip tanımı |
| Skills güncelleme | ✅ | Architecture + quality gate |

**Yapılmadı (doğru karar):**

| İş | Neden | Plan |
|-----|-------|------|
| misc.ts bölme | 13K satır, çok riskli | Ayrı sprint |
| schema.ts bölme | 15K satır, 843 export | Ayrı sprint |
| Kiosk QC mode | Kiosk'a dokunmak riskli | Ayrı sprint |
| Payroll servis entegrasyonu | Dead code — route'lara bağlanmadı | Ayrı sprint |

**2 KRİTİK SORUN:**

1. **Kullanıcı tarayıcıdan giriş yapamıyor** — API çalışıyor ama browser'dan giriş olmuyor. Session cookie veya CORS sorunu olabilir.
2. **payroll-calculation-service.ts entegre değil** — Servis var ama hiçbir endpoint çağırmıyor (dead code).

Giriş sorunu acil — bu olmadan hiçbir şey test edilemez. Replit'e yazın:

---

**"2 acil sorun var:**

**1. ACIL: Kullanıcı tarayıcıdan giriş yapamıyor. API çalışıyor ama browser'dan login olmuyor. Araştır ve düzelt:**
```bash
# Cookie/session kontrol
curl -v -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"admin","password":"Dospresso2026!"}' 2>&1 | grep -i "set-cookie\|session\|cors\|origin"

# CORS headers kontrol
grep -n "cors\|CORS\|origin\|cookie\|sameSite\|secure" server/index.ts server/localAuth.ts | head -20

# Session config
grep -n "session\|cookie\|maxAge\|secure\|sameSite\|httpOnly" server/index.ts | head -15
```
**Düzelt — bu olmadan hiçbir şey test edilemez.**

**2. payroll-calculation-service.ts'i mevcut payroll route'larına entegre et. Servis çağrılmıyor — dead code durumunda.**

**Bu 2 sorunu çöz, sonra rapor ver."**

---

Bu 2 fix bitince Dashboard sprint'lerine geçebiliriz. Yapıştırın.