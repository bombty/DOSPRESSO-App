FIX TURKISH CHARACTER ISSUES IN PURCHASING & ACCOUNTING PAGES — HIGH PRIORITY

The app has systematic Turkish character encoding issues. Menu items and page titles show broken characters like "Satinalma" instead of "Satınalma". Fix all instances below.

IMPORTANT CONTEXT:
- The app uses Turkish locale. Characters like ı, İ, ş, Ş, ö, Ö, ü, Ü, ç, Ç, ğ, Ğ must display correctly.
- These are likely hardcoded strings in route labels, navigation config, or page components.
- Search the entire codebase for wrong versions and fix them all at once.

TASK 1 — Fix "Satinalma" → "Satınalma" (Purchasing)
WHERE: Bottom navigation, sidebar, breadcrumbs, page headers — everywhere this word appears
FIND ALL: "Satinalma", "satinalma", "SATINALMA"
REPLACE WITH: "Satınalma", "satınalma", "SATINALMA" (preserving original case)
ALSO CHECK: Route labels, PATH_LABELS map, menu config arrays, page title components

TASK 2 — Fix "Satin Alma" → "Satın Alma" (with space variant)
FIND ALL: "Satin Alma", "Satin alma", "SATIN ALMA"
REPLACE WITH: "Satın Alma", "Satın alma", "SATIN ALMA"

TASK 3 — Fix Accounting Page Turkish Characters
WHERE: Muhasebe (Accounting) section — menu items and page titles
FIND AND FIX these specific broken strings:
- "Odemeler" → "Ödemeler"
- "Odeme" → "Ödeme"
- "Fatura Olustur" → "Fatura Oluştur"
- "Olustur" → "Oluştur"
- "Gelir Gider" → check if displaying correctly (should have no issues but verify)
- "Butce" → "Bütçe"
- "Donem" → "Dönem"
- "Ozet" → "Özet"

TASK 4 — Systematic Codebase Scan
Run a grep/search across ALL files for these common Turkish character mistakes:
- "inalma" (should be "ınalma" in Satınalma)
- "Odem" (should be "Ödem" in Ödemeler)
- "Olust" (should be "Oluşt" in Oluştur)
- "Butc" (should be "Bütç" in Bütçe)
- "Donem" (should be "Dönem")
- "Urun" (should be "Ürün")
- "Uretim" (should be "Üretim")
- "Musteri" (should be "Müşteri")
- "Calis" (should be "Çalış" in Çalışan)
- "Degerlendirme" (should be "Değerlendirme")
Fix ALL instances found, not just the ones listed above.

DO NOT BREAK:
- URL paths/slugs (these may intentionally use ASCII — only fix display labels, not URL paths)
- Database column names (don't change these)
- API endpoint paths (don't change these)
- Breadcrumb PATH_LABELS map structure (fix values, not keys)
- Any existing working Turkish characters