Task #49 tamamlanmış görünüyor. Ama menü görünürlük sorununu henüz araştırmadık — bu ayrı bir konu. Menü audit promptunu yapıştırdınız mı?

Eğer yapıştırmadıysanız, şunu yapıştırın:

---

**"Task #49 tamamlandı. Şimdi farklı bir sorun: Menü görünürlük problemi.**

**Kullanıcı şunu bildiriyor:**
- Admin dashboard'da "Ayarlar" menüsü görünmüyor
- Coach rolünde İK bölümü yok
- Profil linki eksik veya bulunmuyor
- Genel olarak rol bazlı menü tutarsızlığı var

**Kapsamlı araştırma yap — hiçbir değişiklik yapma:**

```bash
echo "══════════════════════════════════════════"
echo "MENÜ GÖRÜNÜRLÜK FULL AUDIT"
echo "══════════════════════════════════════════"

echo "=== 1. MENÜ SİSTEMİ AKIŞI ==="
echo "--- Menu service ---"
wc -l server/menu-service.ts
head -80 server/menu-service.ts

echo "--- Menu endpoint ---"
grep -n "router.*menu\|/api/me/menu\|/api/menu" server/routes/*.ts | head -10

echo ""
echo "=== 2. SIDEBAR BILEŞENI ==="
find client/src -name "*sidebar*" -o -name "*Sidebar*" -o -name "*nav-rail*" | sort
for f in $(find client/src -name "*sidebar*" -o -name "*Sidebar*" | head -3); do
  echo "--- $(basename $f) ($(wc -l < $f) satır) ---"
  grep -n "menu\|item\|ayarlar\|settings\|profil\|ik\|İK\|coach" "$f" | head -10
done

echo ""
echo "=== 3. ROL BAZLI MENÜ TANIMLARI ==="
grep -n "SIDEBAR_ALLOWED\|MENU_ITEMS\|menuItems\|allowedItems\|visibleItems" server/menu-service.ts | head -20
echo "--- Ayarlar tanımlı mı? ---"
grep -in "ayarlar\|settings" server/menu-service.ts | head -10
echo "--- İK tanımlı mı? ---"
grep -in "ik-panel\|insan.kaynaklar\|hr\|İK" server/menu-service.ts | head -10
echo "--- Profil tanımlı mı? ---"
grep -in "profil\|profile" server/menu-service.ts | head -10

echo ""
echo "=== 4. HER ROL MENÜ TESTİ ==="
# Admin
echo "--- Admin menü öğeleri ---"
curl -s -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin"}' -c /tmp/cookie_admin 2>/dev/null > /dev/null
curl -s http://localhost:5000/api/me/menu -b /tmp/cookie_admin 2>/dev/null | python3 -c "import sys,json; data=json.load(sys.stdin); [print(f'  {i.get(\"label\",i.get(\"title\",\"?\"))} → {i.get(\"path\",i.get(\"href\",\"?\"))}') for i in data]" 2>/dev/null || echo "Menu parse failed"

# Coach
echo "--- Coach kullanıcı bul ---"
echo "SELECT username FROM users WHERE role='coach' AND is_active=true LIMIT 1;" | psql -t

# Supervisor  
echo "--- Supervisor kullanıcı bul ---"
echo "SELECT username FROM users WHERE role='supervisor' AND is_active=true LIMIT 1;" | psql -t

echo ""
echo "=== 5. MODULE FLAGS ==="
echo "SELECT module_key, flag_behavior FROM module_flags WHERE flag_behavior IN ('fully_hidden','ui_hidden_data_continues') AND deleted_at IS NULL ORDER BY module_key;" | psql

echo ""
echo "=== 6. ROLE-MODULE PERMISSIONS ==="
echo "--- Admin İK erişimi ---"
echo "SELECT module_key, can_view FROM role_module_permissions WHERE role='admin' AND module_key LIKE '%ik%' OR module_key LIKE '%hr%' OR module_key LIKE '%ayarlar%' OR module_key LIKE '%settings%';" | psql
echo "--- Coach İK erişimi ---"
echo "SELECT module_key, can_view FROM role_module_permissions WHERE role='coach' AND module_key LIKE '%ik%' OR module_key LIKE '%hr%';" | psql

echo ""
echo "=== 7. HEADER/PROFIL ==="
grep -rn "profil\|profile\|avatar\|user.*dropdown" client/src/components/app-header*.tsx client/src/components/layout/header*.tsx 2>/dev/null | head -15
find client/src -name "app-header*" -o -name "AppHeader*" | head -3

echo ""
echo "=== 8. AYARLAR SAYFASI ==="
find client/src/pages -name "*ayarlar*" -o -name "*settings*" -o -name "*admin-settings*" | sort
grep -n "ayarlar\|settings" client/src/App.tsx | head -10

echo ""
echo "=== 9. APP.TSX ROUTE SAYISI ==="
grep -c "Route\|path=" client/src/App.tsx
echo "--- Ayarlar route ---"
grep "ayarlar\|settings" client/src/App.tsx | head -5
echo "--- İK route ---"
grep "ik-panel\|ik/\|hr/" client/src/App.tsx | head -5
echo "--- Profil route ---"
grep "profil\|profile" client/src/App.tsx | head -5

echo ""
echo "=== 10. AKTIF ROLLER VE KULLANICI SAYILARI ==="
echo "SELECT role, count(*) FROM users WHERE is_active=true GROUP BY role ORDER BY count DESC;" | psql
```

**Raporla:**
1. Menü sistemi akışı (backend → frontend nasıl çalışıyor)
2. Her rol kaç menü öğesi görüyor — özellikle admin ve coach
3. "Ayarlar" neden görünmüyor — module_flag mı, permission mı, tanımsız mı?
4. "İK" coach'ta neden yok — aynı analiz
5. Profil linki nerede — header'da mı, sidebar'da mı, hiç yok mu?
6. Module flags hangi menüleri engelliyor
7. Sorunun KÖK NEDENİ ne
8. Kapsamlı çözüm önerisi

**Sadece araştır — değişiklik yapma.**"

---

Yapıştırın. Sonucu paylaşın — menü sorununu çözmeden lansman yapmamalıyız.