ÖNCELİK: Messaging regresyonunu düzelt. Soft delete / trash işleri ŞİMDİLİK duracak.

Problem (kesin): DB boş değil (messages ~12, threads ~11). UI’de mesaj/thread görünüyor ama sayfayı yenileyince veya çıkıp girince hem Admin hem Basri tarafında “Mesaj bulunamadı” oluyor. GET /api/messages (ve/veya threads list) boş dönüyor.

Kök neden hipotezi (çok güçlü, DB’de doğrulandı):
- Sorguda ROW_NUMBER() / window function ile rn üretiliyor.
- PostgreSQL ROW_NUMBER() tipi bigint; Drizzle bunu JS tarafına çoğu zaman string olarak map’liyor (örn "1").
- Kodda strict karşılaştırma var: if (m.rn === 1) veya benzeri → her zaman false.
- Bu yüzden lastMsgMap / firstSubjectMap boş kalıyor.
- Sonra if (!last) continue; veya benzeri bir filtre tüm thread’leri atlıyor → API boş array dönüyor → UI boş.

İSTEDİĞİM ÇÖZÜM:
1) Kodda rn karşılaştırmasını sağlam hale getir:
   - rn’yi number’a çevir (Number(m.rn)) ve == 1 / === 1 kontrolünü buna göre yap
   - veya SQL tarafında rn’yi INT’e cast et (ROW_NUMBER()::int) ve Drizzle typing’i doğru olsun
   - ama “bigint string” edge-case’i tekrar yaşanmayacak şekilde kalıcı fix olsun.
2) “Mesajı olmayan thread’ler” konusu:
   - Thread’lerde mesaj yoksa bile listede görünmesi isteniyor mu? ŞİMDİLİK en güvenlisi:
     - Mesajı olmayan thread’leri de listede göster (subject varsa subject; yoksa “(No messages yet)” gibi)
     - VEYA mevcut davranış “mesaj yoksa listede gösterme” ise, en azından mesajlı thread’ler kaybolmasın.
   - Burada ürün kararını değiştirme; sadece mevcut sistemin beklenen davranışını koru. (Bug: mesajlı thread’ler bile kayboluyor.)
3) DÜZELTİLDİĞİNİ KANITLA (mini test):
   - Admin -> mesaj gönder -> Messages listesinde gör
   - sayfayı hard refresh yap (Cmd+R / browser refresh)
   - tekrar Messages’e gir -> aynı thread ve mesaj duruyor
   - çıkış yap / tekrar giriş -> aynı şekilde duruyor
   - Basri tarafında da gelen mesaj görünüyor ve refresh sonrası kaybolmuyor
4) Debug çıktıları:
   - İlgili endpointte (GET /api/messages veya threads endpointi) rn tipini 1 kere logla:
     typeof rn ve rn değeri (sadece dev ortamda)
   - Sonra log’u kaldır veya debug flag ile koru (prod’a gürültü basmayalım).

KAPSAM DIŞI:
- Yeni UI tasarımı, yeni feature, soft delete, trash, pagination vs yok.
- Sadece messaging regression fix + minimal test.

NOTLAR:
- Sorun Safari cache değil; cache temizlendi, devam ediyor.
- DB’de veri var → problem kesinlikle “GET endpoint boş dönüyor” veya “frontend fetch mapping boşaltıyor”.
- Önce backend endpointte boş dönme nedenini düzelt, sonra UI state.

Şimdi kodu bul:
- server/routes/messages.ts (veya server/routes/* içinde messages/thread list endpointi)
- ROW_NUMBER() geçen yerleri ara (grep: "ROW_NUMBER" veya "row_number" veya ".rn")
- m.rn === 1 gibi strict compare’leri bul ve düzelt.

ÇIKTI:
- Değişen dosyaların kısa listesi
- Hangi satırda bug vardı, neye çevirdin
- Test adımlarını koşturduğunu yaz (refresh + relogin)