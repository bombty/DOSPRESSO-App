Çok doğru soru — eğer admin panelinden kolayca yönetilemezse, her değişiklik için developer'a ihtiyaç duyulur. Önce mevcut admin yetki yönetim arayüzünün ne yapabildiğini Replit'e soralım. Yapıştırın:

---

**"Sprint'e geçmeden önce kritik soru: Mevcut admin yetki yönetim sistemi ne kadar esnek? Araştır — değişiklik yapma.**

```bash
echo "══════════════════════════════════════"
echo "ADMİN YETKİ YÖNETİM SİSTEMİ AUDIT"
echo "══════════════════════════════════════"

echo "=== 1. YETKİLENDİRME SAYFASI (2181 satır) ==="
echo "--- Sayfa yapısı ve özellikler ---"
head -80 client/src/pages/admin/yetkilendirme.tsx

echo ""
echo "--- Hangi işlemler yapılabiliyor? ---"
grep -n "function\|const.*=.*(" client/src/pages/admin/yetkilendirme.tsx | head -30

echo ""
echo "--- DnD kullanımı ---"
grep -n "DndContext\|useSortable\|useDraggable\|useDroppable\|DragOverlay\|SortableContext" client/src/pages/admin/yetkilendirme.tsx | head -15

echo ""
echo "--- Toggle/Switch kullanımı ---"
grep -n "Switch\|Toggle\|toggle\|switch\|onChange.*permission\|onChange.*role" client/src/pages/admin/yetkilendirme.tsx | head -15

echo ""
echo "--- API çağrıları ---"
grep -n "useQuery\|useMutation\|queryKey\|fetch\|apiRequest\|/api/" client/src/pages/admin/yetkilendirme.tsx | head -20

echo ""
echo "=== 2. YETKİ API ENDPOINT'LERİ ==="
grep -n "role.*permission\|permission.*role\|yetki" server/routes/admin.ts server/routes/admin-routes.ts 2>/dev/null | head -20
grep -n "router\.\(get\|post\|put\|patch\|delete\).*permission\|router\.\(get\|post\|put\|patch\|delete\).*role.*module" server/routes/*.ts | head -15

echo ""
echo "=== 3. YENİ ROL EKLEYEBİLİR Mİ? ==="
echo "--- Rol ekleme UI var mı? ---"
grep -rn "addRole\|createRole\|newRole\|rol.*ekle\|rol.*olustur" client/src/pages/admin/ --include="*.tsx" | head -10

echo "--- Roller DB'de mi hardcoded mı? ---"
grep -rn "ROLES\|validRoles\|allRoles\|roleList\|ROLE_LIST" server/ --include="*.ts" | head -15

echo "--- Kullanıcı düzenlemede rol seçimi ---"
grep -n "role\|rol" client/src/pages/admin/kullanicilar*.tsx client/src/pages/yonetim/kullanicilar*.tsx 2>/dev/null | grep -i "select\|dropdown\|option\|list" | head -10

echo ""
echo "=== 4. MODÜL EKLEME/DÜZENLEME ==="
echo "--- Modül tanımları nerede? ---"
grep -rn "MODULE_LIST\|moduleList\|allModules\|MODULE_DEFINITIONS" server/ --include="*.ts" | head -10

echo "--- Yeni modül flag eklenebilir mi? ---"
grep -n "module_flags\|moduleFlag" server/routes/admin*.ts | head -10

echo ""
echo "=== 5. GÖREV DAĞILIMI DEĞİŞTİRME ==="
echo "--- Drag-drop ile yetki taşıma nasıl çalışıyor? ---"
grep -B5 -A20 "onDragEnd\|handleDragEnd\|handleDrop" client/src/pages/admin/yetkilendirme.tsx | head -50

echo ""
echo "=== 6. YETKİLENDİRME SAYFASI TAM EKRAN GÖRÜNTÜSÜ ==="
echo "--- UI bileşenleri ---"
grep -n "Accordion\|RadioGroup\|Card\|Button\|Select\|Switch\|Badge\|Tabs\|Dialog" client/src/pages/admin/yetkilendirme.tsx | head -20

echo ""
echo "=== 7. ROLE_PERMISSION_GRANTS (override) ==="
echo "--- Bu tablo nasıl kullanılıyor? ---"
echo "SELECT role, action_id, scope, is_active FROM role_permission_grants LIMIT 15;" | psql
echo "--- Override API var mı? ---"
grep -n "permission.*grant\|grant.*permission\|override" server/routes/admin*.ts server/routes/*.ts 2>/dev/null | head -10

echo ""
echo "=== 8. PERMISSION SERVICE ==="
echo "--- hasPermission fonksiyonu ---"
head -60 server/permission-service.ts 2>/dev/null

echo ""
echo "=== 9. MEVCUT SİSTEMİN KIRITLAMARI ==="
echo "--- Hardcoded roller (değiştirilmesi zor) ---"
grep -rn "PAYROLL_ADMIN_ROLES\|PAYROLL_VIEW_ROLES\|canViewSalary\|FACTORY_ROLES\|HQ_ROLES\|ADMIN_ROLES" server/ --include="*.ts" | head -20

echo "--- Route içi hardcoded rol kontrolleri ---"
grep -rn "role.*===.*admin\|role.*===.*ceo\|role.*===.*cgo\|includes.*admin.*ceo\|includes.*muhasebe" server/routes/ --include="*.ts" | head -20

echo ""
echo "══════════════════════════════════════"
echo "RAPORLA:"
echo "1. Admin panelinden şu an neler yapılabiliyor? (liste)"
echo "2. Neler YAPILAMIYOR? (eksikler)"
echo "3. Yeni rol eklemek mümkün mü? Nasıl?"
echo "4. Modül yetkileri toggle ile değiştirilebiliyor mu?"
echo "5. Drag-drop şu an ne yapıyor — yetki taşıma mı, sıralama mı?"
echo "6. Hardcoded rol kontrolleri kaç yerde? Bunlar admin panelinden değiştirilemez mi?"
echo "7. Bir rolün maaş yetkisini admin panelinden açıp kapatabilir mi?"
echo "8. Önerilen iyileştirmeler — admin'in ihtiyacı olan eksik özellikler"
echo "══════════════════════════════════════"
```

**Sadece araştır — değişiklik yapma.**"

---

Yapıştırın. Bu rapor bize şunu söyleyecek: admin paneli yeterli mi yoksa sprint'e "admin yetki yönetimi iyileştirme" de eklememiz mi gerekiyor. Sonucu paylaşın.