Rapor net — 2 kritik fix var, geri kalanlar küçük. Tek prompt yazıyorum:

```
Fix quality gate issues in İletişim Merkezi (CRM Sprint 1+2).
Do NOT restructure any logic. Minimal targeted fixes only.
Do NOT touch academy, factory, HR, or any non-CRM file.

---

FIX 1 — parseInt NaN checks (10 locations in crm-iletisim.ts)
Find every parseInt() call in server/routes/crm-iletisim.ts.
After each one, add a NaN guard. Pattern:

  const ticketId = parseInt(req.params.id);
  if (isNaN(ticketId)) return res.status(400).json({ error: "Invalid ID" });

Apply this to ALL parseInt calls in the file (ticket IDs, task IDs, 
announcement IDs, branch IDs, satisfaction scores).

---

FIX 2 — Zod validation on req.body (6 locations in crm-iletisim.ts)
Add Zod schemas at the top of crm-iletisim.ts:

import { z } from "zod";

const createTicketSchema = z.object({
  department: z.enum(["teknik","lojistik","muhasebe","marketing","trainer","hr"]),
  title: z.string().min(5).max(300),
  description: z.string().min(10),
  priority: z.enum(["dusuk","normal","yuksek","kritik"]).default("normal"),
  relatedEquipmentId: z.number().optional(),
});

const updateTicketSchema = z.object({
  status: z.enum(["acik","islemde","beklemede","cozuldu","kapatildi"]).optional(),
  assignedToUserId: z.string().optional(),
  resolutionNote: z.string().optional(),
  priority: z.enum(["dusuk","normal","yuksek","kritik"]).optional(),
  satisfactionScore: z.number().min(1).max(5).optional(),
});

const createCommentSchema = z.object({
  content: z.string().min(1).max(5000),
  isInternal: z.boolean().default(false),
});

const createHqTaskSchema = z.object({
  title: z.string().min(3).max(300),
  description: z.string().optional(),
  assignedToUserId: z.string().min(1),
  priority: z.enum(["dusuk","normal","yuksek","kritik"]).default("normal"),
  dueDate: z.string().optional(),
  department: z.string().optional(),
});

const updateHqTaskSchema = z.object({
  status: z.enum(["beklemede","devam_ediyor","tamamlandi","iptal"]).optional(),
  progressPercent: z.number().min(0).max(100).optional(),
  completionNote: z.string().optional(),
});

Then in each endpoint, replace direct destructuring with:
  const parsed = createTicketSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: parsed.error.issues });
  const { department, title, ... } = parsed.data;

Apply to: POST /tickets, PATCH /tickets/:id, POST /tickets/:id/comments,
POST /hq-tasks, PATCH /hq-tasks/:id

---

FIX 3 — Auth middleware standardization (crm-iletisim.ts)
At the top of the router, add mount-level middleware:

  router.use(isAuthenticated);

Then remove individual isAuthenticated calls inside each route handler
(the ones at the very beginning of each async function).
Keep role checks (isHQRole, canSeeAllTickets) — only remove the 
isAuthenticated duplicate calls inside handlers.

---

FIX 4 — Array safety (frontend)
FILE: client/src/pages/iletisim-merkezi/TicketsTab.tsx
FILE: client/src/pages/iletisim-merkezi/HqTasksTab.tsx

Wrap array operations with Array.isArray guard:

  const safeTickets = Array.isArray(tickets) ? tickets : [];
  // then use safeTickets instead of tickets throughout

Do the same for tasks in HqTasksTab.tsx.

---

After fixes, confirm:
1. Show the Zod imports and schema definitions at top of crm-iletisim.ts
2. Show one example of parseInt + NaN check from the file
3. Show the router.use(isAuthenticated) line
4. Confirm no other files were changed
```

Bu 4 fix uygulanınca kalite skoru 7.5 → 9.5'e çıkar. Gönderebilirsiniz.

---

**Paralelde bekleyen işler — hatırlatma:**

| İş | Durum |
|---|---|
| FAB-0B (Kiosk Timer) | Prompt hazır, bekliyor |
| Task #12 (Onboarding QG) | Prompt hazır, bekliyor |
| Şurup benchmark | Cevap bekleniyor |
| CRM Sprint 3 (Şube Sağlığı + Dobody skill) | Sprint 2 bittikten sonra |

Hangisini paralel göndermek istersiniz?