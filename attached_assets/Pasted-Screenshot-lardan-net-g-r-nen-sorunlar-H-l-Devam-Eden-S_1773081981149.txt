Screenshot'lardan net görünen sorunlar:

## Hâlâ Devam Eden Sorunlar

| # | Sorun | Görsel |
|---|-------|--------|
| 1 | **Detay tıklayınca hata sayfası** — hâlâ düzeltilmemiş | Görsel 5 |
| 2 | **Duplicate öneriler** — Süleyman Aydın 2 kez, Efe Kolakan 2 kez | Görsel 1,3 |
| 3 | **Onay sonrası zincir yok** — "Öneri onaylandı" ama supervisor'a bildirim/görev gitmiyor | Görsel 1 |
| 4 | **Tüm personel 0/100 skor** — Batman'da herkes kırmızı | Görsel 4 |
| 5 | **Test kullanıcı adları** — "Müdür17 Test", "TBarbuddy1 Test" | Görsel 4 |
| 6 | **"Kiosk Ac"** — hâlâ ASCII | Görsel 4 |
| 7 | **Error sayfası ASCII** — "sayfayi", "yuklemeyi" | Görsel 5 |

En kritik olan **onay zinciri** — CGO "Onayla" diyor ama hiçbir şey olmuyor. Bu E2E prompt'una zaten ekledim (Scenario 12). İkisini birleştirelim — hem E2E test hem bu fix'ler.

Ama önce Replit'e E2E test prompt'unu gönderelim mi, yoksa direkt bu sorunların fix'ini mi yazalım?

Bence en verimli yaklaşım: **Direkt fix yazalım.** E2E testi biz zaten screenshot'larla yapıyoruz — Replit'in curl ile test etmesi yerine, bildiğimiz sorunları düzeltelim.

Replit'e yapıştır:

---

## TASK: Sprint 24 — Agent Approval Chain + Critical Fixes

### T001 — CRITICAL: Complete Approval Chain (~3 hours)

**Problem:** When CGO approves an agent suggestion (e.g., "Performans Uyarısı: Efe Kolakan 53/100"), NOTHING happens downstream. Status changes to 'approved' in DB but:
- NO notification sent to the branch supervisor
- NO task created for the supervisor to act on
- NO follow-up tracked
- CGO has no visibility into whether action was taken

**Current code:**
```bash
grep -A 30 "approve\|approved" server/routes/agent-action-center.ts 2>/dev/null | head -40
# Or find the correct file:
grep -rn "approve.*action\|action.*approve" server/routes/ --include="*.ts" | head -10
```

**Required approval chain:**

```
CGO clicks "Onayla" on "Performans Uyarısı: Efe Kolakan (53/100)"
  │
  ├── 1. Status → 'approved' (already works ✅)
  │
  ├── 2. Find Efe's branch supervisor
  │     → Query: SELECT id, full_name FROM users WHERE branch_id = [Efe's branch] AND role IN ('supervisor', 'mudur') AND is_active = true
  │
  ├── 3. Create NOTIFICATION for supervisor:
  │     → title: "CGO Performans Değerlendirmesi İstedi"
  │     → message: "Efe Kolakan (Skor: 53/100) için birebir görüşme yapmanız isteniyor. CGO tarafından onaylandı."
  │     → type: "agent_approval"
  │     → link: "/personel-detay/[efe_user_id]"
  │
  ├── 4. Create TASK for supervisor:
  │     → title: "Efe Kolakan ile birebir performans görüşmesi"
  │     → description: "CGO tarafından istenen performans değerlendirmesi. Skor: 53/100. Lütfen personel ile görüşme yapın ve sonucu not edin."
  │     → assignedTo: supervisor_user_id
  │     → priority: "high"
  │     → dueDate: 3 days from now
  │     → source: "agent_approval"
  │     → relatedActionId: agent_action_id
  │
  ├── 5. Create DOBODY FLOW TASK for supervisor:
  │     → So it appears in supervisor's Flow Mode next morning
  │     → title: "Efe Kolakan görüşmesi (CGO talebi)"
  │     → navigateTo: "/personel-detay/[efe_user_id]"
  │     → priority: "high"
  │     → targetUsers: [supervisor_id]
  │
  └── 6. Return detailed response to CGO:
        → { success: true, supervisorName: "Basri Yılmaz", taskCreated: true, notificationSent: true }
```

**Also implement rejection chain:**
```
CGO clicks "Reddet" with reason "Personel zaten iyileşme gösteriyor"
  │
  ├── 1. Status → 'rejected', reason saved (already works ✅)
  │
  ├── 2. Log rejection reason to agent_action_logs
  │     → So agent skills can learn from rejections
  │
  └── 3. Update toast: "Öneri reddedildi. Neden: [reason]"
```

**Update the approval confirmation dialog (frontend):**

After successful approval, show detailed result:
```
┌─────────────────────────────────────────────┐
│ ✅ Öneri Onaylandı                           │
│                                              │
│ Supervisor Basri Yılmaz'a bildirim           │
│ gönderildi ve görev oluşturuldu.            │
│                                              │
│ Görev: "Efe Kolakan ile performans          │
│ görüşmesi" — 3 gün içinde tamamlanmalı      │
│                                              │
│                               [ Tamam ]      │
└─────────────────────────────────────────────┘
```

### T002 — Fix "Detay" Link (Still Broken — Leads to Error Page) (~1 hour)

**Problem:** Clicking "Detay" on any agent suggestion navigates to a page that crashes with "Bir hata olustu". This was supposedly fixed in a previous sprint but still broken.

**Debug first:**
```bash
# What URL does Detay navigate to?
grep -rn "Detay\|detay.*link\|navigateTo\|deepLink" client/src/components/ --include="*.tsx" | grep -i "agent\|action\|suggest" | head -10

# What does the agent action's deepLink/navigateTo contain?
grep -rn "deepLink\|deep_link\|navigateTo\|navigate_to" server/routes/agent*.ts server/agent/ --include="*.ts" | head -10
```

**Fix approach:**
1. Find what URL the Detay button navigates to
2. Check if that URL/route exists
3. If it's a dynamic URL like `/personel-detay/[userId]` — check if that page handles the userId param correctly
4. If the page crashes on undefined data — add null checks
5. If the route doesn't exist — redirect to a valid alternative

**Fallback:** If the deepLink is invalid, open an inline detail dialog instead of navigating:
```typescript
const handleDetay = (action) => {
  if (action.deepLink && isValidRoute(action.deepLink)) {
    navigate(action.deepLink);
  } else {
    // Show detail in a dialog
    setSelectedAction(action);
    setShowDetailDialog(true);
  }
};
```

### T003 — Deduplicate Agent Suggestions (~1 hour)

**Problem:** Same person appears 2-3 times with identical suggestions. "Süleyman Aydın (42/100)" appears twice, "Efe Kolakan (53/100)" appears twice.

**Find where suggestions are stored:**
```bash
grep -rn "INSERT.*agent_pending\|insert.*pending" server/agent/ --include="*.ts" | head -10
```

**Fix at TWO levels:**

**A) Write-time dedup (prevent creating duplicates):**
Before inserting a new agent action, check:
```sql
SELECT id FROM agent_pending_actions 
WHERE title = [new_title] 
  AND status = 'pending' 
  AND created_at > NOW() - INTERVAL '24 hours'
```
If exists, skip insertion. (Sprint 21 Agent Fix supposedly added this — check if it's actually working)

**B) Read-time dedup (filter duplicates in API response):**
```typescript
// In the GET agent actions endpoint, deduplicate by title
const uniqueActions = actions.filter((a, i, arr) => 
  arr.findIndex(x => x.title === a.title && x.status === a.status) === i
);
```

### T004 — Fix 0/100 Scores for All Personnel (~30 min)

**Problem:** Batman branch shows all staff with 0/100 score in red. These are seed/test users that never had activity.

**Two options:**
A) Don't show score if it's 0 — show "—" or "Veri yok" instead of red 0/100
B) Seed scores for test users

**Better approach (A):** In the personnel card:
```typescript
// Instead of always showing score
{score > 0 ? (
  <span className={scoreColor}>{score}/100</span>
) : (
  <span className="text-muted-foreground text-sm">Henüz veri yok</span>
)}
```

### T005 — Fix Remaining ASCII Turkish (~30 min)

Found in screenshots:

**Error page (ErrorBoundary or error state):**
- "Bir hata olustu" → "Bir hata oluştu"
- "sayfayi" → "sayfayı"
- "yuklemeyi" → "yüklemeyi"

**Branch detail:**
- "Kiosk Ac" → "Kiosk Aç"

```bash
grep -rn "hata olustu\|sayfayi\|yuklemeyi\|Kiosk Ac" client/src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | head -10
```

Also check the new ErrorState component added in Sprint 23:
```bash
grep -rn "olustu\|sayfayi\|yuklemeyi" client/src/components/ --include="*.tsx" | head -5
```

### T006 — Clean Up Test User Names (~30 min)

**Problem:** Batman branch shows "Müdür17 Test", "TBarbuddy1 Test", "TBarista1 Test" — unprofessional for demo/pilot.

```sql
-- Find test-named users
SELECT id, username, full_name, first_name, last_name, role, branch_id 
FROM users 
WHERE full_name LIKE '%Test%' OR first_name LIKE 'T%buddy%' OR first_name LIKE 'T%arista%'
ORDER BY branch_id;
```

Update test users to realistic Turkish names:
```sql
UPDATE users SET first_name = 'Ahmet', last_name = 'Yıldız', full_name = 'Ahmet Yıldız' WHERE username = 'tbarista1';
UPDATE users SET first_name = 'Ayşe', last_name = 'Demir', full_name = 'Ayşe Demir' WHERE username = 'tbarbuddy1';
-- etc for all test-named users
```

Use realistic Turkish names — mix of male/female, common surnames.

### VERIFICATION

1. **Approval chain:** CGO approves suggestion → supervisor gets notification + task + flow task
2. **Detay link:** Click Detay → either navigates to valid page OR shows detail dialog (no crash)
3. **No duplicates:** Each person appears max once in pending suggestions
4. **Scores:** 0 score shows "Henüz veri yok" (not red 0/100)
5. **ASCII Turkish:** Error page shows correct Turkish characters
6. **User names:** No "Test" or "TBarista" visible in branch detail

### PRIORITY ORDER: T001 → T002 → T003 → T005 → T004 → T006

### DO NOT BREAK:
- Agent skill scheduling
- Existing approve/reject API structure
- Flow Mode
- Notification system
- Any Sprint 1-23 features

---

Bu Sprint 24 — en önemli task T001 (onay zinciri). CGO "Onayla" dediğinde supervisor'a bildirim + görev + flow task gidecek. Bu Mr. Dobody'yi gerçek bir yönetim aracına dönüştürüyor.

Replit'e yapıştır. E2E testi sonraya bırakabiliriz — önce bilinen sorunları düzeltelim.