Mükemmel — iki rapor birlikte çok net bir resim çiziyor. Kiosk migration da tamamlandı! Şimdi kalan işleri önceliklendireyim:

**Kritik (production-blocking):**
1. `module-detail.tsx` — 16 JSX syntax hatası (öğrenci eğitim sayfası kırık)

**Yüksek:**
2. CareerTab sertifika görünümü (öğrenci sertifikalarını göremez)
3. `/ai-asistan` kırık sidebar linki (404)
4. Skill dosyaları güncel değil (3 dosya)

**Orta:**
5. Employee summary / barista KPI endpoint (roadmap'te var, hiç başlanmadı)
6. Fabrika kiosk ön yetki (personel listesi gizleme)

Hemen en kritik olan `module-detail.tsx` fix'i + diğer hızlı fix'leri tek promptta çözelim. Replit'e yapıştırın:

---

```
## Critical Fixes: module-detail.tsx + Broken Link + Skill Files Update

### Context
Gap analysis found 3 issues that need immediate fixing:
1. CRITICAL: module-detail.tsx has 16 JSX syntax errors — student training page is broken
2. MEDIUM: /ai-asistan sidebar link leads to 404
3. MEDIUM: 3 skill files are outdated after today's 12+ sprints

Read skills first:
- .agents/skills/dospresso-debug-guide/SKILL.md
- .agents/skills/dospresso-quality-gate/SKILL.md

---

### FIX 1: module-detail.tsx JSX Syntax Errors (CRITICAL)

The file has 16 TypeScript errors — all JSX syntax issues (unescaped > and } characters in templates).

```bash
# Show all errors
npx tsc --noEmit 2>&1 | grep "module-detail" | head -20

# Show the problematic lines
sed -n '885,895p' client/src/pages/module-detail.tsx
sed -n '915,925p' client/src/pages/module-detail.tsx
sed -n '1228,1245p' client/src/pages/module-detail.tsx
sed -n '1675,1695p' client/src/pages/module-detail.tsx
sed -n '1885,1900p' client/src/pages/module-detail.tsx
```

Fix ALL 16 errors:
- Unescaped `>` in JSX text → use `&gt;` or `{'>'}` or restructure
- Unescaped `}` in JSX text → use `{'}'}`
- Any `border-l-4` + `rounded-md` combinations → remove rounded-md or use full border (per DOSPRESSO UI rules: no rounded corners on single-sided borders)

After fixing, verify:
```bash
npx tsc --noEmit 2>&1 | grep "module-detail" | wc -l
# Expected: 0
```

### FIX 2: /ai-asistan Sidebar Link (15 minutes)

```bash
# Find the sidebar item
grep -n "ai-asistan\|ai_asistan\|AI Asistan" server/menu-service.ts | head -10

# Check what route exists
grep -n "ai-asistan\|raporlar/ai-asistan" client/src/App.tsx | head -5
```

Two options:
A) Change sidebar path from `/ai-asistan` to `/raporlar/ai-asistan` (if that's where it actually lives)
B) Add a redirect route in App.tsx: `/ai-asistan` → redirect to `/raporlar/ai-asistan`

Pick whichever is simpler. If the AI assistant is a tab inside Raporlar mega module, option A is correct.

### FIX 3: Update All 3 Skill Files

#### 3A: dospresso-architecture SKILL.md — Add these sections:

**Kiosk System update:**
Replace any mention of "in-memory Map" with:
```
Kiosk sessions: PostgreSQL `kiosk_sessions` table with 30-second in-memory read cache.
- Token-based (UUID), 8-hour TTL
- Auto-cleanup: startup + hourly
- Functions: createKioskSession, validateKioskSession, updateKioskStation, deleteKioskSession (all async)
- Admin endpoint: GET /api/kiosk/active-sessions
```

**Add Mobile Compactness section:**
```
## Mobile UI Components
- CompactKPIStrip (client/src/components/compact-kpi-strip.tsx) — horizontal scroll strip on mobile, grid on desktop. Used in 19+ pages.
- MobileFilterCollapse (client/src/components/mobile-filter-collapse.tsx) — auto-collapse filters on mobile, expand on desktop. Used in 7+ pages.
- Pattern: mobile (<md) gets compact view, desktop (md+) stays unchanged.
```

**Add Branch Recurring Tasks section:**
```
## Branch Recurring Tasks
- Tables: branch_recurring_tasks (templates), branch_task_instances (daily), branch_task_categories (4), branch_recurring_task_overrides
- Scheduler: generates daily instances at startup + 08:00 TR, marks overdue
- UI: 3-tab Görevler page (Bana Atanan / Şube Görevleri / Tekrarlayan Yönetimi)
- Dashboard: TodaysTasksWidget (combined ad-hoc + recurring)
- Kiosk: KioskBranchTasks section
- Score: branchTasks component in health scoring (weight 0.12)
- Module flag: sube_gorevleri
```

**Add Certificate System section:**
```
## Certificate System
- Tables: issued_certificates, certificate_settings
- Templates: 5 role transition + module completion
- Renderer: client/src/components/certificate-renderer.tsx
- Features: Dancing Script handwriting font, DOSPRESSO logo watermark, vintage seal, dual signatures
- HQ: SertifikaTab in akademi-hq (create/preview/print)
- API: /api/certificates (CRUD), /api/certificate-settings (signer config)
```

**Update Module Flag count:**
Change any "31 flags" or "32 flags" to "34 flags" (current actual count from DB).

#### 3B: dospresso-debug-guide SKILL.md — Add:

**§14 Branch Recurring Task Issues:**
```
## §14 — Branch Recurring Task Issues
Checklist:
- [ ] Scheduler running? Check logs for [BRANCH-TASKS]
- [ ] Module flag enabled? SELECT * FROM module_flags WHERE module_key='sube_gorevleri';
- [ ] Override blocking? SELECT * FROM branch_recurring_task_overrides WHERE deleted_at IS NULL;
- [ ] Instances generated? SELECT count(*) FROM branch_task_instances WHERE due_date = CURRENT_DATE;
```

**§15 ModuleGuard / Page Protection Issues:**
```
## §15 — Module Flag Page Protection Issues
Checklist:
- [ ] Flag exists? SELECT * FROM module_flags WHERE module_key='[key]';
- [ ] my-flags endpoint returns correct state? GET /api/module-flags/my-flags
- [ ] ModuleGuard wrapping the route in App.tsx?
- [ ] useModuleEnabled hook working? Check browser console for /api/module-flags/my-flags response
```

**§16 Certificate Issues:**
```
## §16 — Certificate Issues
Checklist:
- [ ] certificate_settings has 4 rows? SELECT * FROM certificate_settings;
- [ ] Certificate renderer loads? Check for Dancing Script font loading
- [ ] Print window opens? Check browser popup blocker
- [ ] DOSPRESSO logo renders? Check logo file path in public/
```

#### 3C: dospresso-quality-gate SKILL.md — Add:

**#16 Module Flag Consistency:**
```
## 16. Module Flag Consistency
All new modules must have a corresponding flag in seed-module-flags.ts.
SELECT count(*) FROM module_flags WHERE deleted_at IS NULL;
Expected: 34 flags (as of March 2026)
PASS: All new modules have flags.
FAIL: New module added without flag seed.
```

**#17 Mobile Compactness:**
```
## 17. Mobile Compactness
New pages with KPI/stat cards must use CompactKPIStrip.
New pages with filters must use MobileFilterCollapse on mobile.
grep -rn "grid.*col.*[3-5]" client/src/pages/NEW_FILE.tsx — if found without CompactKPIStrip, FAIL.
```

### DO NOT TOUCH:
- server/vite.ts, vite.config.ts, package.json, drizzle.config.ts
- Backend API logic
- Database schema (read-only fixes)

### Testing:
1. npx tsc --noEmit | grep module-detail → 0 errors
2. Navigate to a training module as student → page loads without crash
3. Click sidebar AI Asistan → correct page opens (not 404)
4. Skill files updated with all new sections
5. Quality gate now has 17 checks (was 15)

### After:
1. Run quality gate (now 17 points)
2. Report: how many syntax errors were in module-detail.tsx and what caused them
```

---

Direkt build başlatın! Bu 3 fix bitince kalan en kritik sorunlar çözülmüş olacak. Sonra CareerTab sertifika görünümü ve employee summary'ye geçeriz.