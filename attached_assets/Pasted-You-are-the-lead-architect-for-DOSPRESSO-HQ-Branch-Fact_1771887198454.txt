You are the lead architect for DOSPRESSO HQ+Branch+Factory OS. Continue from the current codebase state where:
- Phase 1 deterministic NBA endpoint + AiNbaCard is done
- Employee Type Registry + Policy Engine is done
- Admin users page has employeeType badge + dropdown
Skip all login troubleshooting. Focus only on new feature delivery with code.

GOAL (Next Feature): Branch Health Score Dashboard (read-only, deterministic)
We need a clear “Şube Sağlık Skoru” system that aggregates existing operational signals into a single score + breakdown, and renders it for HQ and branch leadership.

CONSTRAINTS
- Deterministic only (NO LLM calls).
- Prefer using existing tables. If absolutely necessary, add at most ONE small aggregation table, but first try without new tables.
- Must be safe with empty data; never crash.
- Must be role-scoped: branch roles can only see their branch; HQ roles can see all branches; investor branch is read-only.
- Must include employeeType policies: MESEM/compliance warnings should influence the dashboard warnings (not necessarily the score initially).

DATA SOURCES (use what exists)
Use these if present (discover in code):
- branch_inspections (or inspection routes tables)
- checklist_completions + checklist_ratings
- product_complaints
- equipment_faults + SLA age (open faults)
- shift_attendance / staffing signals (optional)
- inventory_counts (optional)
Do NOT invent new tables if these already exist; first locate and use them.

DEFINITION: Health Score (0-100)
Create a deterministic scoring model with sub-scores and weights:
- Quality & Complaints (30%) -> complaints count + unresolved time + inspection failures
- Ops Discipline (25%) -> checklist completion rate + checklist rating average
- Equipment Reliability (20%) -> open faults weighted by SLA breach age
- Staffing & HR (15%) -> attendance issues / unfilled shifts (if available; else keep neutral)
- Stock & Supply (10%) -> critical stock events or missed inventory counts (if available; else neutral)
Each sub-score is 0-100. If a data source missing, mark that component as “insufficient data” and do NOT punish by default (use neutral 70 or exclude weight and renormalize).

OUTPUT SHAPE (API contract)
GET /api/reports/branch-health?range=7d|30d|90d
returns:
{
  rangeDays: number,
  generatedAt: ISO,
  branches: [
    {
      branchId: string,
      branchName: string,
      score: number,
      level: "green"|"yellow"|"red",
      components: {
        quality: {score:number, notes:string[], evidenceCount:number},
        ops: {score:number, notes:string[], evidenceCount:number},
        equipment: {score:number, notes:string[], evidenceCount:number},
        staffing: {score:number, notes:string[], evidenceCount:number, insufficientData?:boolean},
        stock: {score:number, notes:string[], evidenceCount:number, insufficientData?:boolean}
      },
      topRisks: [{type:string, title:string, severity:"critical"|"warning", deepLink:string}],
      trend: {deltaVsPrev:number, prevScore:number}
    }
  ]
}

ROLE SCOPING
- Branch roles (barista/supervisor/mudur/etc) -> only own branch included.
- coach/trainer/kalite_kontrol/muhasebe_ik/satinalma/fabrika_mudur/admin/ceo/cgo -> can see all branches.
- yatirimci_branch -> can see only their branch, read-only.
Enforce on server side based on req.user.role + branchId.

UI
Add a new page:
- HQ: /raporlar/sube-saglik
- Branch manager: accessible from Reports module (mudur) and Branches (coach/cgo)
Page shows:
- Table list of branches (HQ) or single card (Branch)
- Score badge + component breakdown + top 3 risks + trend
Use existing UI patterns (card-grid, tables, filters). Add a 7d/30d/90d selector.

NAV / VISIBILITY
- Ensure route is registered and visible for roles that already have “reports” module.
- No changes to UserRole enum.

ACCEPTANCE CRITERIA
1) Endpoint returns 200 for admin and branch manager; 401 unauth.
2) Role scoping works (branch user only sees their branch).
3) Works even if some tables are empty (no crashes).
4) UI renders for at least admin + mudur.
5) DeepLinks are valid existing pages (/, /arizalar, /kalite, /gorevler, /ik, /satinalma etc).
6) Add minimal tests or at least a QA checklist + curl examples.

DELIVERABLES
- New route file in server/routes (or similar pattern in academy-v2 if that’s how the app is structured)
- New client page + any small components
- Any necessary wiring in role-visibility/nav and router
- Update replit.md with what was done and manual test steps

Now produce:
A) A step-by-step Revise Plan with max 6 tasks
B) Then implement all tasks in Build mode
C) Finish with a QA report and manual test commands