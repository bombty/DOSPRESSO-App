GOAL:
HR > İK Yönetimi (/ik) > Excel İçe Aktar modalındaki “Geçmiş Importlar” bölümünü “batch detail” + rapor indirme + (opsiyonel) rollback ile tamamla.

CURRENT STATE (verified):
- Excel İçe Aktar modalında “Geçmiş Importlar” tablosu görünüyor (columns: Tarih/Mod/MatchKey/Created/Updated/Skipped/Errors/Status).
- Backend endpoints exist:
  1) GET /api/hr/employees/import/batches
  2) GET /api/hr/employees/import/batches/:batchId
  3) GET /api/hr/employees/import/batches/:batchId/error-report
  4) POST /api/hr/employees/import/:batchId/rollback

TASKS (do in this order):
1) Make each batch row in “Geçmiş Importlar” clickable (row click or “Detay” icon).
2) On click, open a right-side Sheet/Drawer (or modal) named: “Import Detayı”.
   In the sheet show:
   - Header: Batch ID, Tarih, Status badge
   - Meta: Mod, MatchKey, Dosya adı (varsa), Oluşturan (varsa), Oluşturma zamanı
   - Stat cards: Created / Updated / Skipped / Errors (also Deactivated if exists)
   - A results table (row-level outcomes) from the batch detail endpoint:
     columns: ExcelRowNo (or rowIndex), action/result (created/updated/skipped/error), message/error, and matched key value if available.
   - If endpoint returns 403: show explicit Turkish message: “Bu batch detayını görüntüleme yetkiniz yok.”
3) Add download actions inside the detail sheet:
   - If Errors > 0: show button “Hata Raporu İndir” → calls /error-report and downloads file.
   - If Skipped > 0: show button “Atlananları İndir (CSV)” → create CSV client-side from skipped rows in detail payload (or if backend provides list), then download.
4) Add Rollback (optional but desired):
   - Button: “Geri Al” (danger). Only visible for authorized roles (Admin/HQ) AND only if batch age <= 7 days.
   - On click show AlertDialog confirmation (Turkish): “Bu import geri alınacak. Emin misiniz?”
   - Call POST /api/hr/employees/import/:batchId/rollback
   - On success: toast + invalidate queries (batches list and any related employee lists).
   - On 403: show “Geri alma yetkiniz yok.”
5) UX polish:
   - Loading states (skeleton/spinner) for batches list and detail sheet.
   - Empty states: “Henüz import geçmişi yok.”
   - Keep the current import flow intact; no breaking changes.

OUTPUT REQUIRED:
- List the exact files changed.
- Short summary of behavior changes.
- Quick manual test checklist (prod + preview).