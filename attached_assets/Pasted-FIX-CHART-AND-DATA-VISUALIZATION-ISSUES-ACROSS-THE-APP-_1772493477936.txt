FIX CHART AND DATA VISUALIZATION ISSUES ACROSS THE APP

TASK 1 — Fix Şube Sağlık Skoru Gauge/Progress Display
WHERE: Şube Sağlık Skoru (Branch Health Score) page or dashboard widget
PROBLEM: The health score gauge or progress indicator may show incorrect data, stuck at a fixed number, or not reflect real calculated values.
FIX: 
- Verify the health score calculation pulls from real data (tasks completed, audit scores, customer feedback, equipment status)
- Make sure the gauge/progress component receives the calculated score and renders it
- If data is insufficient, show "Veri yetersiz" message instead of a fake number
VERIFY: Score changes when underlying data changes. Display is not hardcoded.

TASK 2 — Fix Trend Charts Not Rendering or Showing Wrong Data
WHERE: Any page with trend/line charts (Satınalma trendleri, Performans trendi, Gelir-Gider trendi)
PROBLEM: Some trend charts may render empty or with incorrect axis labels.
FIX:
- Check that chart data arrays are not empty before rendering
- If data array is empty, show empty state: "Bu dönem için veri bulunmuyor"
- Verify X-axis shows correct Turkish month names (Oca, Şub, Mar, Nis, May, Haz, Tem, Ağu, Eyl, Eki, Kas, Ara)
- Verify Y-axis labels use Turkish number format where applicable
VERIFY: Charts with data render correctly. Charts without data show clean empty state.

TASK 3 — Fix Pie/Donut Chart Color and Label Issues
WHERE: All pie and donut charts across the app
PROBLEM: After the CAPA pie chart fix (Ticket 1), check ALL other pie/donut charts for similar issues:
- Duplicate labels (all slices same name)
- Missing labels
- Colors not distinguishable
- Legend not matching slices
FIX: For each pie chart found, verify:
1. Data mapping correctly assigns unique labels to each slice
2. Color array has enough distinct colors for all categories  
3. Legend matches the actual slices
4. Turkish labels are used (not English status/category names)
VERIFY: Each pie chart has distinct labels and colors per slice.

TASK 4 — Fix Any Empty Analytics Sections
WHERE: All analytics/dashboard pages (Ekipman Analitiği, Satınalma Analitiği, Performans Analitiği)
PROBLEM: Some analytics sections render completely blank — no chart, no message, nothing.
FIX: For EVERY chart component on analytics pages:
- Add a data check: if data is null/empty/undefined, show a clean empty state card with icon and message
- Empty state message format: "[Chart Title] — Henüz veri bulunmuyor" with a subtle gray icon
- Never leave a section completely blank
VERIFY: Navigate through all analytics pages. Every section shows either real data OR a clean empty state. No blank white spaces.

TASK 5 — Verify All Chart Tooltips Are in Turkish
WHERE: All charts across the app (hover tooltips)
PROBLEM: Chart tooltip text may still show English labels like "Value:", "Count:", "Total:"
FIX: Check Recharts/Chart.js tooltip configuration. Replace any English tooltip labels:
- "Value" → "Değer"
- "Count" → "Adet"  
- "Total" → "Toplam"
- "Average" → "Ortalama"
- "Date" → "Tarih"
- "Score" → "Puan"
- "Percentage" → "Yüzde"
VERIFY: Hover over chart elements — tooltips show Turkish labels.

DO NOT BREAK:
- CAPA pie chart fix from Ticket 1
- Equipment analytics fault priority chart fix from Ticket 1
- All previous Ticket 0-4 fixes
- Chart data fetching logic
- Responsive chart sizing