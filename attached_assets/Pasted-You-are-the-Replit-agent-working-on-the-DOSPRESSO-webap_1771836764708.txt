You are the Replit agent working on the DOSPRESSO webapp.

GOAL (Sprint P0):
Implement the audit’s Sprint plan with STRICT scope control:
- Do NOT add new DB tables in P0.
- You MAY write logs ONLY to existing ai_agent_logs (if it exists already in codebase).
- Use existing routes/pages for deepLinks. Do not invent new pages.
- Keep AI outputs READ-ONLY: return recommendations/alerts/actions; no auto-writes to operational tables.
- Implement in the exact order below (1→5). Do not reorder.
- At the end, output: (a) files touched, (b) endpoints added/changed, (c) manual test checklist.

CONTEXT (what audit requires):
- Add “Global Task Taxonomy (8 buckets)” and “Role → Top recurring tasks” mapping into code config, as source of truth.
- Biggest current gaps:
  1) gida_muhendisi exists in shared/schema.ts dashboard routes but is missing from role-visibility.ts (NAV_ITEMS_BY_ROLE + MODULES_BY_ROLE).
  2) mudur has 0 task templates in role_task_templates seed (needs seed).
  3) user_career_progress table exists but has almost no seed rows → AI dashboard becomes empty.
  4) Some tasks reference missing tables (payroll/SGK/kasa). In P0, keep AI outputs READ-ONLY and derive signals only from tables that exist; if missing, show “data missing” style signals.

DELIVERABLES / PLAN (MAX 5 ITEMS, EXACT):

1) Create shared/ai-nba-config.ts as the SINGLE source of truth for:
   - 8 Global Task Taxonomy buckets T1..T8 (definitions + examples).
   - Role → recurring tasks mapping for ALL active roles (19 roles).
   - Role → AI signals/thresholds templates (alerts + actions).
   Acceptance Criteria:
   - All roles from shared/schema.ts UserRole are present in config.
   - Each role has: <=3 alerts, <=3 actions templates, and references to buckets.
   - No DB schema changes.
   Manual Check:
   - Open file; verify every role key exists; no “fallthrough defaults”.

2) Build GET /api/ai/dashboard-nba endpoint:
   - Returns: { role, alerts[], actions[0..3], analyzedAt } using the contract:
     alerts: {type,title,severity,count}
     actions: {type,title,reason,severity,deepLink,estimatedMinutes}
   - Scope results by req.user.role (and branchId/userId where relevant).
   - Reads ONLY existing tables; if data missing, return safe empty states + “missing-data” alert (warning).
   - Log execution to ai_agent_logs (if table exists in code); otherwise skip logging (do not create table).
   Acceptance Criteria:
   - 200 OK for all role groups (Branch Floor, Branch Mgmt, HQ Ops, HQ Finance, Factory, Executive).
   - At least 1 role per group yields non-empty actions when seed exists.
   Manual Test:
   - curl or browser call as: barista, supervisor, satinalma, fabrika_mudur, coach, ceo.
   - Verify deepLink points to existing routes only.

3) Seed user_career_progress for at least 5 active employees with varied scores:
   - Ensure AI can generate improvement/risk signals.
   Acceptance Criteria:
   - user_career_progress has multiple rows (not only admin).
   - Supervisor/coach views show at least one “team risk” signal.
   Manual Test:
   - Login as barista → sees 1 improvement action.
   - Login as supervisor → sees 1 team risk action.
   - Login as coach → sees multi-branch/team insights.

4) Build <AiNbaCard /> dashboard widget and make it visible for ALL roles:
   - Add to WIDGET_VISIBILITY (or equivalent) for every active role.
   - States: loading, empty, error, normal.
   - Each action is clickable and navigates to deepLink.
   Acceptance Criteria:
   - Card renders on dashboard for all 19 roles.
   - data-testid attributes exist for key UI elements.
   Manual Test:
   - Login as stajyer + fabrika_operator + ceo → card visible.
   - Click an action → navigates correctly.

5) Fix role coverage gaps:
   (A) Add gida_muhendisi to client/src/lib/role-visibility.ts:
       - NAV_ITEMS_BY_ROLE + MODULES_BY_ROLE (+ any other visibility maps used).
       - Ensure it shows correct nav/modules for food safety dashboard route.
   (B) Seed role_task_templates for:
       - mudur: at least 8 recurring tasks across T1/T4/T5/T6/T8
       - gida_muhendisi: at least 5 recurring tasks across T3/T1/T8
   Acceptance Criteria:
   - gida_muhendisi no longer falls through to empty nav/modules.
   - mudur shows tasks/AI signals based on seeded templates (where applicable).
   Manual Test:
   - Login as gida_muhendisi → nav/modules visible + dashboard route works.
   - Login as mudur → sees tasks/AI card non-empty (given seed + existing tables).

IMPORTANT IMPLEMENTATION NOTES:
- Do not implement payroll/SGK/kasa DB tables in P0. If a signal depends on missing data, return a warning alert “data missing” with deepLink to relevant module.
- Prefer deterministic logic over LLM calls in P0 (rule-based signals). If any AI/LLM call exists, keep it optional behind feature flag.
- Keep all configs typed and derived from shared/schema.ts roles to prevent drift.
- Output at the end: touched files, endpoints, and exact manual test steps.