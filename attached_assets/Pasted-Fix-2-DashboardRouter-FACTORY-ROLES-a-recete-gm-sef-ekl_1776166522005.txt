Fix 2: DashboardRouter — FACTORY_ROLES’a recete_gm + sef ekle:

Fix 3: RGM sidebar’ına stok yönetimi ekle:

Tüm değişiklikleri doğrulayıp commit ediyorum:​​​​​​​​​​​​​​​​

Push: 8a76670e. İşte Replit talimatı — DB migration gerekiyor:

TASK #93: RGM Dashboard + Inventory Import — Pull + Build + Migration + Test
Step 1: Pull + Build

git pull origin main
npm run build


Step 2: DB Migration

npx drizzle-kit push


Eğer timeout:

echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS market_price NUMERIC(10,2);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS market_price_updated_at TIMESTAMP;" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS material_type VARCHAR(10);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS purchase_unit VARCHAR(20);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS recipe_unit VARCHAR(20);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS conversion_factor NUMERIC(12,4);" | psql $DATABASE_URL

echo "CREATE TABLE IF NOT EXISTS inventory_price_history (
  id SERIAL PRIMARY KEY,
  inventory_id INTEGER NOT NULL REFERENCES inventory(id) ON DELETE CASCADE,
  price_type VARCHAR(20) NOT NULL,
  price NUMERIC(12,2) NOT NULL,
  previous_price NUMERIC(12,2),
  change_percent NUMERIC(6,2),
  source VARCHAR(30) NOT NULL,
  source_reference_id INTEGER,
  effective_date DATE NOT NULL,
  notes TEXT,
  created_by VARCHAR REFERENCES users(id) ON DELETE SET NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);" | psql $DATABASE_URL

echo "CREATE INDEX IF NOT EXISTS iph_inventory_idx ON inventory_price_history(inventory_id);" | psql $DATABASE_URL
echo "CREATE INDEX IF NOT EXISTS iph_date_idx ON inventory_price_history(effective_date);" | psql $DATABASE_URL


Verify:

echo "SELECT column_name FROM information_schema.columns WHERE table_name='inventory' AND column_name IN ('market_price','material_type','purchase_unit','recipe_unit','conversion_factor') ORDER BY column_name;" | psql $DATABASE_URL
echo "SELECT count(*) FROM information_schema.tables WHERE table_name='inventory_price_history';" | psql $DATABASE_URL


Expected: 5 columns + 1 table.
Step 3: Server + RGM Test

npm run dev


# RGM login
curl -s -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"RGM","password":"0000"}' | python3 -c "import sys,json; d=json.load(sys.stdin); print('RGM:', d.get('role',''), d.get('error',''))"


RGM olarak browser’da ana sayfa açıp modül kartlarının göründüğünü doğrula (Fabrika, Reçeteler, Stok, Raporlar).
Step 4: İK Fix Test
Admin olarak login → İK & Personel’e tıkla → “Building2” hatası olmamalı.
DO NOT BREAK: Factory kiosk, recipes, production logs, QC, existing inventory data.
Report:

PULL+BUILD: ✅/❌
MIGRATION: ✅/❌ (5 cols + 1 table)
RGM DASHBOARD: ✅/❌ (module cards visible)
IK FIX: ✅/❌ (no Building2 crash)


Bu talimatı Replit’e gönder. Ben şimdi otomatik reçete versiyonlama (PATCH → snapshot) üzerinde çalışmaya devam ediyorum.​​​​​​​​​​​​​​​​