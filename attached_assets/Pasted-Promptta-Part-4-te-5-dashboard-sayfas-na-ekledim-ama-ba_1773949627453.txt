Promptta Part 4'te 5 dashboard sayfasına ekledim ama bazı roller eksik kaldı. **Yapıştırmadan önce** şu ekleri yapın:

**Part 4'ün sonuna bu sayfaları da ekleyin:**

```
Also add GuidanceWidget to these pages:
6. client/src/pages/admin/ (admin mega page — if it has a dashboard section)
7. client/src/pages/bordro.tsx or wherever muhasebe_ik lands
8. client/src/pages/satinalma/ (satınalma dashboard)
9. client/src/pages/fabrika/index.tsx (fabrika main page — for fabrika_mudur)

Actually, the SIMPLEST approach: Add GuidanceWidget to the MAIN LAYOUT component so it appears on EVERY page for management roles. Find the layout wrapper:

```bash
grep -rn "Layout\|MainLayout\|AppLayout\|PageWrapper" client/src/App.tsx client/src/components/ --include="*.tsx" | head -10
```

If there's a shared layout component, add GuidanceWidget there with a role check:

```typescript
// In the shared layout (or App.tsx wrapper):
const MANAGEMENT_ROLES = ['admin', 'ceo', 'cgo', 'coach', 'trainer', 'muhasebe_ik', 'satinalma', 'kalite_kontrol', 'gida_muhendisi', 'fabrika_mudur', 'mudur', 'supervisor', 'supervisor_buddy'];

{user && MANAGEMENT_ROLES.includes(user.role) && <GuidanceWidget />}
```

This way ALL management roles see guidance on EVERY page — no need to edit 10+ individual page files.
```

**Ve Part 5'e bu rol örneklerini de ekleyin:**

```
### Muhasebe/İK (Mahmut) First Login:
```
🔴 KRİTİK: Lara şubesinde personel hesabı eksik — Oluşturun [Düzelt →]
🟡 YÜKSEK: Bordro parametreleri kontrol edilmeli [Git →]
🔵 ORTA: Personel izin bakiyeleri girilmemiş [Git →]
🔵 ORTA: Personel giriş tarihleri eksik [Git →]
```

### Satınalma (Samet) First Login:
```
🔵 ORTA: Tedarikçi listesi eksik (5 kayıt) — Excel ile import edin [Git →]
🔵 ORTA: Satınalma siparişi henüz oluşturulmamış [Git →]
🔵 DÜŞÜK: Stok minimum seviyeleri tanımlı değil [Git →]
```

### Fabrika Müdür (Eren) First Login:
```
🟡 YÜKSEK: 4 stajyerin onboarding durumu kontrol edilmeli [Git →]
🔵 ORTA: Fabrika vardiya planı bu hafta için oluşturulmamış [Git →]
🔵 ORTA: Üretim hedefleri tanımlı değil [Git →]
✅ 9 istasyon aktif, 74 üretim kaydı mevcut
```

### Kalite Kontrol (Ümran) First Login:
```
🟡 YÜKSEK: 14 şubede denetim checklist'i yok [Düzelt →]
🔵 ORTA: HACCP kontrol noktaları tanımlı değil [Git →]
🔵 ORTA: Son 30 günde 0 kalite denetimi yapılmış [Git →]
```

### Supervisor (Basri — Işıklar) First Login:
```
🟡 YÜKSEK: Açılış checklist'i oluşturulmamış [Düzelt →]  
🟡 YÜKSEK: Kapanış checklist'i oluşturulmamış [Düzelt →]
🔵 ORTA: Bu hafta vardiya planı 3 personel eksik [Git →]
🔵 ORTA: 2 personelin PDKS kaydı hiç yok [Git →]
✅ 7 barista aktif, görev sistemi hazır
```

### Müdür (Erdem — Işıklar) First Login:
```
🟡 YÜKSEK: Bu ay izin planı oluşturulmamış [Git →]
🔵 ORTA: Sipariş listesi onay bekliyor [Git →]
🔵 ORTA: 3 personelin eğitim durumu kontrol edilmeli [Git →]
✅ Şube operasyonları hazır
```
```

Bu ekleri promptun içine koyup öyle yapıştırın. Ana fikir: **layout seviyesinde ekleme** — her yönetici rolü her sayfada guidance görür, tek tek sayfa düzenlemeye gerek kalmaz.