Merhaba, Ticket 8 (10 task) tamamlandı. Ama implementation sırasında subagent bir dosyayı bozmuştu (server/routes/operations — git'ten restore edildi). Şimdi tüm değişikliklerin sağlıklı olduğunu doğrulamam lazım. Kod değişikliği YAPMA, sadece kontrol et ve raporla.

1. DOSYA SAĞLIK KONTROLÜ:
Aşağıdaki dosyaların syntax hatası olmadığını, import'ların çalıştığını doğrula:
- server/routes/operations.ts
- client/src/pages/raporlar/*.tsx (tüm dosyalar)
- client/src/components/turkish-labels.ts
- client/src/pages/personel-profil.tsx
- client/src/pages/misafir-memnuniyeti.tsx
- client/src/pages/mali-yonetim.tsx
- client/src/pages/fabrika/performans.tsx
- client/src/pages/satinalma/trend-analizi.tsx

2. DERLEME KONTROLÜ:
Uygulamayı yeniden başlat ve console'da herhangi bir hata var mı kontrol et. Sadece PostCSS uyarısı kabul edilebilir, başka hata olmamalı.

3. TICKET 8 TASK DOĞRULAMA:
Her task'ın dosyada gerçekten mevcut olduğunu doğrula:
- T001: personel-profil.tsx'de 5sn timeout + "Henüz akademi verisi yok" mesajı + "Tekrar Dene" butonu var mı?
- T002: personel-profil.tsx'de padding/gap azaltması yapılmış mı?
- T003: raporlar tab'larında whitespace-nowrap + shrink-0 + auto-scroll var mı?
- T004: vardiya-planlama.tsx'de AI hata mesajları Türkçe parse ediliyor mu?
- T005: misafir-memnuniyeti'nde skor 0 iken "Henüz değerlendirme yok" gösteriliyor mu?
- T006: Performans bileşeninde totalTasks=0 && completed=0 iken "Henüz yeterli veri yok" gösteriliyor mu?
- T007: Ekipman chart'larında custom label renderer + zero-value filter var mı?
- T008: 8 chart component'te allowDecimals={false} var mı?
- T009: turkish-labels.ts'de formatTurkishDate() fonksiyonu var mı? mali-yonetim ve fabrika/performans'ta kullanılıyor mu?
- T010: "Diğer" → "Belirlenmemiş" rename + %50 info banner var mı?

4. DO NOT BREAK KONTROLÜ:
- Breadcrumb'lar hâlâ PATH_LABELS + toLocaleUpperCase('tr-TR') kullanıyor mu?
- Auth middleware tüm protected route'larda mevcut mu?
- Header'da search ve notification bell YOK (sadece bottom nav'da), doğrula
- Ekipman Yönetimi tek sayfa olarak duruyor mu (eski "Ekipman Servis" route'u geri gelmemiş)?
- Notification dedup aktif mi?

Sadece ham sonuçları döndür, yorum yapma. Her madde için TAMAM veya SORUN yaz.