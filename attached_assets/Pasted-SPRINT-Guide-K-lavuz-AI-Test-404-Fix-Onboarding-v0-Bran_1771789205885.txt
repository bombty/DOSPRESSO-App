SPRINT: Guide (Kılavuz) + AI Test 404 Fix + Onboarding v0 (Branch + Factory + HQ) — minimal, production-safe.

Context:
- App has “Kılavuz” section but it’s empty / shows “Kullanım kılavuzu yüklenemedi.”
- Admin > AI Settings “Bağlantıyı Test Et” returns 404 status code (no body) in UI.
- We need an onboarding system with mentor linkage, 8-week timeline checkpoints, and role upgrade onboarding.
- Roles include: Branch (Stajyer, Bar Buddy, Barista, Supervisor Buddy, Supervisor), Field (Coach, Trainer), HQ (Marketing, Purchasing, Accounting, QA & CX), Factory (Operator, QA, Supervisor), Exec (CGO, CEO).
- Keep changes minimal, reuse existing patterns (RBAC, audit logging, i18n keys, sidebar registry).

GOALS (Deliverables):
1) Fix AI Test 404:
   - Identify the exact endpoint the UI calls when clicking “Bağlantıyı Test Et”.
   - Ensure backend route exists in production, is mounted, RBAC-protected (admin only), and returns JSON body.
   - Align frontend to correct URL.
   - Add a lightweight /api/health check if missing.

2) Implement Kılavuz (Guide) properly (minimal but real):
   - Add DB table (schema.ts) for guide_docs:
     fields: id, title, slug, group(SOP|Onboarding|Playbook|Policy), audienceRoles[], audienceScopes[], contentType(markdown|pdf|link), content(markdown), fileUrl(optional), version, status(draft|published), createdBy, updatedBy, createdAt, updatedAt
   - RBAC:
     - Everyone can READ published docs filtered by their role/scope.
     - Admin can CRUD/publish.
     - Coach/Trainer/QC/Marketing can create drafts (optional) but publish requires admin (if too big, skip drafts and keep admin-only).
   - API:
     - GET /api/guide (list for current user; filter by role/scope)
     - GET /api/guide/:slug (detail)
     - Admin: POST/PUT/DELETE /api/admin/guide
   - Seed initial 6 docs (markdown):
     - Branch SOP: Hygiene & Dress Code
     - FIFO & Stock Basics
     - Waste/Fire Entry Rules
     - Customer Complaint Handling
     - Factory Safety Basics
     - Onboarding Overview (what it is)
   - Frontend:
     - Kılavuz page should show list + search + filters; open detail viewer; handle empty state (no docs) gracefully.

3) Onboarding v0 (pilot-first):
   - Add DB tables:
     a) onboarding_programs (role, scope, durationWeeks, title, version, status)
     b) onboarding_weeks (programId, weekNo, goals, tasksJson, quizJson, evidenceRequired)
     c) onboarding_instances (userId, mentorUserId, programId, startDate, status, currentWeek, riskFlagsJson, decision)
     d) onboarding_checkins (instanceId, weekNo, mentorNotes, score, flags, nextActions)
   - API (RBAC):
     - Supervisor/Coach/Trainer/Admin can assign onboarding to a user with mentor.
     - Mentor can submit weekly check-in.
     - Trainee can mark tasks + upload evidence links (or text) + take quiz (simple).
     - Coach/Trainer can view branch-wide onboarding health dashboards.
   - UI (minimal screens):
     - Trainee: “My Onboarding” timeline (Weeks 1–8) + tasks + submit evidence.
     - Mentor: “My Mentees” list + weekly check-in form.
     - Coach/Trainer: onboarding overview (late check-ins, red flags, completion rates).
   - Seed at least 2 programs:
     - Branch: Stajyer (8 weeks)
     - Factory: Operator (8 weeks)
   - Add “role upgrade onboarding” rule:
     - If user role changes (e.g., Bar Buddy -> Barista), allow assigning a shorter program (2–3 weeks) later; for now just support assigning another instance.

4) Safety & Quality:
   - Audit log: admin publish/permission changes + onboarding assignments + decisions.
   - No secrets in logs.
   - i18n: add keys for new UI labels in TR + EN (minimal).
   - Tests: manual steps documented.

MANUAL TESTS:
- AI:
  a) Admin click “Bağlantıyı Test Et” => 200 JSON, not 404.
- Guide:
  b) Open Kılavuz => list loads; open a seeded doc => detail renders.
  c) Non-admin sees only docs for their role/scope.
- Onboarding:
  d) Create a Stajyer user + assign mentor => trainee sees timeline; mentor can submit week check-in.
  e) Coach/Trainer sees branch onboarding health list.

Output:
- List files changed
- Any new endpoints and how to test with curl
- Confirm migrations applied and app builds/publishes