Net ve basit — tek dosya, tüm endpoint'ler hazır. Replit'e yapıştırın:

---

**"Aşama 2B: Fabrika Mission Control. Araştırman mükemmel — tüm endpoint'ler hazır, pattern Aşama 2A'dan aynen uygulanacak.**

## GÖREV: MissionControlFabrika.tsx

### Hedef
Fabrika rolleri için Mission Control varyantı. Mevcut fabrika/dashboard.tsx'e (729 satır) DOKUNMA — yeni bileşen oluştur. Detaylı tablolar ve Recharts grafikleri klasik dashboard'da kalacak — MC sadece özet KPI + durum kartları gösterecek.

### Kullanılacak endpoint'ler (hepsi hazır, yeni backend kodu YOK):
- `/api/factory/dashboard/stats` → activeWorkers, totalProduced, totalWaste, efficiency, stationProduction
- `/api/factory/stations` → 9 istasyon listesi
- `/api/factory/active-workers` → anlık oturum bazlı personel
- `/api/factory/qc/stats` → QC pending, passRate
- `/api/factory/quality-overview` → todayChecked, todayPassed, todayFailed, qualityRate
- `/api/factory/waste-dashboard-stats` → avgWastePercent, overToleranceCount, trend
- `/api/factory/stock-overview` → lowStockCount, totalRawMaterials, totalFinishedProducts

### Layout:
- Sol: Mr. Dobody paneli (DobodyPanel — Aşama 1'de oluşturuldu)
- Header: UnifiedHero veya basit selamlama + DashboardModeToggle
- KPI strip: 6 metrik — Bugün üretim, Verimlilik %, Aktif personel, QC bekleyen, Kalite oranı, Fire %
- İstasyon durumu: Basit kart grid — her istasyon: isim + bugünkü üretim + hedef + durum rengi
- Aktif personel: Kim hangi istasyonda çalışıyor — basit liste
- QC özet: Pending + passed + failed + pass rate (MissionControlHQ'daki QCStatusMini benzeri)
- Stok uyarı: lowStockCount > 0 ise amber uyarı kartı
- Fire/atık: avgWastePercent + overToleranceCount kartı

### Yeniden kullanılacak bileşenler:
- CompactStatsBar veya DashboardKpiStrip (KPI strip)
- DashboardAlertPills (uyarılar)
- DashboardModeToggle (toggle)
- Aşama 2A'daki MCKpiCard pattern'ı

### Yapılacak:
1. `client/src/components/mission-control/MissionControlFabrika.tsx` oluştur (~250-300 satır)
2. `DashboardRouter.tsx`'te factory placeholder'ı `MissionControlFabrika`'ya bağla
3. Mevcut fabrika/dashboard.tsx'e DOKUNMA

### Kurallar:
- Mevcut endpoint'ler, mevcut widget'lar — yeni backend kodu yok
- Dark mode uyumlu, mobilde responsive (tek kolon)
- Veri yokken crash etme — "Veri yükleniyor" veya "Henüz veri yok" göster
- ErrorBoundary ile sar

### Test:
1. Toggle classic → fabrika/dashboard.tsx AYNEN çalışıyor
2. Toggle mission-control → MissionControlFabrika render ediliyor
3. KPI strip 6 metrik gösteriyor
4. İstasyon kartları görünüyor (9 istasyon)
5. QC özet görünüyor
6. Mobilde responsive
7. E2E 26/26 PASS
8. Quality gate kontrolleri PASS

### Bittikten sonra:
1. Dosya + satır sayısı
2. Hangi endpoint'lerden veri çekiliyor
3. Toggle geçişi çalışıyor mu
4. E2E sonucu
5. Quality gate PASS mı

**Başla.**"

---

Yapıştırın. Bu bittikten sonra Aşama 2 tamamen tamamlanmış olacak (4 varyant: HQ + Supervisor + Stajyer + Fabrika). Sonra Aşama 3 (Widget Studio) veya başka öncelik — yine Replit'e danışacağız.