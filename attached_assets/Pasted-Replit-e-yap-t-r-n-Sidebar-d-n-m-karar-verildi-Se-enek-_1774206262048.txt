Replit'e yapıştırın:

---

**"Sidebar dönüşümü kararı verildi: Seçenek B — Daraltılabilir sidebar (50px ikon ↔ 200px ikon+yazı, toggle ile geçiş).**

**Önce teknik plan öner — değişiklik yapma:**

**Hedef davranış:**
- Desktop: Sidebar varsayılan 50px (şimdiki NavRail gibi, sadece ikonlar)
- Hamburger butonuna (veya sidebar üstündeki toggle'a) tıklayınca → 200px'e genişler (ikon + yazı + kategoriler)
- Tekrar tıklayınca → 50px'e daralır
- Sidebar durumu localStorage'da saklanır (kullanıcı tercihi hatırlanır)
- Bir menü öğesine tıklayınca sayfa değişir ama sidebar KAPANMAZ — yerinde kalır
- Aktif sayfa gold/sarı renk ile işaretlenir
- Sidebar genişken: kullanıcı avatarı + isim + rol en üstte, menü öğeleri kategorilere ayrılmış
- Sidebar darken: sadece ikonlar + avatar (şimdiki NavRail gibi)
- Mobilde (<768px): Sidebar tamamen gizli, mevcut hamburger overlay + bottom nav AYNEN kalır

**Mevcut yapı (senin araştırmandan biliyoruz):**
- `nav-rail.tsx` → şimdiki 50px ikon çubuğu (değiştirilecek/genişletilecek)
- `sidebar.tsx` (components/ui/) → 727 satırlık Shadcn sidebar — kullanılmıyor ama hazır
- `hamburger-menu.tsx` → overlay menü (mobilde kalacak, desktop'ta rolü değişecek)
- `app-header.tsx` → header (hamburger buton burada)
- `App.tsx` → layout yapısı (NavRail burada render ediliyor)
- `navigation-config.ts` → NavRail ikon listesi (rol bazlı)
- `bottom-nav.tsx` → mobil alt bar (dokunulmayacak)

**Sorularım:**

**1. Mevcut NavRail'i genişletmek mi yoksa Shadcn sidebar.tsx'i kullanmak mı daha mantıklı?**
- NavRail zaten 50px'te çalışıyor — genişletme eklemek daha az riskli olabilir
- Shadcn sidebar tam donanımlı ama hiç kullanılmamış — entegrasyon riski var

**2. Sidebar'daki menü öğeleri nereden gelecek?**
- NavRail'in navigation-config.ts'i (5-6 ikon, client-side)
- Yoksa hamburger menünün server-driven API'si (/api/me/menu, 38+ öğe)
- Önerim: server-driven menü kullanılsın — böylece sidebar ve hamburger aynı veriyi gösterir

**3. Etkilenecek dosyalar tam listesi?**

**4. Hamburger butonun yeni rolü ne olacak?**
- Desktop: Sidebar toggle (50px ↔ 200px)
- Mobil: Overlay menü açma (şimdiki gibi)

**5. Tahmini süre ve risk?**

**6. Breadcrumb ne olacak?**
- Sidebar genişken breadcrumb gereksiz — gizlenebilir
- Sidebar darken breadcrumb gösterilebilir
- Yoksa breadcrumb tamamen kalsın mı?

**Plan öner — adımları sırala — değişiklik yapma.**"

---

Yapıştırın. Replit teknik planı önersin — ondan sonra birlikte onaylayıp uygulayacağız.