Harika fikir! Bugün çok büyük değişiklikler yaptık — kapsamlı bir doğrulama şart. Şu promptu Replit'e yapıştırın:

---

```
## Full System Health Check — Post-Sprint Audit (19 Mart 2026)

### Context
Today we made major changes across the entire DOSPRESSO platform:
1. Module Feature Flag system (32 flags, admin UI, page protection, score integration)
2. Mobile Compactness Sprint (16+ pages, CompactKPIStrip, MobileFilterCollapse)
3. QuickTaskModal fix (supervisor task assignment)
4. HQ Academy/Tasks sidebar fix (trainer/coach routing)
5. Branch Recurring Tasks system (full 3-sprint: backend + UI + score)
6. Academy HQ complete overhaul (2300-line monolith → 10 modular files, 7 tabs)

This audit checks for: critical errors, broken pages, UI/UX issues, missing integrations, dead code, and regressions.

Read these skills first:
- .agents/skills/dospresso-quality-gate/SKILL.md
- .agents/skills/dospresso-debug-guide/SKILL.md

### DO NOT MODIFY ANY CODE — this is READ-ONLY audit. Just report findings.

---

### SECTION 1: Server Health & Startup

```bash
# 1.1 Restart app and capture full startup log
# Report: any errors, warnings, seed counts, scheduler status

# 1.2 Check seed counts
echo "SELECT count(*) as module_flags FROM module_flags WHERE deleted_at IS NULL;" | psql
echo "SELECT count(*) as branch_task_categories FROM branch_task_categories;" | psql
echo "SELECT count(*) as branch_recurring_tasks FROM branch_recurring_tasks;" | psql
echo "SELECT count(*) as sla_rules FROM sla_rules;" | psql

# 1.3 Check for crashed/missing tables
echo "SELECT table_name FROM information_schema.tables WHERE table_schema='public' ORDER BY table_name;" | psql | grep -i "module_flag\|branch_recurring\|branch_task_instance\|branch_task_categor\|branch_recurring_task_override" 

# 1.4 Server error log scan
grep -i "error\|crash\|FATAL\|unhandled\|uncaught" /tmp/server.log 2>/dev/null | grep -v "node_modules\|deprecated" | tail -20
```

---

### SECTION 2: API Health — Test Critical Endpoints

Login as admin first, then test each category:

```bash
# 2.1 Module Flags
GET /api/module-flags → should return 32 flags
GET /api/module-flags/my-flags → should return { flags: {...} } with 32 keys
GET /api/module-flags/check?moduleKey=akademi&context=ui → should return { enabled: true }

# 2.2 Branch Tasks
GET /api/branch-tasks/categories → should return 4 categories
GET /api/branch-tasks/templates → should return templates
GET /api/branch-tasks/stats → should return stats object
GET /api/branch-tasks/score?branchId=5 → should return score object

# 2.3 Academy
GET /api/v3/academy/modules → should return modules array
GET /api/v3/academy/webinars → should return webinars array
GET /api/v3/academy/home-data → should return home data

# 2.4 Core system
GET /api/health → should return OK
GET /api/branches → should return branches list

# Report: status code + response structure for each. Flag any non-200 responses.
```

---

### SECTION 3: Page Load Verification — Every Major Page

Test that each page loads without crash (returns 200, no React error):

```bash
# 3.1 Auth pages
curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/login

# 3.2 Admin pages
# Login as admin, then check:
GET /admin → admin mega page loads
GET /akademi-hq → academy HQ loads (7 tabs)

# 3.3 Branch pages (login as barista/supervisor)
GET /gorevler → task page with 3 tabs
GET /ariza-yonetimi → equipment faults page
GET /vardiyalarim → shifts page
GET /devam-takibi → PDKS page

# 3.4 HQ pages (login as trainer/coach)
GET /akademi-hq → academy management (NOT student view)
GET /gorevler → task page visible

# 3.5 Dashboard pages
GET /hq-dashboard → HQ dashboard
GET /ceo-command-center → CEO dashboard
```

For each page, check browser console for JavaScript errors:
```bash
# Look for common React crash patterns in the build
grep -rn "Cannot read properties of undefined\|is not a function\|is not defined" /tmp/server.log 2>/dev/null | tail -10
```

---

### SECTION 4: Feature Flag System Verification

```bash
# 4.1 All 32 flags exist with correct behavior types
echo "SELECT module_key, flag_level, flag_behavior, parent_key, is_enabled FROM module_flags WHERE deleted_at IS NULL ORDER BY flag_level, module_key;" | psql

# 4.2 Verify always_on modules can't be disabled
# Check: admin, dashboard, bordro, dobody, fabrika, satinalma should all be always_on

# 4.3 Verify flag behavior types distribution
echo "SELECT flag_behavior, count(*) FROM module_flags WHERE deleted_at IS NULL GROUP BY flag_behavior;" | psql
# Expected: always_on=6, fully_hidden=23, ui_hidden_data_continues=3

# 4.4 Test page protection
# Disable akademi globally via API, then check /api/module-flags/my-flags shows akademi=false
# Re-enable immediately after test

# 4.5 Admin UI exists
# Check /admin page has "Modül Bayrakları" tab
grep -rn "module-flags\|Modül Bayrakları\|modul-bayraklari" client/src/pages/admin/ --include="*.tsx" | head -5
```

---

### SECTION 5: Branch Tasks System Verification

```bash
# 5.1 Tables exist and have data
echo "SELECT count(*) FROM branch_task_categories;" | psql
echo "SELECT count(*) FROM branch_recurring_tasks WHERE deleted_at IS NULL;" | psql
echo "SELECT count(*) FROM branch_task_instances;" | psql
echo "SELECT count(*) FROM branch_recurring_task_overrides WHERE deleted_at IS NULL;" | psql

# 5.2 Module flag exists
echo "SELECT module_key, is_enabled, flag_behavior FROM module_flags WHERE module_key='sube_gorevleri';" | psql

# 5.3 Score integration
echo "SELECT key FROM unnest(ARRAY['inspections','complaints','equipment','training','opsHygiene','customerSatisfaction','branchTasks']) as key;" | psql
# Verify branchTasks is a component in health scoring:
grep -n "branchTasks\|branch_tasks\|sube_gorev" server/services/branch-health-scoring.ts | head -10

# 5.4 Görevler page has 3 tabs
grep -n "Bana Atanan\|Şube Görevleri\|Tekrarlayan" client/src/pages/tasks.tsx | head -10

# 5.5 Dashboard widget exists
grep -rn "TodaysTasksWidget\|todays-tasks-widget\|Bugünün Görevleri" client/src/ --include="*.tsx" | head -5

# 5.6 Kiosk integration
grep -n "KioskBranchTasks\|branch-tasks.*kiosk\|kiosk.*branch.*task" client/src/pages/sube/kiosk.tsx | head -5

# 5.7 Scheduler registered
grep -n "generateDailyTaskInstances\|markOverdueInstances\|branch.*task.*schedule" server/index.ts server/scheduler-manager.ts 2>/dev/null | head -5
```

---

### SECTION 6: Academy HQ Verification

```bash
# 6.1 New modular structure exists
ls -la client/src/pages/akademi-hq/ 2>/dev/null
ls -la client/src/pages/akademi-hq/components/ 2>/dev/null

# 6.2 Old monolithic file deleted
ls -la client/src/pages/academy-hq.tsx 2>/dev/null
# Should NOT exist

# 6.3 All 7 tabs are defined
grep -n "TabsTrigger\|TabsContent" client/src/pages/akademi-hq/index.tsx | head -20

# 6.4 Route points to new directory
grep -n "akademi-hq\|AcademyHQ" client/src/App.tsx | head -5

# 6.5 No broken imports referencing old file
grep -rn "academy-hq\|from.*academy-hq" client/src/ --include="*.tsx" --include="*.ts" | grep -v "akademi-hq/" | grep -v node_modules | head -10

# 6.6 Trainer/Coach sidebar points to /akademi-hq
grep -n "training-academy-hq\|akademi-hq" server/menu-service.ts | head -5
```

---

### SECTION 7: Mobile Compactness Verification

```bash
# 7.1 CompactKPIStrip component exists
ls -la client/src/components/compact-kpi-strip.tsx 2>/dev/null

# 7.2 MobileFilterCollapse component exists
ls -la client/src/components/mobile-filter-collapse.tsx 2>/dev/null

# 7.3 CompactKPIStrip is used across pages
grep -rn "CompactKPIStrip" client/src/pages/ --include="*.tsx" -l | wc -l
# Expected: 13+ pages

# 7.4 MobileFilterCollapse is used
grep -rn "MobileFilterCollapse" client/src/pages/ --include="*.tsx" -l | wc -l
# Expected: 6+ pages

# 7.5 No desktop regression — check desktopGridClass/desktopRenderer usage
grep -rn "desktopGridClass\|desktopRenderer" client/src/pages/ --include="*.tsx" | wc -l
```

---

### SECTION 8: QuickTaskModal & Sidebar Fixes Verification

```bash
# 8.1 QuickTaskModal used in sube-ozet
grep -n "QuickTaskModal" client/src/pages/sube-ozet.tsx | head -3

# 8.2 QuickTaskModal used in kocluk-paneli
grep -n "QuickTaskModal" client/src/pages/kocluk-paneli.tsx | head -3

# 8.3 Trainer has tasks-list and training-academy-hq in sidebar
grep -A20 "trainer" server/menu-service.ts | grep "tasks-list\|training-academy-hq" | head -5

# 8.4 Coach has tasks-list and training-academy-hq in sidebar
grep -A20 "coach" server/menu-service.ts | grep "tasks-list\|training-academy-hq" | head -5
```

---

### SECTION 9: UI/UX Consistency Check

```bash
# 9.1 Dead code — AppSidebar still unused?
grep -rn "AppSidebar\|app-sidebar" client/src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | head -5

# 9.2 Dead code — PageHeader unused?
grep -rn "PageHeader\|page-header" client/src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | head -5

# 9.3 Header color consistency
grep -rn "#1e3a5f\|#17305c\|#122549" client/src/ --include="*.tsx" --include="*.css" | head -10

# 9.4 Logout endpoint consistency
grep -rn "/api/auth/logout\|/api/logout" client/src/ --include="*.tsx" --include="*.ts" | head -10

# 9.5 Hardcoded colors without dark mode variant
grep -rn "bg-white\|text-black\|border-gray-200" client/src/pages/ --include="*.tsx" | grep -v "dark:\|print:\|QR\|certificate" | wc -l

# 9.6 Unguarded .toFixed() calls
grep -rn "\.toFixed\|\.toLocaleString" client/src/pages/ --include="*.tsx" | grep -v "Number(\|?? 0\||| 0" | wc -l

# 9.7 Unguarded array methods on API data
grep -rn "\.map(\|\.filter(" client/src/pages/ --include="*.tsx" | grep "data\.\|result\." | grep -v "|| \[\]\|??\|Array.isArray" | wc -l
```

---

### SECTION 10: Full Quality Gate (15 points)

Run the complete 15-point quality gate from .agents/skills/dospresso-quality-gate/SKILL.md. Report PASS/FAIL for each:

1. Auth Middleware
2. Turkish UI
3. Null Safety
4. Drizzle Transactions
5. Data Lock
6. Soft Delete
7. Dark Mode
8. Role Access
9. Endpoint↔Table
10. TypeScript/React
11. Kiosk Auth
12. bcrypt Security
13. SLA Consistency
14. CRM Endpoint Auth
15. Kiosk Role Safety

---

### OUTPUT FORMAT

Produce a structured report:

```
═══════════════════════════════════════
DOSPRESSO Full System Audit — 19 Mar 2026
═══════════════════════════════════════

SECTION 1: Server Health
  1.1 Startup: PASS/FAIL — [details]
  1.2 Seed counts: module_flags=X, categories=X, templates=X, sla=X
  1.3 Tables: PASS/FAIL
  1.4 Error log: PASS/FAIL — [count] errors found

SECTION 2: API Health
  2.1 Module Flags: PASS/FAIL
  2.2 Branch Tasks: PASS/FAIL
  2.3 Academy: PASS/FAIL
  2.4 Core: PASS/FAIL

[...continue for all 10 sections...]

SECTION 10: Quality Gate
  Score: X/15 PASS

═══════════════════════════════════════
CRITICAL ISSUES (must fix before next sprint):
  1. [description]
  2. [description]

WARNINGS (should fix soon):
  1. [description]

MINOR (nice to have):
  1. [description]
═══════════════════════════════════════
```

Report EVERYTHING — do not skip any section.
```

---

Bu kapsamlı audit bugün yapılan tüm değişiklikleri doğrulayacak. Direkt build başlatın — sonucu buraya paylaşın, birlikte critical issue'ları değerlendiririz.