Aynı crash tekrar — `dispatcher.useState` hatası. Radix nested paket sorunu geri gelmiş. Replit'e yapıştır:

---

## TASK: EMERGENCY — App Crashed Again (Same Radix UI Issue)

Same error: `null is not an object (evaluating 'dispatcher.useState')`

This is the THIRD time. The cause is nested Radix UI packages in node_modules.

### Fix immediately:

```bash
# 1. Check for ALL nested packages under @radix-ui
find node_modules/@radix-ui -path "*/node_modules/*" -name "package.json" 2>/dev/null

# 2. Remove ALL nested packages
find node_modules/@radix-ui -mindepth 2 -name "node_modules" -type d -exec rm -rf {} + 2>/dev/null

# 3. Check which Radix packages have caret ^ versions
cat package.json | grep "@radix-ui" | grep "\^"

# 4. Remove ALL carets from ALL @radix-ui packages
sed -i 's/"\^/@radix-ui/g' package.json  
# Better approach - manually check and fix:
cat package.json | grep "@radix-ui"

# 5. Nuclear clean + reinstall
rm -rf node_modules/.vite
rm -rf node_modules/.cache
npm install

# 6. Verify NO nested packages exist
NESTED=$(find node_modules/@radix-ui -path "*/node_modules/*" -name "package.json" 2>/dev/null | wc -l)
echo "Nested packages: $NESTED"
# MUST be 0

# 7. If still nested, identify which package and downgrade it
find node_modules/@radix-ui -path "*/node_modules/*" -name "package.json" -exec grep -l "name" {} \; 2>/dev/null

# 8. Restart and verify app loads
npm run dev &
sleep 10
curl -s http://localhost:5000/ | head -5
```

### If npm install keeps creating nested packages:

```bash
# Add overrides to package.json to force compatible versions
# In package.json, add at root level:
"overrides": {
  "@radix-ui/react-use-controllable-state": "1.1.1",
  "@radix-ui/react-primitive": "2.0.3",
  "@radix-ui/react-presence": "1.1.3",
  "@radix-ui/react-compose-refs": "1.1.1",
  "@radix-ui/react-context": "1.1.1",
  "@radix-ui/react-id": "1.1.1",
  "@radix-ui/react-use-callback-ref": "1.1.1",
  "@radix-ui/react-use-layout-effect": "1.1.1"
}
```

Then:
```bash
rm -rf node_modules package-lock.json
npm install
find node_modules/@radix-ui -path "*/node_modules/*" -name "package.json" 2>/dev/null | wc -l
# MUST be 0
```

### After fix, run the build safety script:
```bash
bash scripts/check-build-safety.sh
```

### IMPORTANT: Do NOT add or upgrade ANY npm package without checking for nested Radix dependencies first.

---

Bu 3. kez aynı sorun. `overrides` field'ı package.json'a eklemek kalıcı çözüm — npm'i uyumlu versiyonları kullanmaya zorlar.