Sorun devam ediyor — Replit'in fix'i frontend'e ulaşmamış. Çok agresif bir diagnostic + fix prompt yazıyorum. Replit'e yapıştır:

---

## TASK: EMERGENCY — Sidebar Still Shows Full Menu for All Roles

The sidebar simplification is NOT working. After multiple fix attempts, ALL roles still see the full grouped menu (40+ items with accordion groups like "Operasyonlar", "Ekipman & Bakım", "Raporlar & Analitik", etc.).

**Evidence:** CGO (Utku) still sees: Akademi, Bilgi Bankası, Mutfak & Tarifler, Fabrika & Üretim (3 sub-items), Raporlar & Analitik (10 sub-items), Yeni Şube Açılışı (4 sub-items), Satınalma, Yönetim & Ayarlar (5 sub-items), İletişim Merkezi, Profilim. This is 30+ items. Should be 6 flat items.

### STEP 1: FIND THE ACTUAL SIDEBAR COMPONENT

```bash
# What file renders the sidebar that users see?
find client/src -name "*.tsx" | xargs grep -l "sidebar\|Sidebar\|SideBar" | head -20

# What component renders the menu items list?
find client/src -name "*.tsx" | xargs grep -l "menuItem\|MenuItem\|nav-item\|NavItem\|SidebarItem" | head -20

# What API endpoint provides menu data to the sidebar?
grep -rn "fetch.*menu\|useQuery.*menu\|/api/menu\|/api/me/menu\|/api/dashboard-module" client/src/ --include="*.tsx" --include="*.ts" | head -20
```

### STEP 2: TRACE THE DATA FLOW

```bash
# Where does the sidebar get its items from?
# Read the main sidebar/layout component
cat client/src/components/layout/app-sidebar.tsx 2>/dev/null | head -200
cat client/src/components/sidebar.tsx 2>/dev/null | head -200
cat client/src/components/app-sidebar.tsx 2>/dev/null | head -200

# If not found, search more broadly
find client/src -name "*sidebar*" -o -name "*Sidebar*" | head -10
find client/src -name "*layout*" | head -10
```

### STEP 3: FIND WHERE MENU DATA IS FETCHED

```bash
# Check all menu-related API calls
grep -rn "menu\|Menu" client/src/hooks/ --include="*.ts" --include="*.tsx" | head -20
grep -rn "menu\|Menu" client/src/lib/ --include="*.ts" --include="*.tsx" | head -20
grep -rn "/api/me/menu\|/api/menu\|/api/dashboard" client/src/ --include="*.ts" --include="*.tsx" | head -20
```

### STEP 4: CHECK SERVER-SIDE MENU BUILDING

```bash
# How does menu-service.ts build and filter the menu?
wc -l server/menu-service.ts
head -100 server/menu-service.ts
tail -100 server/menu-service.ts

# Find the function that returns menu for a specific role
grep -n "function\|export\|getMenu\|buildMenu\|filterMenu\|menuForRole" server/menu-service.ts | head -20

# Find where SIDEBAR_ALLOWED_ITEMS is used
grep -n "SIDEBAR_ALLOWED_ITEMS\|allowedItems" server/menu-service.ts | head -20

# Find the route that serves menu data
grep -rn "menu\|/me/menu\|dashboard-module" server/routes/ --include="*.ts" | head -20
```

### STEP 5: UNDERSTAND THE FULL PIPELINE

Report back:
1. What file is the sidebar component? (exact filename)
2. What API endpoint does it call to get menu items?
3. What server function builds the menu response?
4. Where in that function is SIDEBAR_ALLOWED_ITEMS applied?
5. Is the filtering actually running? (add a console.log to verify)
6. What does the API response look like for a barista user? (test with curl or by adding a log)

### STEP 6: IMPLEMENT THE FIX

Based on findings from steps 1-5, the fix must ensure:

**For roles with ≤6 allowed items (everyone except admin):**
- The API response contains ONLY the allowed items
- Items are returned as a FLAT array (no groups/sections/parents)
- The sidebar component renders them as simple links (no accordion, no collapsible groups)

**The fix must happen at the CORRECT point in the pipeline:**
- If the API returns grouped data and the frontend renders groups → fix the API to return flat data for roles with ≤6 items
- If the API returns flat data but the frontend adds groups → fix the frontend
- If the API filtering isn't running at all → fix the server route to call the filter function

**Add a debug log to verify the fix is working:**
```typescript
// In the menu API route handler, TEMPORARILY add:
console.log(`[MENU DEBUG] Role: ${user.role}, Items returned: ${items.length}, Items: ${items.map(i => i.key).join(', ')}`);
```

After confirming the log shows correct filtered items, remove the debug log.

### STEP 7: VERIFY

After implementing the fix:

1. Restart the server
2. Login as barista (cihan) — sidebar should show exactly 4 flat links:
   - Ana Sayfa (branch-dashboard)
   - Görevler (tasks-list)
   - Eğitim (training-academy)
   - Bildirimler (notifications)
   NO group headers, NO accordion, NO sub-items.

3. Login as CEO (ali) — sidebar should show exactly 5 flat links:
   - Dashboard
   - Raporlar
   - AI Asistan
   - Bildirimler
   - CRM

4. Login as admin — should still see the FULL grouped menu (admin is the exception)

5. Check the debug log output to confirm filtering is working:
```bash
grep "MENU DEBUG" /tmp/server.log 2>/dev/null || echo "Check server console output"
```

### IMPORTANT NOTES:
- Do NOT just change the SIDEBAR_ALLOWED_ITEMS arrays again — they are already correct from Sprint 20B
- The problem is that the filtering is NOT being applied somewhere in the pipeline
- Find WHERE the break is and fix THAT specific point
- The hamburger menu (mobile) and the desktop sidebar must BOTH be fixed
- After confirming the fix works, REMOVE the debug console.log

### DO NOT BREAK:
- Admin's full grouped sidebar
- Bottom navigation
- Direct URL access to any page
- Login redirects
- Any existing functionality

---

Bu prompt Replit'i adım adım zorluyor — önce teşhis, sonra fix. Yapıştır ve sonucu getir.