PLAN FIRST — do NOT start building until plan is shown.

Goal: Enhance existing HQ IK Excel Import/Export (already implemented) by adding missing gaps ONLY. Do not rebuild.

Safety rules (MUST):
- Default import mode MUST be UPSERT (create + update).
- NEVER delete or deactivate anyone by default.
- If a “DeactivateMissing” (remove people not in Excel) mode is implemented, it must:
  1) be OFF by default,
  2) require explicit typed confirmation string: DEACTIVATE,
  3) show impact summary (counts + who),
  4) hard-block ADMIN user/role/title from any change (cannot be deleted/deactivated/overwritten).
- Apply endpoint must run in a DB transaction.
- If there are validation errors and continueWithValid=false: do not apply anything.
- Every apply must write detailed audit logs (mode, actor, matchKey, counts, errors).

Implement these gaps incrementally (minimal UI, keep existing flows):
1) Export filters:
   - Add Title (ünvan) filter (uses titles table / user.titleId or equivalent).
   - Add frontend date-range pickers for hireDateFrom / hireDateTo (backend already supports).
   - Ensure filters work for scope: HQ / Factory / Branch and by branch selection.

2) Export content:
   - Add separate XLSX sheets:
     a) Payroll (salary-related fields)
     b) Documents/Özlük (document metadata columns; can be empty if no data yet)
   - Keep existing main sheet intact.

3) Import options:
   - Add import modes: AddOnly, UpdateOnly, Upsert (default), DeactivateMissing (guarded).
   - Add matchKey option: employeeId OR username OR email (default username/email).
   - Dry-run must preview the first N rows and show column mapping result.

4) Import UX gaps:
   - Add file preview step (first rows table).
   - Add manual column mapping UI (allow user to map incoming columns to required fields if auto-map fails).
   - Provide downloadable error report after dry-run (CSV or XLSX).
   - Improve batch history view with mode/matchKey/counts/errors summary.

5) Auditing:
   - Expand audit/batch records to store: mode, matchKey, continueWithValid, totals (created/updated/skipped/errors), and top error samples.
   - Ensure admin protection is enforced at both dry-run and apply.

Deliverables:
- Exact files changed
- Manual test steps:
  1) Export with scope filters + title + date range
  2) Import dry-run with mapping + error report download
  3) Import apply in Upsert
  4) UpdateOnly mode (no new creates)
  5) AddOnly mode (no updates)
  6) DeactivateMissing guarded flow (confirm string) and verify Admin is untouched
  7) Rollback a batch and verify changes revert
After plan appears, proceed to build.