Mükemmel — 3 kritik fix yapıldı:

1. ✅ **Mola fix** — on-break ekranı + süre sayacı + "Üretime Dön" butonu
2. ✅ **login-by-username crash** — avatarUrl → profileImageUrl
3. ✅ **Arıza bildir FK hatası** — userId='system' düzeltildi

Ama performans skorlama raporu (Step 15-18) sonucu gelmemiş — Replit mola fix'e odaklanmış. Performans skorlama analizi için şu kısa promptu yapıştırın:

---

```
## Factory Performance Scoring — Analysis Only (READ-ONLY)

Do NOT modify any code. Just analyze and report.

```bash
# 1. Is there a factory-specific scoring system?
grep -rn "factory.*score\|factory.*performance\|operator.*score\|production.*score" server/services/ --include="*.ts" | head -10

# 2. What does production_director AI skill measure?
head -80 server/agent/skills/production-director.ts

# 3. Employee summary for factory workers
FACTORY_BRANCH=$(echo "SELECT id FROM branches WHERE name LIKE '%Fabrika%' LIMIT 1;" | psql -t | tr -d ' ')
echo "Factory branch: $FACTORY_BRANCH"
curl -s -b "connect.sid=$(curl -s -c - -X POST http://localhost:5000/api/login -H 'Content-Type: application/json' -d '{"username":"admin","password":"'$ADMIN_BOOTSTRAP_PASSWORD'"}' | grep connect.sid | awk '{print $NF}')" "http://localhost:5000/api/employee-summary/branch/$FACTORY_BRANCH" | head -200

# 4. Production data per worker
echo "SELECT u.first_name, u.last_name, count(pr.id) as runs, COALESCE(sum(pr.quantity),0) as total_qty
FROM users u
LEFT JOIN factory_production_runs pr ON (pr.operator_id = u.id OR pr.created_by_user_id = u.id)
AND pr.started_at > NOW() - INTERVAL '30 days'
WHERE u.branch_id = $FACTORY_BRANCH AND u.is_active = true
GROUP BY u.first_name, u.last_name ORDER BY total_qty DESC;" | psql 2>/dev/null

# 5. Break stats per worker
echo "SELECT u.first_name, u.last_name, count(bl.id) as breaks, 
  COALESCE(avg(bl.duration_minutes),0) as avg_break_min
FROM users u
LEFT JOIN factory_break_logs bl ON bl.user_id = u.id AND bl.started_at > NOW() - INTERVAL '30 days'
WHERE u.branch_id = $FACTORY_BRANCH AND u.is_active = true
GROUP BY u.first_name, u.last_name ORDER BY breaks DESC;" | psql 2>/dev/null

# 6. Session durations per worker
echo "SELECT u.first_name, u.last_name, count(s.id) as sessions,
  COALESCE(avg(EXTRACT(EPOCH FROM (s.ended_at - s.started_at))/3600),0) as avg_hours
FROM users u
LEFT JOIN factory_kiosk_sessions s ON s.user_id = u.id AND s.started_at > NOW() - INTERVAL '30 days'
WHERE u.branch_id = $FACTORY_BRANCH AND u.is_active = true
GROUP BY u.first_name, u.last_name ORDER BY sessions DESC;" | psql 2>/dev/null

# 7. Factory dashboard stats
grep -n "dashboard.*stats\|stats\|summary\|performance" server/routes/factory.ts | head -15

# 8. Available factory tables and row counts  
for tbl in factory_production_runs factory_stations factory_products factory_kiosk_sessions factory_kiosk_session_events factory_break_logs factory_quality_records factory_daily_targets; do
  count=$(echo "SELECT count(*) FROM $tbl;" | psql -t 2>/dev/null | tr -d ' ')
  echo "$tbl: ${count:-MISSING}"
done

# 9. What factory-specific pages exist?
find client/src/pages/fabrika -name "*.tsx" | sort

# 10. Is there daily target vs actual tracking?
echo "SELECT * FROM factory_daily_targets ORDER BY target_date DESC LIMIT 5;" | psql 2>/dev/null || echo "No daily targets"
```

Report: What scoring/performance data EXISTS, what's MISSING, and what needs to be BUILT for a proper factory worker evaluation system.
```

---

Bu kısa rapor gelince tam resmi göreceğiz: fabrika skorlama var mı yok mu, ne eksik, ne inşa edilmeli. Sonucu paylaşın!