Çok değerli audit — 3 gerçek launch blocker bulundu. Hemen aksiyon planı:Kod tarafında çözülmesi gereken 4 iş var. Hepsini tek promptta yazıyorum. Replit'e yapıştırın:

---

```
## Pre-Launch Critical Fixes — Password Change + Notification Prefs + Responsive + Test Endpoint

### Context
Final audit found 4 code-level issues that must be fixed before launch (~26 March). These are small, focused fixes.

Read skills:
- .agents/skills/dospresso-architecture/SKILL.md
- .agents/skills/dospresso-debug-guide/SKILL.md

### CRITICAL RULES
- Turkish UI, users.id is VARCHAR, soft delete
- Do NOT touch server/vite.ts, vite.config.ts, package.json, drizzle.config.ts

---

## FIX 1: Self-Service Password Change (LAUNCH BLOCKER)

Users cannot change their own password. Only admin can reset via POST /api/employees/:id/reset-password.

### 1A: Create endpoint
```bash
# Check existing password-related endpoints
grep -rn "password\|reset-password\|change-password" server/routes/ --include="*.ts" | head -10
```

Add to appropriate route file (user profile or auth routes):
```typescript
// POST /api/me/change-password
router.post("/api/me/change-password", isAuthenticated, async (req, res) => {
  try {
    const user = req.user as any;
    const { currentPassword, newPassword } = req.body;
    
    if (!currentPassword || !newPassword) {
      return res.status(400).json({ error: "Mevcut şifre ve yeni şifre gereklidir" });
    }
    
    if (newPassword.length < 6) {
      return res.status(400).json({ error: "Yeni şifre en az 6 karakter olmalıdır" });
    }
    
    // Get current user with password
    const [dbUser] = await db.select().from(users)
      .where(eq(users.id, user.id));
    
    if (!dbUser) {
      return res.status(404).json({ error: "Kullanıcı bulunamadı" });
    }
    
    // Verify current password
    const bcrypt = require("bcrypt");
    const isValid = await bcrypt.compare(currentPassword, dbUser.hashedPassword);
    
    if (!isValid) {
      return res.status(401).json({ error: "Mevcut şifre yanlış" });
    }
    
    // Hash and update new password
    const hashedPassword = await bcrypt.hash(newPassword, 10);
    await db.update(users)
      .set({ hashedPassword, updatedAt: new Date() })
      .where(eq(users.id, user.id));
    
    res.json({ success: true, message: "Şifreniz başarıyla değiştirildi" });
  } catch (error) {
    console.error("[Password Change] Error:", error);
    res.status(500).json({ error: "Şifre değiştirme başarısız" });
  }
});
```

### 1B: Add UI to profile/settings page
```bash
# Find the profile or settings page
grep -rn "profil\|profile\|ayarlar\|settings\|hesabım\|account" client/src/pages/ --include="*.tsx" -l | head -10
grep -rn "me/settings\|profile\|hesap" client/src/App.tsx | head -5
```

Add a "Şifre Değiştir" section to the profile/settings page:
```typescript
// Password change form
const [currentPassword, setCurrentPassword] = useState("");
const [newPassword, setNewPassword] = useState("");
const [confirmPassword, setConfirmPassword] = useState("");

const changePasswordMutation = useMutation({
  mutationFn: async () => {
    if (newPassword !== confirmPassword) throw new Error("Şifreler eşleşmiyor");
    const res = await fetch("/api/me/change-password", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ currentPassword, newPassword }),
    });
    if (!res.ok) {
      const data = await res.json();
      throw new Error(data.error || "Şifre değiştirilemedi");
    }
    return res.json();
  },
  onSuccess: () => {
    toast({ title: "Şifre değiştirildi", description: "Yeni şifreniz aktif" });
    setCurrentPassword(""); setNewPassword(""); setConfirmPassword("");
  },
  onError: (err: Error) => {
    toast({ title: "Hata", description: err.message, variant: "destructive" });
  },
});
```

UI:
```tsx
<div className="border rounded-lg p-4 mt-4">
  <h3 className="text-sm font-medium mb-3">Şifre Değiştir</h3>
  <div className="space-y-3">
    <div>
      <label className="text-xs text-muted-foreground">Mevcut Şifre</label>
      <Input type="password" value={currentPassword} onChange={e => setCurrentPassword(e.target.value)} />
    </div>
    <div>
      <label className="text-xs text-muted-foreground">Yeni Şifre</label>
      <Input type="password" value={newPassword} onChange={e => setNewPassword(e.target.value)} />
    </div>
    <div>
      <label className="text-xs text-muted-foreground">Yeni Şifre (Tekrar)</label>
      <Input type="password" value={confirmPassword} onChange={e => setConfirmPassword(e.target.value)} />
    </div>
    <Button onClick={() => changePasswordMutation.mutate()} disabled={changePasswordMutation.isPending} size="sm">
      {changePasswordMutation.isPending ? "Değiştiriliyor..." : "Şifreyi Değiştir"}
    </Button>
  </div>
</div>
```

If NO profile/settings page exists, create a minimal one at `/profil`:
```bash
ls client/src/pages/profil*.tsx client/src/pages/profile*.tsx client/src/pages/hesab*.tsx client/src/pages/settings*.tsx 2>/dev/null
```

### 1C: Also add bulk password reset for admin (launch preparation)
```typescript
// POST /api/admin/bulk-reset-passwords
// Resets ALL users in specified branch to a default password
router.post("/api/admin/bulk-reset-passwords", isAuthenticated, async (req, res) => {
  const user = req.user as any;
  if (!["admin", "muhasebe_ik"].includes(user.role)) {
    return res.status(403).json({ error: "Yetkiniz yok" });
  }
  
  const { branchId, defaultPassword } = req.body;
  if (!defaultPassword || defaultPassword.length < 6) {
    return res.status(400).json({ error: "Varsayılan şifre en az 6 karakter olmalı" });
  }
  
  const bcrypt = require("bcrypt");
  const hashedPassword = await bcrypt.hash(defaultPassword, 10);
  
  let query = db.update(users).set({ hashedPassword, updatedAt: new Date() });
  if (branchId) {
    query = query.where(and(eq(users.branchId, branchId), eq(users.isActive, true)));
  } else {
    query = query.where(eq(users.isActive, true));
  }
  
  const result = await query.returning({ id: users.id });
  
  res.json({ 
    success: true, 
    message: `${result.length} kullanıcının şifresi sıfırlandı`,
    count: result.length 
  });
});
```

---

## FIX 2: Notification Preferences Table

```bash
# Check if table exists
echo "SELECT table_name FROM information_schema.tables WHERE table_name='notification_preferences';" | psql
grep -n "notificationPreferences\|notification_preferences" shared/schema.ts | head -5
```

If table doesn't exist in schema, add it:
```typescript
export const notificationPreferences = pgTable("notification_preferences", {
  id: serial("id").primaryKey(),
  userId: varchar("user_id").references(() => users.id).notNull(),
  category: varchar("category", { length: 50 }).notNull(), // task_overdue, agent_guidance, sla_breach, etc.
  enabled: boolean("enabled").default(true).notNull(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow(),
});
```

Create table via SQL and add basic CRUD:
```typescript
// GET /api/notification-preferences — get my preferences
// PATCH /api/notification-preferences — update a preference
```

Add UI to profile/settings page: toggle switches for each notification category.

---

## FIX 3: sube-ozet.tsx Responsive Breakpoints

```bash
# Check what sube-ozet looks like
head -50 client/src/pages/sube-ozet.tsx
grep -c "className" client/src/pages/sube-ozet.tsx
```

The page has 0 responsive breakpoints (md:, sm:, lg:). Add basic mobile responsiveness:
- Grid layouts: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
- Card widths: `w-full` on mobile
- Text sizes: responsive where needed
- Use CompactKPIStrip for KPI cards if not already

Focus only on making it USABLE on mobile — not pixel-perfect.

---

## FIX 4: Remove/Protect Test Endpoint

```bash
grep -rn "test-hq-login" server/ --include="*.ts" | head -5
```

Either:
A) Delete the endpoint entirely
B) Add admin-only protection:
```typescript
if (process.env.NODE_ENV === 'production') {
  // Don't register this endpoint in production
} else {
  app.get('/api/test-hq-login', ...);
}
```

Verify it's already wrapped in NODE_ENV check. If yes, just confirm. If not, wrap it.

---

## FIX 5 (BONUS): First Login Force Password Change

When a user logs in for the first time (or after admin reset), show a "Şifrenizi değiştirin" dialog:

```bash
# Check if users table has a 'mustChangePassword' or 'lastPasswordChange' column
grep -n "mustChange\|force.*password\|password.*change\|lastPassword" shared/schema.ts | head -5
```

If no such field exists, add a simple approach:
- When admin resets password, set a flag (or use a separate table)
- On login, check if flag is set → redirect to password change page
- After password change, clear the flag

This ensures users change their default password on first login.

---

## DO NOT TOUCH:
- server/vite.ts, vite.config.ts, package.json, drizzle.config.ts
- Working endpoints
- Module flags
- Agent/guidance system

## Testing:
1. POST /api/me/change-password with valid data → 200, password changed
2. POST /api/me/change-password with wrong current → 401
3. POST /api/admin/bulk-reset-passwords → resets all users in branch
4. Profile page shows "Şifre Değiştir" form
5. Notification preferences CRUD works
6. sube-ozet.tsx renders properly on mobile (320px viewport)
7. /api/test-hq-login → 404 in production mode
8. App starts without errors

## After:
1. Report which fixes were applied
2. Report if profile/settings page existed or was created
3. Report notification_preferences table status
```

---

Direkt yapıştırın! Bu 4-5 fix bitince launch blocker'lar çözülmüş olacak. Sonra sizin yapmanız gereken manuel işler:
- Lara personeli oluşturma
- Toplu şifre reset (yeni endpoint ile)
- Vardiya planı girme