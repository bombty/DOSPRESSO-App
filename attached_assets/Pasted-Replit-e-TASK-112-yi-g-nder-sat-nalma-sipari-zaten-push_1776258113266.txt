Replit’e TASK #112’yi gönder (satınalma sipariş — zaten push ettim). Sonra Replit Task #91 (Stok Merkezi) üzerinde devam etsin:

TASK #112: Satınalma Sipariş + Fiyat — Pull + Build + Test
Step 1: Pull + Build

git pull origin main
npm run build


Step 2: Test

npm run dev
COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"adminhq","password":"133200"}' | grep connect.sid | awk '{print $NF}')

# Inventory arama
curl -s -b "connect.sid=$COOKIE" "http://localhost:5000/api/inventory?search=un&category=hammadde" | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'{len(d)} results')"

# Sipariş oluşturma (items ile)
curl -s -b "connect.sid=$COOKIE" -X POST "http://localhost:5000/api/purchase-orders" -H "Content-Type: application/json" -d '{"supplierId":1,"status":"taslak","items":[{"inventoryId":1,"quantity":"10","unit":"KG","unitPrice":"34.08"}]}' | python3 -c "import sys,json; d=json.load(sys.stdin); print(json.dumps(d, indent=2))"


Step 3: Browser test (varsa)
Satınalma → Sipariş Yönetimi → “Yeni Sipariş” → Malzeme ara → Ekle → Miktar gir → Fiyat otomatik → Sipariş Oluştur
Report:

PULL+BUILD: ✅/❌
INVENTORY SEARCH: ✅/❌
ORDER WITH ITEMS: ✅/❌


Test bittikten sonra devam et — kiosk batch→stok düşme veya fire hesaplama hangisi uygunsa başla.

Ben kiosk batch→stok düşme otomasyonuna başlıyorum:​​​​​​​​​​​​​​​​

Batch→stok düşme karmaşık — mevcut endpoint’i bozmak riskli. Güvenli yaklaşım: batch tamamlandığında ayrı stok düşme endpoint’i:​​​​​​​​​​​​​​​​

İki ayrı üretim sistemi var (eski schema-10 batch + yeni schema-22 reçete). Bunları birleştirmek büyük iş. Şimdilik MRP üzerinden stok düşme API’si ekliyorum — batch otomasyonu sonraki sprint:​​​​​​​​​​​​​​​​

Push: 13e03cb0. Stok düşme API hazır (10. MRP endpoint). Fire hesaplama ekliyorum:​​​​​​​​​​​​​​​​

Push: 59e3e706. MRP artık 11 endpoint — stok düşme + fire hesaplama hazır.
Replit’e toplu test:

TASK #113: Stok Düşme + Fire + Satınalma — Pull + Build + Test
Step 1: Pull + Build

git pull origin main
npm run build


Step 2: Stok düşme test

npm run dev
COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"adminhq","password":"133200"}' | grep connect.sid | awk '{print $NF}')

# Stok düşme (Cinnabon reçetesi × 1 batch)
RECIPE_ID=$(curl -s -b "connect.sid=$COOKIE" "http://localhost:5000/api/factory/recipes?code=CIN-001" 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print(d[0]['id'] if d else '1')" 2>/dev/null || echo "1")
curl -s -b "connect.sid=$COOKIE" -X POST "http://localhost:5000/api/mrp/deduct-stock" -H "Content-Type: application/json" -d "{\"recipeId\":$RECIPE_ID,\"batchCount\":1}" | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Deducted: {d.get(\"deducted\",0)}, Skipped: {d.get(\"skipped\",0)}')"


Step 3: Fire hesaplama test

curl -s -b "connect.sid=$COOKIE" -X POST "http://localhost:5000/api/mrp/calculate-waste" -H "Content-Type: application/json" -d '{"date":"2026-04-15"}' | python3 -c "import sys,json; d=json.load(sys.stdin); print(json.dumps(d.get('summary',{}), indent=2))"


Step 4: Satınalma sipariş test

curl -s -b "connect.sid=$COOKIE" "http://localhost:5000/api/inventory?search=un&category=hammadde" | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'{len(d)} results')"


Report:

PULL+BUILD: ✅/❌
DEDUCT STOCK: ✅/❌
WASTE CALC: ✅/❌
INVENTORY SEARCH: ✅/❌


Bu oturumun nihai özeti:
468 tablo, 1725 endpoint, 314 sayfa, 31 rol, 27 QG, 50+ commit
Son eklenen 3 özellik:
	•	Satınalma sipariş listesi (malzeme seçimi + güncel fiyat)
	•	Üretim stok düşme API
	•	Fire hesaplama API
Devam mı?​​​​​​​​​​​​​​​​