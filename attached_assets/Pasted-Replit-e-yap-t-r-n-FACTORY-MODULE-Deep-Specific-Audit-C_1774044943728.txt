Replit'e yapıştırın:

---

```
## FACTORY MODULE — Deep Specific Audit

### Context
This is a FOCUSED audit on the factory module ONLY. We have the full system audit (384 tables, 1420+ endpoints). Now we need to verify the factory module works end-to-end from a REAL USER perspective.

Factory files:
- client/src/pages/fabrika/ (17 pages, kiosk.tsx = 2,129 lines)
- server/routes/factory.ts (7,044 lines, 124 endpoints)
- server/services/factory-scoring-service.ts (752 lines)
- Tables: factory_stations, factory_products, factory_shift_sessions, factory_production_runs, factory_production_outputs, factory_session_events, factory_break_logs, factory_worker_scores, factory_quality_checks, factory_daily_targets, factory_station_benchmarks, factory_management_scores, factory_kiosk_config, factory_waste_reasons, kiosk_sessions

This is 100% READ-ONLY. Do NOT modify any code.

---

## PART A: REAL USER JOURNEY SIMULATION

### A1: Factory Manager (Eren) — Web Login Experience
```bash
echo "═══ A1: FACTORY MANAGER WEB EXPERIENCE ═══"

# Login as Eren
EREN_COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d "{\"username\":\"eren\",\"password\":\"$ADMIN_BOOTSTRAP_PASSWORD\"}" 2>/dev/null | grep connect.sid | awk '{print $NF}')

if [ -z "$EREN_COOKIE" ]; then
  # Try with Dospresso2026!
  EREN_COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"eren","password":"Dospresso2026!"}' 2>/dev/null | grep connect.sid | awk '{print $NF}')
fi
echo "Eren cookie: ${EREN_COOKIE:0:20}..."

# What does Eren see in sidebar?
echo "--- Sidebar menu ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/me/menu 2>/dev/null | grep -o '"path":"[^"]*"' | head -20

# What role does Eren have?
echo "--- Eren's role ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/me 2>/dev/null | grep -o '"role":"[^"]*"'

# Factory dashboard data
echo "--- Factory dashboard ---"
curl -s -o /dev/null -w "status:%{http_code} time:%{time_total}s" -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/factory/dashboard/stats
echo ""

# Factory stations
echo "--- Stations ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/factory/stations 2>/dev/null | head -200

# Factory staff
echo "--- Staff ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/factory/staff 2>/dev/null | head -200

# Factory products
echo "--- Products (count) ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/factory/products 2>/dev/null | grep -c '"id"'

# Worker performance / scoring
echo "--- Worker scores ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/factory/analytics/workers 2>/dev/null | head -200

# Active workers right now
echo "--- Active workers ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/factory/kiosk/active-workers 2>/dev/null | head -100

# Guidance widget
echo "--- Guidance gaps ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/agent/guidance 2>/dev/null | head -200
```

### A2: Kiosk Device Boot → Personnel Screen
```bash
echo "═══ A2: KIOSK DEVICE BOOT ═══"

# Step 1: Device auth
echo "--- Device auth ---"
# Read what credentials device-auth expects
grep -B2 -A25 "device-auth" server/routes/factory.ts | head -30

# Check factory_kiosk_config table
echo "SELECT * FROM factory_kiosk_config;" | psql 2>/dev/null

# Try device auth
DEVICE_RESP=$(curl -s -X POST http://localhost:5000/api/factory/kiosk/device-auth \
  -H "Content-Type: application/json" \
  -d '{"username":"fabrika","password":"0000"}')
echo "Device auth response: $DEVICE_RESP"

# Does device-auth create a session or token?
echo "--- Does device-auth set any cookie or return token? ---"
curl -s -i -X POST http://localhost:5000/api/factory/kiosk/device-auth \
  -H "Content-Type: application/json" \
  -d '{"username":"fabrika","password":"0000"}' 2>/dev/null | grep -i "set-cookie\|token\|session"

# Step 2: After device auth, what can the kiosk access WITHOUT login?
echo "--- API access after device-auth (no login yet) ---"
# These should work for the kiosk to show staff list
curl -s -o /dev/null -w "staff: %{http_code}" http://localhost:5000/api/factory/staff
echo ""
curl -s -o /dev/null -w "stations: %{http_code}" http://localhost:5000/api/factory/stations
echo ""
curl -s -o /dev/null -w "active-workers: %{http_code}" http://localhost:5000/api/factory/kiosk/active-workers
echo ""
# Expected: 401 for all (no auth yet)
```

### A3: Worker Login (Arife — PIN)
```bash
echo "═══ A3: WORKER LOGIN ═══"

# Find Arife's actual PIN
echo "SELECT id, username, first_name, last_name, kiosk_pin FROM users WHERE username='arifeyildirim0';" | psql 2>/dev/null

# If PIN is hashed, check how login verifies it
grep -B5 -A30 "login-by-username" server/routes/factory.ts | head -40

# Login
LOGIN_RESP=$(curl -s -X POST http://localhost:5000/api/factory/kiosk/login-by-username \
  -H "Content-Type: application/json" \
  -d '{"username":"arifeyildirim0","pin":"0000"}')
echo "Login response: $(echo $LOGIN_RESP | head -c 500)"

# Extract token
KIOSK_TOKEN=$(echo $LOGIN_RESP | grep -oP '"kioskToken":"[^"]*"' | cut -d'"' -f4)
echo "Kiosk token: ${KIOSK_TOKEN:0:30}..."

# What does login return? (user data, active session, etc.)
echo "--- Login response structure ---"
echo $LOGIN_RESP | python3 -c "import sys,json; d=json.load(sys.stdin); print(json.dumps({k:type(v).__name__ for k,v in d.items()}, indent=2))" 2>/dev/null || echo "$LOGIN_RESP" | head -300

# Does login auto-start shift?
echo "--- Active session after login ---"
echo $LOGIN_RESP | grep -o '"activeSession":[^}]*}' | head -1

# Now test API access WITH kiosk token
echo "--- API access with kiosk token ---"
curl -s -o /dev/null -w "staff: %{http_code}" -H "x-kiosk-token: $KIOSK_TOKEN" http://localhost:5000/api/factory/staff
echo ""
curl -s -o /dev/null -w "stations: %{http_code}" -H "x-kiosk-token: $KIOSK_TOKEN" http://localhost:5000/api/factory/stations
echo ""
curl -s -o /dev/null -w "products: %{http_code}" -H "x-kiosk-token: $KIOSK_TOKEN" http://localhost:5000/api/factory/products
echo ""
curl -s -o /dev/null -w "today-stats: %{http_code}" -H "x-kiosk-token: $KIOSK_TOKEN" "http://localhost:5000/api/factory/kiosk/worker-today-stats"
echo ""
curl -s -o /dev/null -w "active-workers: %{http_code}" -H "x-kiosk-token: $KIOSK_TOKEN" http://localhost:5000/api/factory/kiosk/active-workers
echo ""
curl -s -o /dev/null -w "waste-reasons: %{http_code}" -H "x-kiosk-token: $KIOSK_TOKEN" http://localhost:5000/api/factory/kiosk/waste-reasons
echo ""
```

### A4: Start Shift → Station Selection → Working
```bash
echo "═══ A4: SHIFT + STATION + WORKING ═══"

# Start shift (stationId optional per redesign)
echo "--- Start shift ---"
SHIFT_RESP=$(curl -s -X POST -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  http://localhost:5000/api/factory/kiosk/start-shift \
  -d '{}')
echo "Start shift: $SHIFT_RESP"
SESSION_ID=$(echo $SHIFT_RESP | grep -oP '"sessionId":\d+' | cut -d: -f2)
echo "Session ID: $SESSION_ID"

# Assign station
echo "--- Assign station ---"
ASSIGN_RESP=$(curl -s -X POST -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  http://localhost:5000/api/factory/kiosk/assign-station \
  -d "{\"sessionId\":$SESSION_ID,\"stationId\":1}")
echo "Assign station: $ASSIGN_RESP"

# Phase transition: hazirlik → uretim
echo "--- Phase: hazirlik → uretim ---"
PHASE_RESP=$(curl -s -X PATCH -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  "http://localhost:5000/api/factory/kiosk/session/$SESSION_ID/phase" \
  -d '{"phase":"uretim"}')
echo "Phase transition: $PHASE_RESP"

# Phase transition: uretim → temizlik
echo "--- Phase: uretim → temizlik ---"
PHASE_RESP2=$(curl -s -X PATCH -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  "http://localhost:5000/api/factory/kiosk/session/$SESSION_ID/phase" \
  -d '{"phase":"temizlik"}')
echo "Phase transition: $PHASE_RESP2"
```

### A5: Break Flow (Mola)
```bash
echo "═══ A5: BREAK FLOW ═══"

# Log break
echo "--- Start break ---"
BREAK_RESP=$(curl -s -X POST -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  http://localhost:5000/api/factory/kiosk/log-break \
  -d "{\"eventType\":\"mola\",\"stationId\":1,\"sessionId\":$SESSION_ID}")
echo "Break response: $BREAK_RESP"
BREAK_ID=$(echo $BREAK_RESP | grep -oP '"breakLogId":\d+' | cut -d: -f2)

# End break
echo "--- End break ---"
if [ ! -z "$BREAK_ID" ]; then
  END_BREAK=$(curl -s -X POST -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
    http://localhost:5000/api/factory/kiosk/end-break \
    -d "{\"breakLogId\":$BREAK_ID}")
  echo "End break: $END_BREAK"
else
  echo "NO BREAK ID — break logging failed"
fi

# Check break record in DB
echo "--- Break log in DB ---"
echo "SELECT id, user_id, break_type, station_id, started_at, ended_at, duration_minutes FROM factory_break_logs ORDER BY id DESC LIMIT 3;" | psql 2>/dev/null
```

### A6: Production Entry
```bash
echo "═══ A6: PRODUCTION ENTRY ═══"

# Log production (mid-shift, not end-shift)
echo "--- Log production ---"
PROD_RESP=$(curl -s -X POST -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  http://localhost:5000/api/factory/kiosk/log-production \
  -d "{\"sessionId\":$SESSION_ID,\"stationId\":1,\"productId\":1,\"quantity\":100,\"wasteQuantity\":5}")
echo "Production log: $PROD_RESP"

# Check production output in DB
echo "--- Production output in DB ---"
echo "SELECT id, user_id, station_id, product_id, quantity, waste_quantity, created_at FROM factory_production_outputs ORDER BY id DESC LIMIT 3;" | psql 2>/dev/null
```

### A7: Fault Report
```bash
echo "═══ A7: FAULT REPORT ═══"

echo "--- Report fault ---"
FAULT_RESP=$(curl -s -X POST -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  http://localhost:5000/api/factory/kiosk/report-fault \
  -d '{"stationId":1,"description":"Test arıza bildirimi","severity":"medium"}')
echo "Fault report: $FAULT_RESP"
```

### A8: End Shift
```bash
echo "═══ A8: END SHIFT ═══"

echo "--- End shift ---"
END_RESP=$(curl -s -X POST -H "Content-Type: application/json" -H "x-kiosk-token: $KIOSK_TOKEN" \
  http://localhost:5000/api/factory/kiosk/end-shift \
  -d "{\"sessionId\":$SESSION_ID}")
echo "End shift: $END_RESP"

# Check shift session in DB
echo "--- Shift session in DB ---"
echo "SELECT id, user_id, station_id, status, started_at, ended_at FROM factory_shift_sessions ORDER BY id DESC LIMIT 3;" | psql 2>/dev/null

# Check PDKS record created
echo "--- PDKS records ---"
echo "SELECT id, user_id, record_type, source, created_at FROM pdks_records WHERE source='kiosk' ORDER BY id DESC LIMIT 5;" | psql 2>/dev/null
```

---

## PART B: STANDARD LOGIN → KIOSK REDIRECT

```bash
echo "═══ PART B: LOGIN REDIRECT ═══"

# What happens when "fabrika" user logs in via standard /login page?
echo "--- How does App.tsx route fabrika roles? ---"
grep -n "fabrika\|factory\|kiosk" client/src/App.tsx | head -15

# Is there a redirect based on role?
grep -n "redirect\|navigate.*fabrika\|role.*fabrika\|fabrika.*redirect" client/src/hooks/useAuth.tsx client/src/pages/login.tsx client/src/App.tsx 2>/dev/null | head -15

# What does kiosk.tsx do on mount when user has web session?
grep -B3 -A20 "useEffect" client/src/pages/fabrika/kiosk.tsx | grep -A20 "user\|session\|auth\|role\|standardLogin\|webSession" | head -30

# What step does kiosk start at?
grep -n "useState.*step\|initialStep\|defaultStep" client/src/pages/fabrika/kiosk.tsx | head -5

# Is there logic to skip device-password for web-session users?
grep -n "device-password\|skipDeviceAuth\|isStandardLogin\|webLogin" client/src/pages/fabrika/kiosk.tsx | head -10
```

---

## PART C: FRONTEND KIOSK FLOW ANALYSIS

```bash
echo "═══ PART C: FRONTEND FLOW ═══"

# Read the FULL kiosk state machine
echo "--- All steps and their line numbers ---"
grep -n "step === '" client/src/pages/fabrika/kiosk.tsx | head -20

# All setStep transitions (what triggers what)
echo "--- All step transitions ---"
grep -n "setStep(" client/src/pages/fabrika/kiosk.tsx | head -50

# For each step, what renders?
echo "--- Step → UI mapping ---"
for step in "device-password" "enter-credentials" "enter-pin" "select-user" "select-station" "worker-home" "working" "stop-options" "on-break" "production-entry" "log-production" "fault-report" "end-shift-summary" "auto-logout"; do
  lines=$(grep -n "step === '$step'" client/src/pages/fabrika/kiosk.tsx | head -1)
  buttons=$(sed -n "$(echo $lines | cut -d: -f1),+80p" client/src/pages/fabrika/kiosk.tsx 2>/dev/null | grep -c "Button\|button\|onClick")
  echo "  $step: $lines (buttons: $buttons)"
done

# Inactivity timer
echo "--- Inactivity timer ---"
grep -n "inactivity\|timeout\|autoReturn\|AUTO_RETURN\|INACTIVITY" client/src/pages/fabrika/kiosk.tsx | head -10

# What happens after each mutation's onSuccess?
echo "--- Mutation onSuccess handlers ---"
grep -B3 "onSuccess" client/src/pages/fabrika/kiosk.tsx | head -40

# resetWorker function
echo "--- resetWorker ---"
grep -B2 -A15 "function resetWorker\|const resetWorker" client/src/pages/fabrika/kiosk.tsx | head -20

# resetKiosk function
echo "--- resetKiosk ---"
grep -B2 -A10 "function resetKiosk\|const resetKiosk" client/src/pages/fabrika/kiosk.tsx | head -15
```

---

## PART D: FACTORY MANAGEMENT PAGES (non-kiosk)

```bash
echo "═══ PART D: MANAGEMENT PAGES ═══"

# List all factory pages and what they do
for f in $(find client/src/pages/fabrika -name "*.tsx" ! -name "kiosk.tsx" | sort); do
  echo ""
  echo "=== $(basename $f) ==="
  echo "Lines: $(wc -l < $f)"
  # Main purpose
  grep "export default\|export function" $f | head -1
  # API calls
  grep -c "useQuery\|useMutation" $f
  # Key features
  grep "TabsTrigger\|Chart\|Table\|Dialog\|Modal" $f | head -3
done

# Factory dashboard — what does it show?
echo ""
echo "=== DASHBOARD ANALYSIS ==="
grep -n "useQuery\|api/" client/src/pages/fabrika/dashboard.tsx 2>/dev/null | head -10
# Or find the actual dashboard file
find client/src/pages/fabrika -name "*dashboard*" -o -name "*index*" | head -5
```

---

## PART E: SCORING & ANALYTICS VERIFICATION

```bash
echo "═══ PART E: SCORING ═══"

# Current factory worker scores
echo "--- Worker scores ---"
echo "SELECT u.first_name, u.last_name, fws.production_score, fws.attendance_score, fws.quality_score, fws.break_score, fws.waste_score, fws.total_score 
FROM factory_worker_scores fws 
JOIN users u ON fws.user_id = u.id 
WHERE fws.total_score IS NOT NULL 
ORDER BY fws.total_score DESC;" | psql 2>/dev/null

# Employee summary for factory branch
FACTORY_BRANCH=$(echo "SELECT id FROM branches WHERE name LIKE '%Fabrika%' LIMIT 1;" | psql -t 2>/dev/null | tr -d ' ')
echo "--- Employee summary (factory branch $FACTORY_BRANCH) ---"
ADMIN_COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d "{\"username\":\"admin\",\"password\":\"$ADMIN_BOOTSTRAP_PASSWORD\"}" 2>/dev/null | grep connect.sid | awk '{print $NF}')
curl -s -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000/api/employee-summary/branch/$FACTORY_BRANCH" 2>/dev/null | head -300

# Analytics endpoints
echo "--- Analytics ---"
for ep in "analytics/stats" "analytics/workers" "analytics/stations" "analytics/daily-production" "analytics/station-performance" "analytics/worker-comparison" "analytics/waste"; do
  status=$(curl -s -o /dev/null -w "%{http_code}" -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000/api/factory/$ep" 2>/dev/null)
  echo "  /api/factory/$ep → $status"
done

# Station benchmarks
echo "--- Station benchmarks ---"
echo "SELECT id, station_id, target_hourly_output, benchmark_workers FROM factory_station_benchmarks;" | psql 2>/dev/null

# Daily targets
echo "--- Daily targets ---"
echo "SELECT target_date, product_id, target_quantity FROM factory_daily_targets ORDER BY target_date DESC LIMIT 5;" | psql 2>/dev/null
```

---

## PART F: CROSS-MODULE CONNECTIONS

```bash
echo "═══ PART F: CROSS-MODULE ═══"

# 1. Kiosk sessions — shared between factory and branch?
echo "--- Kiosk sessions ---"
echo "SELECT ks.token, ks.user_id, u.role, u.branch_id, b.name as branch, ks.expires_at 
FROM kiosk_sessions ks 
JOIN users u ON ks.user_id = u.id 
LEFT JOIN branches b ON u.branch_id = b.id 
WHERE ks.expires_at > NOW() 
ORDER BY ks.created_at DESC LIMIT 10;" | psql 2>/dev/null

# 2. Factory vs regular shifts
echo "--- Regular shifts for factory ---"
echo "SELECT count(*) as regular_shifts FROM shifts WHERE branch_id = $FACTORY_BRANCH;" | psql -t 2>/dev/null
echo "--- Factory shift sessions ---"
echo "SELECT count(*) as factory_shifts FROM factory_shift_sessions;" | psql -t 2>/dev/null

# 3. PDKS for factory workers
echo "--- Factory PDKS ---"
echo "SELECT p.record_type, p.source, count(*) FROM pdks_records p JOIN users u ON p.user_id = u.id WHERE u.branch_id = $FACTORY_BRANCH GROUP BY p.record_type, p.source;" | psql 2>/dev/null

# 4. Agent skills using factory data
echo "--- Production director skill ---"
head -30 server/agent/skills/production-director.ts

# 5. Guidance gaps for factory
echo "--- Factory guidance gaps ---"
curl -s -b "connect.sid=$EREN_COOKIE" http://localhost:5000/api/agent/guidance 2>/dev/null | grep -i "fabrika\|factory\|üretim\|istasyon" | head -10
```

---

## PART G: KNOWN ISSUES IN CURRENT CODE

```bash
echo "═══ PART G: ISSUES ═══"

# 1. req.user?.id in kiosk endpoints (should be kioskUserId)
echo "--- req.user in kiosk endpoints ---"
grep -n "isKioskAuthenticated" server/routes/factory.ts | cut -d: -f1 | while read line; do
  end=$((line + 40))
  issues=$(sed -n "${line},${end}p" server/routes/factory.ts | grep -n "req\.user\?\.\|req\.user\." | grep -v "kioskUserId" | head -3)
  if [ ! -z "$issues" ]; then
    echo "  ISSUE near factory.ts:$line — $issues"
  fi
done

# 2. Non-kioskFetch calls in kiosk.tsx
echo "--- Non-kioskFetch API calls ---"
grep -n "apiRequest\|fetch(" client/src/pages/fabrika/kiosk.tsx | grep -v "kioskFetch\|device-auth\|login\|auth/logout\|import\|//" | head -10

# 3. TypeScript errors
echo "--- TS errors in factory ---"
npx tsc --noEmit 2>&1 | grep -i "fabrika\|factory\|kiosk" | head -15

# 4. Stale data
echo "--- Stale sessions ---"
echo "SELECT count(*) as stale_shifts FROM factory_shift_sessions WHERE status='active' AND started_at < NOW() - INTERVAL '24 hours';" | psql -t 2>/dev/null
echo "SELECT count(*) as open_breaks FROM factory_break_logs WHERE ended_at IS NULL;" | psql -t 2>/dev/null
echo "SELECT count(*) as expired_kiosk FROM kiosk_sessions WHERE expires_at < NOW();" | psql -t 2>/dev/null

# 5. Console.log count
echo "--- Console.log ---"
grep -c "console.log" server/routes/factory.ts client/src/pages/fabrika/kiosk.tsx

# 6. Empty catch blocks
echo "--- Empty catches ---"
grep -c "catch {}\|catch (e) {}\|catch(e){}" server/routes/factory.ts client/src/pages/fabrika/kiosk.tsx

# 7. TODO/FIXME
echo "--- TODOs ---"
grep -n "TODO\|FIXME\|HACK\|TEMP" server/routes/factory.ts client/src/pages/fabrika/kiosk.tsx | head -10

# 8. Ownership checks
echo "--- Ownership patterns ---"
grep -n "kioskUserId.*!==\|!==.*kioskUserId\|session\.userId.*!==\|!==.*session\.userId" server/routes/factory.ts | head -10
```

---

## OUTPUT FORMAT

```
═══════════════════════════════════════════════════════════════
FACTORY MODULE — SPECIFIC AUDIT REPORT
═══════════════════════════════════════════════════════════════

PART A: User Journey
  A1 (Manager web):     Sidebar items, dashboard data, analytics
  A2 (Device boot):     Auth flow, what's accessible
  A3 (Worker login):    Token, session, data returned
  A4 (Shift/Station):   Start, assign, phases — each response
  A5 (Break):           Start/end, DB records
  A6 (Production):      Log entry, DB records  
  A7 (Fault):           Report, notification
  A8 (End shift):       Summary, PDKS record

PART B: Login Redirect
  Standard login → kiosk path: [how it works or doesn't]

PART C: Frontend Flow
  Step map: [all 11 steps with transitions]
  Inactivity timer: [exists? configured?]
  Reset functions: [what they clear]

PART D: Management Pages
  [list with purpose and status]

PART E: Scoring
  Workers scored: X with scores, Y without
  Analytics: [which work, which don't]

PART F: Cross-Module
  Shared tables: [list]
  PDKS integration: [status]
  Agent skills: [factory-related]

PART G: Issues
  CRITICAL: [numbered]
  HIGH: [numbered]  
  MEDIUM: [numbered]
  LOW: [numbered]

COMPLETE BUG LIST:
  1. [description + root cause + which file + which line]
  2. ...

FLOW ISSUES (UX/design problems):
  1. [description + expected vs actual]
  2. ...

RECOMMENDATION:
  [prioritized fix list]
═══════════════════════════════════════════════════════════════
```

Be EXHAUSTIVE. Test every API, check every flow, verify every connection. This is the FINAL audit before we fix everything in ONE prompt.
```

---

Bu rapor gelince fabrika modülünün **her köşesini** bileceğiz — çalışan, çalışmayan, kırık bağlantılar hepsi. Sonra tek bir mega fix promptu yazacağım. Yapıştırın!