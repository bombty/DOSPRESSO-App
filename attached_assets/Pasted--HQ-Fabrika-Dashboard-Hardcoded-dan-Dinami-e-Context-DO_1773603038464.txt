# HQ Fabrika Dashboard — Hardcoded'dan Dinamiğe

## Context
DOSPRESSO franchise platform. Stack: React 18 + TypeScript, TanStack Query v5, Drizzle ORM, Express.

Problem: The HQ fabrika overview card in hq-dashboard.tsx shows hardcoded data
because /api/hq-dashboard/fabrika returns static JSON without any DB queries.
The real factory data already exists in 7 dynamic endpoints in factory-routes.ts.

Goal: Replace the hardcoded /api/hq-dashboard/fabrika backend with real aggregated data
from existing factory endpoints. Do NOT touch fabrika/dashboard.tsx (already dynamic).

---

## TASK 1 — Fix Backend: /api/hq-dashboard/fabrika

**File:** `server/routes/hq-dashboard-routes.ts`
Find the existing `/fabrika` handler (around line 877) — it returns hardcoded JSON.

Replace it with real queries. First check what factory tables exist:
```bash
grep -n "factoryProduction\|productionLogs\|factoryStation\|wasteLog\|qualityCheck\|factoryStock" shared/schema.ts | grep "pgTable" | head -15
```

Then replace the hardcoded handler with real aggregate queries:

```typescript
router.get('/fabrika', requireAuth, requireRole(['admin','ceo','cgo']), async (req, res) => {
  try {
    const today = new Date();
    today.setHours(0, 0, 0, 0);

    // 1. Today's total production (kg or units)
    // Check: grep -n "productionLog\|production_log\|üretim" shared/schema.ts | grep "pgTable"
    // Use actual table/column names

    // 2. Today's waste percentage
    // Check waste table or waste columns in production logs

    // 3. Active station count
    // Check factory stations table

    // 4. Active worker count
    // Check factory worker/shift table

    // Build response with real data:
    res.json({
      metrics: [
        {
          title: 'Bugün Üretim',
          value: `${todayProduction ?? 0} kg`, // real value
          icon: 'Factory',
          trend: null,
        },
        {
          title: 'Fire Oranı',
          value: `${wastePercent ?? 0}%`,
          icon: 'AlertTriangle',
          trend: null,
          status: (wastePercent ?? 0) > 3 ? 'warning' : 'good',
        },
        {
          title: 'Aktif İstasyon',
          value: `${activeStations ?? 0}`,
          icon: 'Settings',
          trend: null,
        },
        {
          title: 'Aktif Personel',
          value: `${activeWorkers ?? 0}`,
          icon: 'Users',
          trend: null,
        },
      ]
    });
  } catch (err) {
    console.error('HQ fabrika dashboard error:', err);
    res.status(500).json({ error: 'Fabrika verisi alınamadı' });
  }
});
```

**Important:** 
- Run the grep commands first to find actual table and column names
- If a specific metric's table doesn't exist yet, return `0` or `null` — don't hardcode fake numbers
- Check how factory-routes.ts queries these tables and reuse the same patterns

---

## TASK 2 — Frontend: hq-dashboard.tsx FabrikaDashboard

**File:** `client/src/pages/hq-dashboard.tsx`
Find the FabrikaDashboard component (around lines 310-360).

The hardcoded fallbackMetrics should already work once the backend returns real data.
But verify:
- Does the component use `data?.metrics` from the API response?
- Does it fall back to hardcoded values when data is null?

If fallback values are always shown (not just when API fails), update to show '—':

```tsx
// Replace hardcoded fallback numbers with '—' placeholders:
const fallbackMetrics = [
  { title: 'Bugün Üretim', value: '—', icon: Factory, trend: null },
  { title: 'Fire Oranı',   value: '—', icon: AlertTriangle, trend: null },
  { title: 'Aktif İstasyon', value: '—', icon: Settings, trend: null },
  { title: 'Aktif Personel', value: '—', icon: Users, trend: null },
];
```

---

## Verification

- [ ] GET /api/hq-dashboard/fabrika returns real DB data (not hardcoded strings)
- [ ] Production value comes from actual production log table
- [ ] Waste percentage is calculated from real data
- [ ] Station/worker counts from real tables
- [ ] If no data exists yet, values show as 0 or null (not fake numbers)
- [ ] HQ dashboard fabrika card shows real values
- [ ] fabrika/dashboard.tsx untouched (still fully dynamic)
- [ ] TypeScript has no new errors
- [ ] App starts without errors