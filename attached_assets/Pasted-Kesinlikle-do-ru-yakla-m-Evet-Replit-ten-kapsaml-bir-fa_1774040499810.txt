Kesinlikle doğru yaklaşım. Evet — Replit'ten kapsamlı bir fabrika raporu isteyeceğim. Çünkü:

1. Birçok kez değişiklik yapıldı — mevcut durumu **tam** bilmiyorum
2. Her fix başka bir şeyi bozmuş olabilir
3. Benim haritam transcript'ten derlendi — **gerçek kodla** doğrulanmalı

Replit'e yapıştırın:

---

```
## COMPLETE FACTORY MODULE AUDIT — End-to-End Architecture Report

### Context
The factory module has been modified 10+ times across multiple sprints. Each fix may have broken something else. Before making ANY more changes, we need a COMPLETE picture of the ENTIRE factory system — every file, every table, every endpoint, every connection, every state.

This is 100% READ-ONLY. Do NOT modify any code. Just report.

---

## SECTION 1: ALL FACTORY FILES

```bash
echo "═══ SECTION 1: FILE INVENTORY ═══"

# Frontend files
echo "--- Frontend Pages ---"
find client/src/pages/fabrika -name "*.tsx" -o -name "*.ts" 2>/dev/null | sort
wc -l client/src/pages/fabrika/*.tsx 2>/dev/null

# Frontend components used by factory
echo "--- Factory-related components ---"
grep -rn "fabrika\|factory\|kiosk\|Factory\|Kiosk" client/src/components/ --include="*.tsx" -l 2>/dev/null | sort

# Backend route files
echo "--- Backend Routes ---"
wc -l server/routes/factory.ts
grep -c "router\.\(get\|post\|patch\|put\|delete\)" server/routes/factory.ts

# Backend services
echo "--- Factory Services ---"
find server/services -name "*factory*" -o -name "*scoring*" | sort
wc -l server/services/factory*.ts 2>/dev/null

# Agent skills related to factory
echo "--- Agent Skills ---"
grep -l "factory\|production\|fabrika" server/agent/skills/*.ts 2>/dev/null

# Schema tables
echo "--- Factory Tables in Schema ---"
grep -n "pgTable.*factory\|pgTable.*kiosk" shared/schema.ts | head -20
```

---

## SECTION 2: ALL FACTORY DATABASE TABLES

```bash
echo "═══ SECTION 2: DATABASE TABLES ═══"

# List all factory-related tables with row counts
for tbl in $(echo "SELECT table_name FROM information_schema.tables WHERE table_name LIKE 'factory%' OR table_name LIKE 'kiosk%' ORDER BY table_name;" | psql -t 2>/dev/null | tr -d ' '); do
  if [ ! -z "$tbl" ]; then
    count=$(echo "SELECT count(*) FROM $tbl;" | psql -t 2>/dev/null | tr -d ' ')
    cols=$(echo "SELECT count(*) FROM information_schema.columns WHERE table_name='$tbl';" | psql -t 2>/dev/null | tr -d ' ')
    echo "$tbl: $count rows, $cols columns"
  fi
done

# Show columns for each critical table
for tbl in factory_stations factory_products factory_shift_sessions factory_production_runs factory_production_outputs factory_break_logs factory_worker_scores factory_kiosk_sessions factory_kiosk_session_events factory_kiosk_config factory_quality_records factory_quality_checks factory_daily_targets factory_station_benchmarks factory_management_scores kiosk_sessions; do
  echo ""
  echo "=== $tbl ==="
  echo "SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name='$tbl' ORDER BY ordinal_position;" | psql 2>/dev/null || echo "TABLE DOES NOT EXIST"
done
```

---

## SECTION 3: ALL FACTORY API ENDPOINTS

```bash
echo "═══ SECTION 3: API ENDPOINTS ═══"

# Every single endpoint in factory.ts
grep -n "router\.\(get\|post\|patch\|put\|delete\)" server/routes/factory.ts | cat -n

# Which auth middleware does each use?
echo ""
echo "--- Auth per endpoint ---"
grep -n "isAuthenticated\|isKioskAuthenticated" server/routes/factory.ts | head -50

# Test each kiosk endpoint status
echo ""
echo "--- Kiosk endpoint test ---"
ADMIN_COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d "{\"username\":\"admin\",\"password\":\"$ADMIN_BOOTSTRAP_PASSWORD\"}" 2>/dev/null | grep connect.sid | awk '{print $NF}')

for ep in \
  "GET /api/factory/stations" \
  "GET /api/factory/products" \
  "GET /api/factory/staff" \
  "GET /api/factory/dashboard/stats" \
  "GET /api/factory/kiosk/active-workers" \
  "GET /api/factory/kiosk/waste-reasons" \
  "GET /api/factory/analytics/stats" \
  "GET /api/factory/analytics/workers" \
  "GET /api/factory/analytics/stations" \
  "GET /api/factory/scoring/worker/test"; do
  method=$(echo $ep | cut -d' ' -f1)
  url=$(echo $ep | cut -d' ' -f2)
  status=$(curl -s -o /dev/null -w "%{http_code}" -X $method -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000$url" 2>/dev/null)
  echo "$ep → $status"
done
```

---

## SECTION 4: KIOSK COMPLETE STATE MACHINE

```bash
echo "═══ SECTION 4: KIOSK STATE MACHINE ═══"

# All step types defined
grep -n "KioskStep\|type.*Step\|step ===" client/src/pages/fabrika/kiosk.tsx | head -30

# All useState declarations (state variables)
grep -n "useState" client/src/pages/fabrika/kiosk.tsx | head -40

# All useMutation declarations
grep -n "useMutation" client/src/pages/fabrika/kiosk.tsx | head -20

# All useQuery declarations
grep -n "useQuery" client/src/pages/fabrika/kiosk.tsx | head -20

# All useEffect hooks
grep -n "useEffect" client/src/pages/fabrika/kiosk.tsx | head -20

# All step transitions (setStep calls)
grep -n "setStep(" client/src/pages/fabrika/kiosk.tsx | head -40

# All button onClick handlers
grep -n "onClick" client/src/pages/fabrika/kiosk.tsx | head -40

# Total file size
wc -l client/src/pages/fabrika/kiosk.tsx
```

---

## SECTION 5: KIOSK FULL CODE READ

Read the ENTIRE kiosk.tsx in manageable chunks. Report the structure:

```bash
echo "═══ SECTION 5: KIOSK CODE STRUCTURE ═══"

# File size
LINES=$(wc -l < client/src/pages/fabrika/kiosk.tsx)
echo "Total lines: $LINES"

# Section 1: Imports and type definitions (first 50 lines)
echo "--- IMPORTS & TYPES (1-50) ---"
head -50 client/src/pages/fabrika/kiosk.tsx

# Section 2: Component start, state declarations
echo "--- STATE DECLARATIONS ---"
grep -n "const \[" client/src/pages/fabrika/kiosk.tsx | head -30

# Section 3: All handler functions
echo "--- HANDLER FUNCTIONS ---"
grep -n "function \|const .* = async\|const .* = (" client/src/pages/fabrika/kiosk.tsx | grep -v "import\|from\|useQuery\|useMutation\|useState\|useEffect" | head -30

# Section 4: JSX return — what renders at each step
echo "--- JSX RENDER SECTIONS ---"
grep -n "step === \|{step ==" client/src/pages/fabrika/kiosk.tsx | head -20

# Section 5: Which step renders which UI
for step in "device-password" "enter-credentials" "enter-pin" "select-user" "select-station" "worker-home" "working" "stop-options" "on-break" "production-entry" "log-production" "fault-report" "end-shift-summary" "auto-logout"; do
  line=$(grep -n "step === '$step'" client/src/pages/fabrika/kiosk.tsx | head -1 | cut -d: -f1)
  if [ ! -z "$line" ]; then
    echo "  $step → line $line"
  fi
done
```

---

## SECTION 6: AUTH CHAIN — HOW DOES KIOSK AUTH WORK?

```bash
echo "═══ SECTION 6: AUTH CHAIN ═══"

# 1. isKioskAuthenticated middleware — full code
echo "--- isKioskAuthenticated middleware ---"
grep -B2 -A30 "isKioskAuthenticated" server/localAuth.ts | head -40

# 2. isAuthenticated kiosk fallback — full code
echo "--- isAuthenticated kiosk fallback ---"
grep -B2 -A30 "kiosk.*token\|x-kiosk-token" server/localAuth.ts | head -50

# 3. createKioskSession — full code
echo "--- createKioskSession ---"
grep -B2 -A20 "async function createKioskSession\|export async function createKioskSession" server/localAuth.ts | head -25

# 4. validateKioskSession — full code
echo "--- validateKioskSession ---"
grep -B2 -A30 "async function validateKioskSession\|export async function validateKioskSession" server/localAuth.ts | head -35

# 5. What does req.kioskUserId contain after auth?
echo "--- req.kioskUserId assignment ---"
grep -n "kioskUserId" server/localAuth.ts | head -10

# 6. What does req.user contain after kiosk auth?
echo "--- req.user in kiosk auth ---"
grep -n "req.user" server/localAuth.ts | head -15

# 7. Frontend kioskFetch helper
echo "--- kioskFetch helper ---"
grep -B2 -A15 "function kioskFetch\|const kioskFetch" client/src/pages/fabrika/kiosk.tsx | head -20

# 8. How is kiosk token stored?
echo "--- token storage ---"
grep -n "localStorage.*kiosk\|factory-kiosk-token" client/src/pages/fabrika/kiosk.tsx | head -10

# 9. How does login store the token?
echo "--- login onSuccess ---"
grep -B5 -A20 "loginByUsername.*onSuccess\|loginMutation.*onSuccess" client/src/pages/fabrika/kiosk.tsx | head -30
```

---

## SECTION 7: FACTORY SCORING & ANALYTICS

```bash
echo "═══ SECTION 7: SCORING ═══"

# Scoring service
wc -l server/services/factory-scoring-service.ts 2>/dev/null
head -60 server/services/factory-scoring-service.ts 2>/dev/null

# Current scores in DB
echo "SELECT user_id, production_score, waste_score, quality_score, attendance_score, break_score, total_score FROM factory_worker_scores WHERE total_score IS NOT NULL LIMIT 10;" | psql 2>/dev/null

# Employee summary for factory
FACTORY_BRANCH=$(echo "SELECT id FROM branches WHERE name LIKE '%Fabrika%' LIMIT 1;" | psql -t 2>/dev/null | tr -d ' ')
curl -s -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000/api/employee-summary/branch/$FACTORY_BRANCH" 2>/dev/null | head -200

# Factory analytics endpoints
for ep in "analytics/stats" "analytics/workers" "analytics/stations" "analytics/daily-production"; do
  echo "--- /api/factory/$ep ---"
  curl -s -b "connect.sid=$ADMIN_COOKIE" "http://localhost:5000/api/factory/$ep" 2>/dev/null | head -100
done
```

---

## SECTION 8: FACTORY ↔ OTHER MODULES CONNECTIONS

```bash
echo "═══ SECTION 8: CROSS-MODULE CONNECTIONS ═══"

# Does factory use kiosk_sessions (shared with branch kiosk)?
echo "--- Shared kiosk_sessions usage ---"
grep -rn "kiosk_sessions\|kioskSessions" server/routes/factory.ts server/routes/branches.ts server/localAuth.ts --include="*.ts" | head -15

# Does factory affect employee-summary?
echo "--- Employee summary factory integration ---"
grep -n "factory\|fabrika\|isFactoryWorker" server/services/employee-summary-service.ts | head -10

# Does factory affect branch health score?
echo "--- Branch health factory ---"
grep -n "factory\|fabrika" server/services/branch-health-scoring.ts 2>/dev/null | head -5

# Does factory affect guidance/completeness?
echo "--- Guidance factory checks ---"
grep -n "factory\|fabrika" server/services/system-completeness-service.ts | head -10

# Agent skills that query factory data
echo "--- Agent skills using factory data ---"
grep -rn "factory\|production_runs\|factory_station" server/agent/skills/*.ts | head -15

# PDKS records for factory workers
echo "--- Factory PDKS ---"
echo "SELECT count(*) FROM pdks_records WHERE user_id IN (SELECT id FROM users WHERE branch_id = $FACTORY_BRANCH);" | psql -t 2>/dev/null

# Shifts for factory (regular shifts table vs factory_shift_sessions)
echo "--- Regular shifts for factory ---"
echo "SELECT count(*) FROM shifts WHERE branch_id = $FACTORY_BRANCH;" | psql -t 2>/dev/null
echo "--- Factory shift sessions ---"
echo "SELECT count(*) FROM factory_shift_sessions;" | psql -t 2>/dev/null
```

---

## SECTION 9: STANDARD LOGIN → KIOSK REDIRECT

```bash
echo "═══ SECTION 9: LOGIN REDIRECT ═══"

# How does standard login redirect to kiosk?
grep -n "fabrika\|kiosk\|redirect.*fabrika\|role.*fabrika" client/src/pages/login.tsx client/src/hooks/useAuth.tsx client/src/App.tsx 2>/dev/null | head -20

# What happens when "fabrika" user logs in via /login?
grep -n "fabrika_operator\|fabrika_kiosk\|fabrika_mudur\|role.*redirect" client/src/App.tsx client/src/hooks/ -r --include="*.tsx" | head -15

# Is there a useEffect in kiosk.tsx that detects web session?
grep -B5 -A15 "useEffect.*user\|useEffect.*auth\|useEffect.*session\|isStandardLogin\|webSession" client/src/pages/fabrika/kiosk.tsx | head -30
```

---

## SECTION 10: KNOWN ISSUES IN CURRENT CODE

```bash
echo "═══ SECTION 10: POTENTIAL ISSUES ═══"

# 1. Any TODO/FIXME/HACK comments
grep -n "TODO\|FIXME\|HACK\|TEMP\|WORKAROUND" client/src/pages/fabrika/kiosk.tsx server/routes/factory.ts | head -20

# 2. req.user?.id in kiosk endpoints (should be kioskUserId)
echo "--- req.user in kiosk endpoints ---"
grep -n "isKioskAuthenticated" server/routes/factory.ts | cut -d: -f1 | while read line; do
  end=$((line + 30))
  match=$(sed -n "${line},${end}p" server/routes/factory.ts | grep -n "req\.user\?\.id\|req\.user\.id" | head -1)
  if [ ! -z "$match" ]; then
    echo "  ISSUE at factory.ts:$line — uses req.user.id in kiosk endpoint"
  fi
done

# 3. Any fetch() calls without kioskFetch in kiosk.tsx
echo "--- Non-kioskFetch API calls ---"
grep -n "apiRequest\|fetch(" client/src/pages/fabrika/kiosk.tsx | grep -v "kioskFetch\|device-auth\|login\|login-by-username\|auth/logout\|import" | head -10

# 4. Ownership checks that compare kioskUserId with worker userId
echo "--- Ownership check patterns ---"
grep -n "kioskUserId.*!==\|!==.*kioskUserId\|ownership\|permission" server/routes/factory.ts | head -10

# 5. TypeScript errors in factory files
echo "--- TS errors ---"
npx tsc --noEmit 2>&1 | grep "fabrika\|factory\|kiosk" | head -15

# 6. Empty catch blocks
grep -n "catch {}\|catch (e) {}" client/src/pages/fabrika/kiosk.tsx server/routes/factory.ts | head -5

# 7. Console.log in production code
grep -c "console.log" server/routes/factory.ts client/src/pages/fabrika/kiosk.tsx

# 8. Stale/orphan sessions
echo "--- Stale data ---"
echo "SELECT count(*) as stale_shifts FROM factory_shift_sessions WHERE status='active' AND started_at < NOW() - INTERVAL '24 hours';" | psql -t 2>/dev/null
echo "SELECT count(*) as stale_kiosk FROM kiosk_sessions WHERE expires_at < NOW();" | psql -t 2>/dev/null
echo "SELECT count(*) as open_breaks FROM factory_break_logs WHERE ended_at IS NULL;" | psql -t 2>/dev/null
```

---

## SECTION 11: ALL FACTORY FRONTEND PAGES — WHAT THEY DO

```bash
echo "═══ SECTION 11: FRONTEND PAGES ═══"

for f in $(find client/src/pages/fabrika -name "*.tsx" | sort); do
  echo ""
  echo "=== $f ==="
  echo "Lines: $(wc -l < $f)"
  echo "Imports:"
  grep "from " $f | head -5
  echo "Main export:"
  grep "export default\|export function" $f | head -2
  echo "API calls:"
  grep "useQuery\|useMutation\|fetch\|apiRequest\|kioskFetch" $f | head -5
  echo "Key UI elements:"
  grep "TabsTrigger\|Button.*onClick\|<Card\|<Dialog" $f | head -5
done
```

---

## OUTPUT FORMAT

```
═══════════════════════════════════════════════════════════════
DOSPRESSO FACTORY MODULE — COMPLETE ARCHITECTURE REPORT
═══════════════════════════════════════════════════════════════

1. FILE INVENTORY:
   Frontend: X pages, Y lines total
   Backend: Z endpoints in factory.ts
   Services: [list]
   Agent skills: [list]

2. DATABASE:
   Tables: [list with row counts]
   Key relationships: [FK connections]

3. API HEALTH:
   Working: X endpoints
   Broken: Y endpoints (list each)
   Missing: Z endpoints

4. KIOSK STATE MACHINE:
   Steps: [ordered list]
   Transitions: [from → to with trigger]
   Current bugs in flow: [list]

5. AUTH CHAIN:
   Standard login → kiosk path: [description]
   Kiosk token flow: [stored? sent? validated?]
   req.user vs req.kioskUserId: [issues]

6. SCORING:
   Service exists: YES/NO
   Scores calculated: X workers
   Analytics working: YES/NO

7. CROSS-MODULE:
   Shared with branch kiosk: [what's shared]
   Employee summary integration: [status]
   Agent skills: [which ones use factory data]

8. CRITICAL BUGS:
   [numbered list with severity]

9. DESIGN ISSUES:
   [numbered list — flow problems, UX issues]

10. OPTIMIZATION OPPORTUNITIES:
    [numbered list]

11. RECOMMENDATION:
    [what should be fixed first, what can wait]

═══════════════════════════════════════════════════════════════
```

Be EXHAUSTIVE. Every file, every table, every endpoint, every connection. This report will be the SINGLE SOURCE OF TRUTH for the factory module.
```

---

Bu rapor gelince fabrika modülünün **tam röntgenini** göreceğiz — her dosya, her endpoint, her tablo, her bağlantı. Sonra tek seferde doğru fix promptunu yazarız. Yapıştırın!