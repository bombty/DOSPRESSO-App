# Sprint R-2 + R-3 + R-4 — Replit Test
## Commit: 0fd18860

```bash
git pull --rebase origin main
npm run build
```

## TEST 1: Cinnabon Seed (R-4)
```bash
curl -X POST http://localhost:5000/api/factory/seed-cinnabon \
  -H "Cookie: connect.sid=SESSION" -H "Content-Type: application/json"
```
Beklenen: recipeId, 15 malzeme, 10 adım, keyblendId

## TEST 2: Reçete Listesi
```bash
curl http://localhost:5000/api/factory/recipes -H "Cookie: connect.sid=SESSION"
```
Beklenen: Cinnabon reçetesi listede

## TEST 3: Reçete Detay
```bash
curl http://localhost:5000/api/factory/recipes/[ID] -H "Cookie: connect.sid=SESSION"
```
Beklenen: ingredients (15), steps (10), keyblendInfo (1)

## TEST 4: Batch Hesaplama
```bash
curl "http://localhost:5000/api/factory/recipes/[ID]/calculate?multiplier=2" \
  -H "Cookie: connect.sid=SESSION"
```
Beklenen: outputCount=130, ingredients scaled ×2

## TEST 5: Besin Değer Hesaplama (R-4)
```bash
curl -X POST http://localhost:5000/api/factory/recipes/[ID]/calculate-nutrition \
  -H "Cookie: connect.sid=SESSION" -H "Content-Type: application/json"
```
Beklenen: nutritionFacts (kcal, fat, carb...), allergens (gluten, süt, yumurta, soya), confidence >50%

## TEST 6: Keyblend Listesi
```bash
curl http://localhost:5000/api/factory/keyblends -H "Cookie: connect.sid=SESSION"
```
Beklenen: KB-CIN01 keyblend

## TEST 7: Keyblend Bileşenleri (admin)
```bash
curl http://localhost:5000/api/factory/keyblends/[KB_ID]/ingredients \
  -H "Cookie: connect.sid=SESSION"
```
Admin: 6 bileşen görür (DATEM, SSL, Soya...)
Sef: 403

## TEST 8: Frontend Sayfalar
- /fabrika/receteler → Liste (Cinnabon kartı görünmeli)
- /fabrika/receteler/[ID] → Detay (malzeme + adımlar + besin tablosu)
- /fabrika/receteler/[ID]/uretim → Üretim modu (batch seçim → adım adım)
- /fabrika/receteler/yeni → Yeni reçete formu
- /fabrika/keyblend-yonetimi → Keyblend yönetim paneli

## RAPOR
```
BUILD: ✅/❌
SEED: ✅/❌ (recipeId? ingredientCount? stepCount?)
RECIPES API: ✅/❌
BATCH CALC: ✅/❌ (130 adet?)
NUTRITION: ✅/❌ (kcal? allergens?)
KEYBLEND: ✅/❌ (admin görür, sef 403?)
FRONTEND: ✅/❌ (hangi sayfalar açılıyor?)
PUSH: hayır
```