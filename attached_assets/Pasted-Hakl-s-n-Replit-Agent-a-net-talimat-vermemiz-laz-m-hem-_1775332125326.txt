Haklısın. Replit Agent'a net talimat vermemiz lazım — hem Sprint B'yi çekmesi hem de neyi test edip neyi düzeltmesi gerektiğini. İşte kopyala-yapıştır mesajı:

---

**Replit Agent'a gönderilecek mesaj:**

```
Sprint B push edildi (commit bfab23a7). Yapman gerekenler:

1. GIT PULL:
   git pull --rebase origin main
   (ASLA git reset --hard yapma!)

2. SERVER RESTART:
   Server'ı durdur ve yeniden başlat.

3. TEST ET (sırasıyla):

   A) Şablon testi (Sprint A — önceden çalışıyordu, doğrula):
      - /denetim-sablonlari sayfasını aç
      - Mevcut şablonu kontrol et (kategoriler + sorular görünmeli)

   B) Denetim formu testi (Sprint B — YENİ):
      - Coach olarak giriş yap
      - /coach-sube-denetim sayfasını aç
      - "Yeni Denetim" tab'ında:
        → Şablon seç (Sprint A'da oluşturulan)
        → Şube seç
        → "Denetimi Başlat" butonuna bas
        → Form açılmalı (kategoriler + 7 tip soru)
        → Sorulara cevap ver (checkbox tıkla, yıldız ver, slider kaydır)
        → Sağ üstte skor canlı güncellensin
        → "Personel Denetimi →" butonuna bas
        → Şube personeli listesi gelmeli
        → Personel seç + 4 boyutta yıldız ver
        → "Denetimi Tamamla" butonuna bas
      - "Denetim Geçmişi" tab'ında kayıt görünmeli

   C) API test (hata olursa logları kontrol et):
      curl -s http://localhost:5000/api/v2/audit-templates | head -c 200
      curl -s http://localhost:5000/api/v2/audits | head -c 200

4. HATA BULURSAN:
   - Server loglarını paylaş
   - Hata mesajını tam yaz
   - Küçük fix ise (typo, missing import) kendin düzelt + push et
   - Büyük hata ise (mantık, yeni endpoint lazım) sadece raporla, Claude düzeltecek

5. PUSH ETME KURALI:
   - Sadece küçük hotfix'leri push et (typo, eksik import, SQL hata)
   - Yeni sayfa, yeni endpoint, büyük değişiklik YAPMA — Claude yapar
   - Push öncesi: npm run build test et, hata yoksa push et

6. YENİ ENDPOINT'LER (audit-v2.ts'e eklendi):
   - POST /api/v2/audits/:id/responses
   - POST /api/v2/audits/:id/personnel
   - PATCH /api/v2/audits/:id/complete
   - POST /api/v2/audits/:id/actions
   - PATCH /api/v2/audit-actions/:id
   - GET /api/v2/branch-on-shift/:branchId
```

---

Bu mesajı Replit'e yapıştır. Agent test sonuçlarını raporlayacak — sonra Sprint C'ye geçeriz.