Replit DB sorunu geçici altyapı kesintisi — bizim kodla ilgisi yok. Beklerken ben burada çalışmaya devam ediyorum.
Excel verilerini şimdi parse edip DB seed scripti hazırlıyorum — Replit DB geldiğinde direkt çalıştırır:​​​​​​​​​​​​​​​​

Seed data push edildi: bde2898f. Replit DB çözülünce import hazır.
Replit’i beklerken Replit’in task #93 raporuna güncelleme — son commit’leri de kapsayacak şekilde güncellenmiş Replit talimatı:

TASK #93 (GÜNCELLENMİŞ): 6 Commit Test — Pull + Build + Migration + RGM/İK/Versiyonlama
Son commit: bde2898f — Toplam 6 yeni commit (8defacff → bde2898f)
Step 1: Pull + Build

git pull origin main
npm run build


Step 2: DB Migration (6 kolon + 1 tablo + 1 index)

echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS market_price NUMERIC(10,2);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS market_price_updated_at TIMESTAMP;" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS material_type VARCHAR(10);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS purchase_unit VARCHAR(20);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS recipe_unit VARCHAR(20);" | psql $DATABASE_URL
echo "ALTER TABLE inventory ADD COLUMN IF NOT EXISTS conversion_factor NUMERIC(12,4);" | psql $DATABASE_URL
echo "CREATE INDEX IF NOT EXISTS inventory_material_type_idx ON inventory(material_type);" | psql $DATABASE_URL

echo "CREATE TABLE IF NOT EXISTS inventory_price_history (
  id SERIAL PRIMARY KEY,
  inventory_id INTEGER NOT NULL REFERENCES inventory(id) ON DELETE CASCADE,
  price_type VARCHAR(20) NOT NULL,
  price NUMERIC(12,2) NOT NULL,
  previous_price NUMERIC(12,2),
  change_percent NUMERIC(6,2),
  source VARCHAR(30) NOT NULL,
  source_reference_id INTEGER,
  effective_date DATE NOT NULL,
  notes TEXT,
  created_by VARCHAR REFERENCES users(id) ON DELETE SET NULL,
  created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL
);" | psql $DATABASE_URL
echo "CREATE INDEX IF NOT EXISTS iph_inventory_idx ON inventory_price_history(inventory_id);" | psql $DATABASE_URL
echo "CREATE INDEX IF NOT EXISTS iph_type_idx ON inventory_price_history(price_type);" | psql $DATABASE_URL
echo "CREATE INDEX IF NOT EXISTS iph_date_idx ON inventory_price_history(effective_date);" | psql $DATABASE_URL


Step 3: Server Start + Test

npm run dev


COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"adminhq","password":"TestPass123!"}' | grep connect.sid | awk '{print $NF}')

# İK sayfası crash fix
curl -s -b "connect.sid=$COOKIE" http://localhost:5000/ -o /dev/null -w "%{http_code}"

# Recipe API
curl -s -b "connect.sid=$COOKIE" "http://localhost:5000/api/factory/recipes" | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Recipes: {len(d)}')"

# Inventory price history API
curl -s -b "connect.sid=$COOKIE" "http://localhost:5000/api/inventory/price-history/1" -o /dev/null -w "%{http_code}"


Step 4: RGM Dashboard Test
Admin olarak → browser’da İlker/RGM hesabıyla giriş yap → Ana sayfa’da modül kartları görünmeli (Fabrika, Reçeteler, Stok, Raporlar). “0 modül” OLMAMALI.
Step 5: İK Fix Test
Admin olarak → İK & Personel’e tıkla → Building2 crash olmamalı.
Report:

PULL+BUILD: ✅/❌
MIGRATION: ✅/❌ (6 cols + 1 table + indexes)
SERVER: ✅/❌
IK FIX: ✅/❌ (no Building2 crash)
RGM DASHBOARD: ✅/❌ (module cards visible)
RECIPE API: ✅/❌
PRICE HISTORY API: ✅/❌


Replit DB geldiğinde bu talimatı gönder. Ben şimdi kalan işlere (depocu rolü, devir teslim güncelleme) devam ediyorum.​​​​​​​​​​​​​​​​