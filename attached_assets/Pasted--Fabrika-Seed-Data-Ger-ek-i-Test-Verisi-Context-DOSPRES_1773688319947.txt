# Fabrika Seed Data — Gerçekçi Test Verisi

## Context
DOSPRESSO franchise platform. Factory module needs realistic test data
to properly test all dashboard views, analytics, and kiosk flows.

Current issues:
- Product categories inconsistent (Turkish/English mix, case issues)
- Empty tables: factory_teams, factory_daily_targets, factory_worker_scores, factory_management_scores
- Only "sabah" shift exists — no öğle/akşam shifts
- 7 production runs only — not enough for trend charts

Do NOT touch: auth system, user accounts, CRM module.
Use db.execute() with .rows access pattern (not destructuring).

---

## TASK 1 — Fix Product Category Inconsistencies

**Problem:** Categories have Turkish/English/case inconsistencies.
Fix by standardizing all to Turkish, capitalized:

```sql
-- Standardize category names
UPDATE factory_products SET category = 'Donut' WHERE category = 'donut';
UPDATE factory_products SET category = 'Şurup' WHERE category IN ('sirup', 'syrup', 'Şuruplar');
UPDATE factory_products SET category = 'Cheesecake' WHERE category = 'cheesecake';
UPDATE factory_products SET category = 'Cookie' WHERE category = 'cookie';
UPDATE factory_products SET category = 'Brownie' WHERE category = 'brownie';
UPDATE factory_products SET category = 'Mambon' WHERE category = 'mamabon';
UPDATE factory_products SET category = 'Wrapitos' WHERE category = 'wrapitos';
UPDATE factory_products SET category = 'Cinnaboom' WHERE category = 'cinnaboom';
```

**Important:** First check actual category values before running:
```sql
SELECT DISTINCT category, COUNT(*) FROM factory_products GROUP BY category ORDER BY category;
```
Then apply standardization based on actual values found.

---

## TASK 2 — Add Missing Shift Types

**Problem:** Only "sabah" (06:00-14:00) shift exists. Need full day coverage.

```sql
-- Check existing shifts first:
SELECT * FROM factory_shifts;

-- Add missing shifts if they don't exist:
INSERT INTO factory_shifts (name, start_time, end_time, is_active)
SELECT * FROM (VALUES 
  ('Öğle Vardiyası', '14:00', '22:00', true),
  ('Gece Vardiyası', '22:00', '06:00', true)
) AS new_shifts(name, start_time, end_time, is_active)
WHERE NOT EXISTS (
  SELECT 1 FROM factory_shifts WHERE name IN ('Öğle Vardiyası', 'Gece Vardiyası')
);
```

**Check actual column names first:**
```bash
grep -n "factoryShifts\|factory_shifts" shared/schema.ts | head -10
```

---

## TASK 3 — Add Factory Teams

**Problem:** factory_teams table is empty. Teams are needed for shift planning.

```sql
-- Check table structure first:
-- grep -n "factoryTeams\|factory_teams" shared/schema.ts | head -10

-- Add 2 teams:
INSERT INTO factory_teams (name, description, shift_id, is_active)
SELECT * FROM (VALUES
  ('A Ekibi', 'Sabah vardiyası ekibi', 1, true),
  ('B Ekibi', 'Öğle vardiyası ekibi', 2, true)
) AS new_teams(name, description, shift_id, is_active)
WHERE NOT EXISTS (SELECT 1 FROM factory_teams LIMIT 1);
```

---

## TASK 4 — Add Daily Targets

**Problem:** factory_daily_targets is empty. Dashboard shows no targets.

```sql
-- Check table structure:
-- grep -n "factoryDailyTargets\|factory_daily_targets" shared/schema.ts | head -10

-- Add weekly targets for each station (next 7 days):
-- Use actual column names from schema
-- Typical structure: station_id, target_date, target_output, target_efficiency
```

For each of the 9 stations, insert realistic daily targets:
- Donut Hamur Hattı: 200 kg/day
- Konsantre Dolum: 150 units/day  
- Cheesecake: 80 units/day
- Mamabon: 100 units/day
- Wrapitos: 120 units/day
- Cookies: 150 units/day
- Donut Süsleme: 200 units/day
- Donut Paketleme: 200 units/day
- Cinnaboom: 90 units/day

---

## TASK 5 — Add Production Runs (Last 30 Days)

**Problem:** Only 7 production runs — not enough for trend charts.
Analytics need at least 30 days of data.

**Check actual table structure:**
```bash
grep -n "factoryProductionRuns\|factory_production_runs" shared/schema.ts | head -20
```

Add realistic production data for the last 30 days:

```typescript
// Generate 30 days of production data
const today = new Date();
const productIds = [1, 2, 3, 4, 5]; // use real product IDs from DB
const stationIds = [1, 2, 3, 4, 5, 6, 7, 8, 9]; // use real station IDs

for (let daysAgo = 30; daysAgo >= 1; daysAgo--) {
  const runDate = new Date(today);
  runDate.setDate(runDate.getDate() - daysAgo);
  
  // 2-3 production runs per day
  for (let run = 0; run < 2; run++) {
    const startTime = new Date(runDate);
    startTime.setHours(7 + run * 4, 0, 0, 0);
    const endTime = new Date(startTime);
    endTime.setHours(startTime.getHours() + 3 + Math.floor(Math.random() * 2));
    
    // Insert production run with realistic values
    // actual_output: 80-120% of target
    // waste_kg: 2-8% of output
    // efficiency: 75-98%
  }
}
```

Use actual column names from schema. Insert via db.execute() with .rows pattern.

**Minimum viable:** 30 production runs, 1 per day for last 30 days.

---

## TASK 6 — Add Worker Scores

**Problem:** factory_worker_scores is empty. Performance page shows no data.

For each factory user (7 users), add performance scores for the last 4 weeks:

```sql
-- Check table structure:
-- grep -n "factoryWorkerScores\|factory_worker_scores" shared/schema.ts | head -10

-- Insert weekly scores for each worker
-- Typical structure: user_id, week_start, efficiency_score, quality_score, attendance_score, composite_score
```

Realistic score ranges:
- Efficiency: 70-95
- Quality: 75-98
- Attendance: 80-100
- Composite: average of above

---

## TASK 7 — Add Management Scores

**Problem:** factory_management_scores is empty.

```sql
-- Check structure:
-- grep -n "factoryManagementScores\|factory_management_scores" shared/schema.ts | head -10

-- Add monthly management scores for factory_mudur users
-- For the last 3 months
```

---

## TASK 8 — Verify Dashboard After Seed

After seeding, test all 4 previously broken endpoints:

```bash
# These should all return 200 with real data now:
curl -s http://localhost:5000/api/factory/dashboard/stats | python3 -c "import sys,json; d=json.load(sys.stdin); print('stats OK:', list(d.keys())[:3])"
curl -s http://localhost:5000/api/factory/cost-dashboard-stats | python3 -c "import sys,json; d=json.load(sys.stdin); print('cost OK:', list(d.keys())[:3])"  
curl -s http://localhost:5000/api/factory/quality-overview | python3 -c "import sys,json; d=json.load(sys.stdin); print('quality OK:', list(d.keys())[:3])"
curl -s http://localhost:5000/api/factory/waste-dashboard-stats | python3 -c "import sys,json; d=json.load(sys.stdin); print('waste OK:', list(d.keys())[:3])"
```

Also verify analytics endpoints return trend data:
```bash
curl -s "http://localhost:5000/api/factory/analytics/daily-production?days=30" | python3 -c "import sys,json; d=json.load(sys.stdin); print('daily production rows:', len(d) if isinstance(d, list) else 'object')"
```

---

## IMPORTANT NOTES

1. **Check schema first** — before every INSERT, grep the schema for actual column names
2. **Use db.execute().rows** — not destructuring (bug was already fixed in Task #26)
3. **Don't duplicate** — check if data exists before inserting
4. **Realistic values** — use believable Turkish factory context (not random 999 numbers)
5. **Keep existing data** — don't delete existing production runs, stations, or products

---

## Verification Checklist

- [ ] Product categories consistent (all Turkish, capitalized)
- [ ] 3 shift types: Sabah, Öğle, Gece
- [ ] At least 2 factory teams created
- [ ] 30+ production runs in DB (last 30 days)
- [ ] factory_worker_scores has data for all 7 factory users
- [ ] factory_management_scores has data
- [ ] All 4 dashboard endpoints return 200 with real data
- [ ] Analytics trend charts have 30 days of data
- [ ] App starts without errors