Basri login olamıyorsa bu kritik — Replit'e yapıştır:

---

## TASK: EMERGENCY — Supervisor Cannot Login (Basri)

**Problem:** Barista Cihan can login (password: 0000) but Supervisor Basri CANNOT login. This blocks pilot.

### STEP 1: Diagnose Basri's account

```bash
# Find Basri's user record
psql -c "SELECT id, username, full_name, role, branch_id, is_active, locked_until, failed_login_attempts FROM users WHERE full_name ILIKE '%basri%' OR username ILIKE '%basri%';" 2>/dev/null
```

Check:
- `is_active` = true? (if false, account was deactivated)
- `locked_until` > now? (if yes, account is locked from too many failed attempts)
- `failed_login_attempts` > 5? (rate limit kicked in)
- `role` correct? (should be 'supervisor')
- `username` — what is the exact username?

### STEP 2: Check if account is locked

```bash
# If locked_until is set, clear it
psql -c "UPDATE users SET locked_until = NULL, failed_login_attempts = 0 WHERE full_name ILIKE '%basri%' RETURNING id, username, full_name, role, locked_until;"
```

### STEP 3: Check password

```bash
# Test login via API
curl -s -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"BASRI_USERNAME_HERE","password":"0000"}' | python3 -c "
import json,sys
d=json.load(sys.stdin)
if d.get('user'):
  print(f'SUCCESS: {d[\"user\"][\"fullName\"]} ({d[\"user\"][\"role\"]})')
else:
  print(f'FAIL: {d}')
"
```

If password 0000 doesn't work, try:
```bash
curl -s -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"BASRI_USERNAME_HERE","password":"dospresso123"}'

curl -s -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"BASRI_USERNAME_HERE","password":"Test1234!"}'
```

### STEP 4: If password is unknown, reset it

```bash
# Generate bcrypt hash for "0000"
node -e "const bcrypt=require('bcrypt'); bcrypt.hash('0000',10).then(h=>console.log(h));"

# Then update
psql -c "UPDATE users SET hashed_password='HASH_FROM_ABOVE', locked_until=NULL, failed_login_attempts=0 WHERE full_name ILIKE '%basri%' RETURNING id, username, full_name;"
```

### STEP 5: Check ALL supervisor/mudur accounts can login

```bash
# List all supervisors and their status
psql -c "SELECT id, username, full_name, role, branch_id, is_active, locked_until, failed_login_attempts FROM users WHERE role IN ('supervisor','mudur') AND is_active=true ORDER BY branch_id;" 2>/dev/null
```

### STEP 6: Also check for the "plugin error" issue

```bash
# Clear Vite cache
rm -rf node_modules/.vite

# Check build
npx vite build 2>&1 | grep -i "error\|fail" | head -5

# Check current SW version
grep "CACHE_VERSION\|cache-v" client/public/service-worker.js | head -1
```

### STEP 7: Test ALL critical role logins

```bash
USERS="adminhq:TestPass123! utku:0000 cihan:0000"

# Add Basri after fixing
for CRED in $USERS; do
  USER=$(echo $CRED | cut -d: -f1)
  PASS=$(echo $CRED | cut -d: -f2)
  RESULT=$(curl -s -X POST http://localhost:5000/api/login \
    -H "Content-Type: application/json" \
    -d "{\"username\":\"$USER\",\"password\":\"$PASS\"}" | python3 -c "import json,sys;d=json.load(sys.stdin);print('OK' if d.get('user') else f'FAIL: {d.get(\"error\",d)}')" 2>/dev/null)
  echo "$USER: $RESULT"
done
```

### Report:
1. What was wrong with Basri's account?
2. What is Basri's correct username and password?
3. How many other users have the same problem?
4. Are there any plugin/build errors?

---

Muhtemelen Basri'nin hesabı kilitlenmiş (çok fazla yanlış şifre denemesi) veya E2E test sırasında şifresi değiştirilmiş. Sonucu getir.