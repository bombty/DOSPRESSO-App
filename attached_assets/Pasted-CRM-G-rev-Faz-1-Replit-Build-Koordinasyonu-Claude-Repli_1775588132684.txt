CRM/Görev Faz 1 — Replit Build Koordinasyonu
Claude ↔ Replit İş Bölümü
7 Nisan 2026

ZATEN YAPILDILAR (Claude tarafından — TEKRAR YAPMA)
İşCommitDurumDobody branchPerf fixa17825e8✅ TAMAMLANDI — let branchPerf: any[] = [] eklendipayroll_deduction_config tablosu727ddfa1✅ Schema'da + API route'u hazır2026 tatil seed endpoint'i2e47102d✅ POST /api/admin/seed-holidays-2026FM 30dk eşik2e47102d✅ pdks-engine.ts güncellendiTatil mesai hesabı2e47102d✅ publicHolidays join eklendiYemek bedeli (stajyer)2e47102d✅ payroll-engine.ts'e eklendiPayroll Bridge (Motor birleştirme)0465448f✅ payroll-bridge.ts oluşturulduDuyuruStudioV2 D-R1db59bf0d✅ 8 modüler dosya, /duyuru-studio route
REPLİT'İN YAPMASI GEREKENLER
1. DB Migration'lar (ÖNCELİKLİ)
sql-- monthly_payroll'a 3 yeni kolon (henüz yapılmadı!)
ALTER TABLE monthly_payroll ADD COLUMN IF NOT EXISTS holiday_worked_days INTEGER NOT NULL DEFAULT 0;
ALTER TABLE monthly_payroll ADD COLUMN IF NOT EXISTS holiday_pay INTEGER NOT NULL DEFAULT 0;
ALTER TABLE monthly_payroll ADD COLUMN IF NOT EXISTS meal_allowance INTEGER NOT NULL DEFAULT 0;

-- payroll_deduction_config tablosu (eğer henüz yapılmadıysa kontrol et)
-- Daha önce yapıldıysa atla
2. 2026 Tatil Seed
bashcurl -X POST http://localhost:5000/api/admin/seed-holidays-2026 \
  -H "Cookie: connect.sid=SESSION" -H "Content-Type: application/json"
3. Faz 1 Build (Replit'in planına göre)
Task 1: Pilot öncesi veri temizliği

seed_test source'lu 93 task'ı soft-delete
branch_task_instances overdue kayıtları temizle
37+ duplikat beklemede görevleri arşivle

Task 2: Scheduler max instance limiti

branch-task-scheduler.ts → 3'ten fazla tamamlanmamış instance varsa oluşturma

Task 3: Dobody branchPerf fix → ZATEN YAPILDI, ATLA!
Task 4: Legacy ticket guard

hq-support-routes.ts POST'a deprecated uyarısı ekle

4. Faz 1 Build Sonrası Test
Build tamamlandıktan sonra:

npm run build hata vermemeli
Görev sayfaları temizlenmiş veriyle doğru göstermeli
Scheduler yeni instance oluşturma limiti çalışmalı


CLAUDE'UN YAPACAKLARI (Paralel)

✅ Faz 2 CRM mimari plan detaylandırma
✅ Şube Panel hatasını tespit ve fix
✅ Kiosk ayarlarında eksik şubeleri düzeltme
✅ CRM Task channel kaldırma hazırlığı


İLETİŞİM KURALI

Replit Faz 1'i bitirip push ettikten sonra Claude git pull --rebase
Claude kendi değişikliklerini push etmeden önce Replit'in push'unu bekler
Aynı dosyayı aynı anda değiştirmemeye dikkat