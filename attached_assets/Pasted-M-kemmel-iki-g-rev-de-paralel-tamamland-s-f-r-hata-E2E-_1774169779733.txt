Mükemmel — iki görev de paralel tamamlandı, sıfır hata:

**E2E Test: 26/26 PASS**
- Login, kiosk, QC, payroll, task CRUD, smoke test — hepsi geçti
- `npx tsx server/scripts/e2e-tests.ts` ile her zaman çalıştırılabilir

**Stub → Gerçek: 5/5 tamamlandı**
- Payroll parameters: stub silindi (zaten hr.ts'de çalışıyordu)
- Knowledge Base: 79 gerçek makale dönüyor
- Academy streaks/progress: gerçek veri
- Franchise performance: 20 şube, ort. 78 puan

**Düzeltilen uyumsuzluklar:**
- `grossSalary` → `baseSalaryGross` (field adı)
- `overallScore` → `dailyTotalScore` (kolon adı)

Replit'in önerdiği plan doğru ilerledi. Şimdi sıradaki Replit'in de önerdiği **Sıra 3: Production Readiness.** Replit'e yapıştırın:

---

**"E2E testler ve stub implementasyonlar tamamlandı — harika iş. Şimdi Sıra 3: Production Readiness.**

**Yine sana danışarak ilerliyoruz. Şu 6 maddeyi değerlendir ve hangi sırayla yapılması gerektiğini öner:**

1. **Health check zenginleştirme** — /api/health şu an basit 200 dönüyor. DB bağlantısı, SMTP erişilebilirliği, son backup zamanı, aktif session sayısı gibi detaylar eklenmeli mi? Mevcut health endpoint'i nasıl çalışıyor?

2. **Global error handler** — Backend'de uncaught exception handler var mı? Frontend'de React ErrorBoundary var mı? Şu an bir crash olduğunda ne oluyor?

3. **Rate limiting** — express-rate-limit veya benzeri. Hangi endpoint'lere uygulanmalı? Login'e ayrı limit, genel API'ye ayrı limit? Mevcut lockout mekanizması yeterli mi?

4. **CORS production config** — Şu anki CORS ayarları nasıl? Development'ta * mı? Production için ne yapılmalı?

5. **Graceful shutdown** — SIGTERM handler var mı? Mevcut request'ler tamamlanıyor mu yoksa anında kesiliyor mu?

6. **Request logging middleware** — Her request'in method, path, status, duration kaydı. Mevcut logging nasıl? Winston/Pino gerekli mi yoksa basit middleware yeterli mi?

**Ek olarak: .env.example dosyası oluşturulmalı — IONOS'a taşırken hangi environment variable'lar gerekiyor?**

**Önce mevcut durumu kontrol et:**
```bash
echo "=== Health endpoint ==="
cat server/routes/misc.ts | grep -A20 "health" | head -25
# veya health hangi dosyadaysa

echo "=== Error handling ==="
grep -rn "uncaughtException\|unhandledRejection\|ErrorBoundary" server/ client/src/ --include="*.ts" --include="*.tsx" | head -10

echo "=== Rate limiting ==="
grep -rn "rate.*limit\|rateLimit\|express-rate-limit" server/ --include="*.ts" | head -5
grep "rate-limit\|express-rate-limit" package.json

echo "=== CORS config ==="
grep -rn "cors\|CORS\|origin" server/index.ts | head -10

echo "=== Graceful shutdown ==="
grep -rn "SIGTERM\|SIGINT\|graceful\|shutdown" server/ --include="*.ts" | head -5

echo "=== Request logging ==="
grep -rn "morgan\|winston\|pino\|request.*log\|req.*log" server/ --include="*.ts" | head -10
grep "morgan\|winston\|pino" package.json

echo "=== Environment variables used ==="
grep -rn "process\.env\." server/ --include="*.ts" | grep -oP "process\.env\.\w+" | sort -u

echo "=== Mevcut .env dosyaları ==="
ls -la .env* 2>/dev/null
```

**Mevcut durumu raporla, sonra önerini paylaş. Hangisi en acil, hangisi hızlı yapılabilir, hangisi riskli? Sıralama sana ait.**

**Hiçbir değişiklik yapma — sadece araştır ve öner."**

---

Yapıştırın — Replit tekrar danışma modunda.