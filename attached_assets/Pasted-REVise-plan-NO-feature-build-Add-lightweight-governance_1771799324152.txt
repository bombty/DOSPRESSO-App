REVise plan (NO feature build). Add lightweight governance docs to prevent scope creep and enforce global consistency.

Important notes (must follow):
- Keep each doc max ~1 page (checklist + examples; avoid theory).
- RBAC doc must reference the existing code source of truth (e.g., shared/permissions.ts and current auth/guards). Docs must NOT contradict implementation.
- AI surface rules must be flexible enough to cover multiple AI surfaces (NBA, quiz analysis, photo verification, RAG/chat), using patterns not one-off rules.

Goals:
1) Prevent scope creep and random updates.
2) Make changes global and consistent across the app (avoid role-specific one-off fixes).
3) Ensure every sprint has acceptance criteria + manual test steps.

Tasks:
A) Add docs:
- docs/00-north-star.md (1 page): mission + 6 non-negotiables:
  (1) RBAC/policy-first, (2) tenant isolation, (3) single source of truth, (4) global consistency gate,
  (5) deterministic validation layer before writes, (6) observability/audit for critical flows.
- docs/01-scope-lock.md: pick max 3-5 items per week; only P0 hotfix outside scope; backlog rule.
- docs/02-definition-of-done.md: checklist required before merge/build:
  RBAC, audit, empty/loading states, i18n keys (no hardcode), seeds/migrations if needed,
  change-impact checklist (cross-module + cross-role), manual tests (min 3), backward compatibility (API additive).
- docs/03-rbac-policy.md: role→permissions matrix template + “deny by default”.
  Must reference shared/permissions.ts as implementation source of truth + note how to update without breaking other modules.
- docs/04-ai-surface-rules.md: patterns for AI visibility:
  NBA cards must show rationale + data used + confidence; critical AI actions require human approval.
  Apply same pattern across AI surfaces (NBA/quiz/photo/RAG/chat).

B) Update replit.md:
- Add a short “Workflow Rules” section linking to these docs.
- Add a line: “No feature build without DoD + acceptance criteria + manual test steps.”

Deliver:
- List files added/changed
- 5 manual checks: where to find docs, how to use Scope Lock + DoD before next build

Constraints:
- No UI changes
- No DB changes
- No new features