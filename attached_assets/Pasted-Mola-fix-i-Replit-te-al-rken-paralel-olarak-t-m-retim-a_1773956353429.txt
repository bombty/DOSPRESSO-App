Mola fix'i Replit'te çalışırken, paralel olarak **tüm üretim akışını simüle eden** bir test promptu gönderebiliriz. Mola fix bittikten sonra bunu yapıştırın:

---

```
## Factory Kiosk — Complete Production Workflow Simulation Test

### Context
We fixed the break/mola flow. Now we need to TEST EVERY SINGLE ACTION in the factory kiosk — simulate a real factory worker's FULL day. Click every button, try every flow, find every bug.

This is a TEST + REPORT task. Fix critical bugs found, report everything else.

Read skills:
- .agents/skills/dospresso-architecture/SKILL.md
- .agents/skills/dospresso-debug-guide/SKILL.md

---

## SIMULATION: A Full Factory Worker Day

You will simulate Arife Yıldırım (fabrika_operator) going through an entire work day. Test EVERY action at EVERY step.

### STEP 0: Setup — Get Auth Tokens
```bash
# Admin cookie for API testing
ADMIN_COOKIE=$(curl -s -c - -X POST http://localhost:5000/api/login -H "Content-Type: application/json" -d '{"username":"admin","password":"AdminPass123!"}' 2>/dev/null | grep connect.sid | awk '{print $NF}')

# Find Arife's user ID
echo "SELECT id, first_name, last_name, role, branch_id FROM users WHERE username='arifeyildirim0';" | psql

# Find factory branch ID
echo "SELECT id, name FROM branches WHERE name LIKE '%Fabrika%';" | psql

# Find factory kiosk config
echo "SELECT * FROM factory_kiosk_config LIMIT 5;" | psql 2>/dev/null || echo "No config table"
grep -n "devicePassword\|device_password\|KIOSK_PASSWORD" server/routes/factory.ts | head -5
```

---

### STEP 1: DEVICE AUTH — Simulate kiosk tablet boot
```bash
echo "═══ STEP 1: Device Auth ═══"

# What does device-auth expect?
grep -B2 -A20 "device-auth" server/routes/factory.ts | head -30

# Try device auth with various inputs
curl -s -X POST http://localhost:5000/api/factory/kiosk/device-auth \
  -H "Content-Type: application/json" \
  -d '{"username":"factory_kiosk","password":"1234"}' | python3 -c "import sys,json; print(json.dumps(json.load(sys.stdin), indent=2))" 2>/dev/null

# Try without credentials
curl -s -X POST http://localhost:5000/api/factory/kiosk/device-auth \
  -H "Content-Type: application/json" \
  -d '{}' | head -50

# What does success look like? What does failure look like?
# Does it create any session/token?
# EXPECTED: success=true, no token needed
```

**Report:**
- What credentials does device-auth accept?
- Does it create a session? Token?
- What happens with wrong password?

---

### STEP 2: PERSONNEL LIST — After device auth
```bash
echo "═══ STEP 2: Personnel List ═══"

# Frontend calls /api/factory/staff after device-auth
# But this needs isAuthenticated — will it work?
curl -s -o /dev/null -w "%{http_code}" http://localhost:5000/api/factory/staff
# Expected: 401 (no auth)

# With admin cookie (simulating web login path)
curl -s -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/staff | python3 -c "
import sys,json
d=json.load(sys.stdin)
if isinstance(d, list):
  print(f'{len(d)} staff members')
  for s in d[:3]:
    print(f'  {s.get(\"firstName\",\"?\")} {s.get(\"lastName\",\"?\")} — {s.get(\"role\",\"?\")}')
" 2>/dev/null

# CRITICAL: Does the kiosk flow use select-user (showing personnel list) 
# or enter-credentials (username+PIN only)?
grep -n "select-user\|enter-credentials\|staffList\|staffQuery" client/src/pages/fabrika/kiosk.tsx | head -10

# If enter-credentials: personnel list is NOT shown (GOOD — privacy fix applied)
# If select-user: personnel list IS shown (privacy concern)
```

**Report:**
- Does personnel list require web auth?
- Which flow is active: select-user or enter-credentials?
- Is the privacy fix (username+PIN only) working?

---

### STEP 3: PERSONNEL LOGIN — Username + PIN
```bash
echo "═══ STEP 3: Login ═══"

# Test login-by-username endpoint
curl -s -X POST http://localhost:5000/api/factory/kiosk/login-by-username \
  -H "Content-Type: application/json" \
  -d '{"username":"arifeyildirim0","pin":"1234"}' | head -100

# Test with wrong PIN
curl -s -X POST http://localhost:5000/api/factory/kiosk/login-by-username \
  -H "Content-Type: application/json" \
  -d '{"username":"arifeyildirim0","pin":"0000"}' | head -50

# Test with wrong username
curl -s -X POST http://localhost:5000/api/factory/kiosk/login-by-username \
  -H "Content-Type: application/json" \
  -d '{"username":"nonexistent","pin":"1234"}' | head -50

# Does login return a kiosk token?
# Does the frontend STORE and USE the token?
grep -n "kioskToken\|setKioskToken\|x-kiosk-token" client/src/pages/fabrika/kiosk.tsx | head -10

# What happens after successful login?
grep -n "onSuccess" client/src/pages/fabrika/kiosk.tsx | head -10
```

**Report:**
- Login works with correct credentials?
- What does wrong PIN return? (error message, status code)
- Is kiosk token stored and used?
- What's the next step after login?

---

### STEP 4: START SHIFT — Shift recording
```bash
echo "═══ STEP 4: Shift Start ═══"

# Is there a shift-start endpoint?
grep -n "start-shift\|shift.*start\|startShift\|begin.*shift" server/routes/factory.ts | head -10

# What does the frontend call after login?
grep -n "start-shift\|startShift\|shiftStart\|clock-in\|clockIn" client/src/pages/fabrika/kiosk.tsx | head -10

# Is shift tracking automatic or manual?
# Does logging in automatically start a shift?
grep -n "onSuccess.*login\|login.*onSuccess" client/src/pages/fabrika/kiosk.tsx | head -5
# Read the login onSuccess handler
grep -A20 "loginMutation.*onSuccess\|loginByUsername.*onSuccess" client/src/pages/fabrika/kiosk.tsx | head -25

# Test start-shift endpoint
curl -s -X POST -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/kiosk/start-shift \
  -H "Content-Type: application/json" \
  -d '{"stationId":1}' | head -50

# Check if shift/session records are created
echo "SELECT id, user_id, station_id, started_at, ended_at, status FROM factory_kiosk_sessions ORDER BY id DESC LIMIT 5;" | psql 2>/dev/null
```

**Report:**
- Is shift start automatic (on login) or manual (button click)?
- Does start-shift create a session record?
- What data is recorded?

---

### STEP 5: STATION SELECTION
```bash
echo "═══ STEP 5: Station Selection ═══"

# List all stations
curl -s -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/stations | python3 -c "
import sys,json
d=json.load(sys.stdin)
if isinstance(d, list):
  for s in d:
    print(f'  ID:{s.get(\"id\")} — {s.get(\"name\",\"?\")} — status:{s.get(\"status\",\"?\")} — active:{s.get(\"isActive\",\"?\")}')
" 2>/dev/null

# Can a station be occupied by only one worker?
# What happens if two workers select the same station?
grep -n "occupied\|busy\|inUse\|available" client/src/pages/fabrika/kiosk.tsx | head -10
grep -n "occupied\|busy\|available" server/routes/factory.ts | head -10

# Station selection API call
grep -n "selectStation\|select-station\|updateStation" client/src/pages/fabrika/kiosk.tsx | head -10

# What happens after station selection?
grep -A10 "selectStation\|setSelectedStation" client/src/pages/fabrika/kiosk.tsx | head -20
```

**Report:**
- How many stations? Names?
- Can multiple workers use same station?
- What API is called on station select?
- What's the next step?

---

### STEP 6: WORKING SCREEN — Production Phases
```bash
echo "═══ STEP 6: Working Screen ═══"

# What phases exist in the working step?
grep -n "hazirlik\|üretim\|uretim\|temizlik\|tamamlandi\|phase\|Phase\|setPhase\|currentPhase" client/src/pages/fabrika/kiosk.tsx | head -20

# What does each phase show?
grep -n "hazırlık\|Hazırlık\|Üretim\|Temizlik\|Tamamlandı" client/src/pages/fabrika/kiosk.tsx | head -20

# How does the user move between phases?
grep -n "setPhase\|nextPhase\|handlePhase" client/src/pages/fabrika/kiosk.tsx | head -10

# Is there a timer for each phase?
grep -n "timer\|Timer\|elapsed\|duration\|chrono\|stopwatch" client/src/pages/fabrika/kiosk.tsx | head -10

# What buttons are on the working screen?
grep -B2 "Button\|button" client/src/pages/fabrika/kiosk.tsx | grep -i "hazırlık\|üretim\|temizlik\|bitir\|başla\|tamamla\|stop\|ara ver" | head -15

# Phase transition API calls
grep -n "phase.*update\|update.*phase\|log.*phase" server/routes/factory.ts | head -10
```

**Report:**
- What phases exist? (hazırlık → üretim → temizlik → tamamlandı?)
- How long does each phase take?
- What buttons are available in each phase?
- Are phase transitions logged to DB?

---

### STEP 7: PRODUCTION ENTRY — Logging what was produced
```bash
echo "═══ STEP 7: Production Entry ═══"

# When does production-entry step appear?
grep -n "production-entry\|productionEntry" client/src/pages/fabrika/kiosk.tsx | head -15

# What fields does production entry have?
grep -n "productId\|quantity\|adet\|miktar\|ürün\|urun\|product" client/src/pages/fabrika/kiosk.tsx | grep -i "input\|select\|form\|value\|onChange" | head -10

# Product list API
curl -s -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/products | python3 -c "
import sys,json
d=json.load(sys.stdin)
if isinstance(d, list):
  print(f'{len(d)} products')
  for p in d[:5]:
    print(f'  ID:{p.get(\"id\")} — {p.get(\"name\",\"?\")}')
" 2>/dev/null

# Production run creation API
grep -n "POST.*production\|create.*run\|production.*create" server/routes/factory.ts | head -10

# What data is sent when saving production?
grep -n "productionMutation\|createProduction\|saveProduction\|logProduction" client/src/pages/fabrika/kiosk.tsx | head -10

# Test creating a production run
curl -s -X POST -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/production \
  -H "Content-Type: application/json" \
  -d '{"stationId":1,"productId":1,"quantity":100}' | head -50

# Recent production runs
echo "SELECT id, station_id, product_id, quantity, status, started_at FROM factory_production_runs ORDER BY id DESC LIMIT 5;" | psql
```

**Report:**
- What fields are in production entry form?
- How many products available?
- Does saving work? What's stored in DB?
- Any validation errors?

---

### STEP 8: STOP OPTIONS — All 6 Choices
```bash
echo "═══ STEP 8: Stop Options ═══"

# All stop options
grep -n "stop-options\|stopOption\|handleBreakReason\|breakReason" client/src/pages/fabrika/kiosk.tsx | head -15

# List all stop choices
grep -n "mola\|ozel_ihtiyac\|gorev_bitis\|vardiya_kapat\|yardim\|ariza_bildir" client/src/pages/fabrika/kiosk.tsx | head -20

# What does each choice trigger?
grep -A5 "handleBreakReasonSelect" client/src/pages/fabrika/kiosk.tsx | head -30

# Which choices auto-logout vs return to working? (after fix)
grep -n "auto-logout\|autoLogout\|resetWorker\|on-break\|onBreak" client/src/pages/fabrika/kiosk.tsx | head -15
```

**Report for EACH stop option:**
1. Mola → What happens? (should: on-break screen → return to working)
2. Özel İhtiyaç → What happens?
3. Görev Bitir → What happens? (should: production entry → logout)
4. Vardiya Kapat → What happens? (should: production entry → end shift → logout)
5. Yardım İste → What happens?
6. Arıza Bildir → What happens? (should: fault report → logout)

---

### STEP 9: BREAK FLOW (after fix)
```bash
echo "═══ STEP 9: Break Flow ═══"

# Is the on-break step implemented?
grep -n "on-break\|onBreak\|BreakTimer\|Üretime Dön\|returnFromBreak" client/src/pages/fabrika/kiosk.tsx | head -10

# Break log API
curl -s -X POST -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/kiosk/log-break \
  -H "Content-Type: application/json" \
  -d '{"eventType":"mola","stationId":1,"notes":"test break"}' | head -50

# Break end log
curl -s -X POST -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/kiosk/log-break \
  -H "Content-Type: application/json" \
  -d '{"eventType":"break_end","stationId":1,"notes":"test break end"}' | head -50

# Check break records in DB
echo "SELECT id, user_id, event_type, station_id, notes, created_at FROM factory_kiosk_session_events ORDER BY id DESC LIMIT 10;" | psql 2>/dev/null

echo "SELECT id, user_id, break_type, station_id, notes, started_at FROM factory_break_logs ORDER BY id DESC LIMIT 10;" | psql 2>/dev/null
```

**Report:**
- on-break screen exists?
- Break timer works?
- "Üretime Dön" button works?
- break_start and break_end both logged?

---

### STEP 10: FAULT REPORT FLOW
```bash
echo "═══ STEP 10: Fault Report ═══"

# Fault report step
grep -n "fault-report\|faultReport\|arıza\|ariza" client/src/pages/fabrika/kiosk.tsx | head -15

# What fields are in fault report form?
grep -n "faultDescription\|faultType\|equipment\|ekipman\|arıza açıklama" client/src/pages/fabrika/kiosk.tsx | head -10

# Fault report API
grep -n "fault\|ariza\|arıza" server/routes/factory.ts | head -10

# Test fault report
curl -s -X POST -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/kiosk/fault-report \
  -H "Content-Type: application/json" \
  -d '{"stationId":1,"description":"Test arıza","severity":"medium"}' | head -50

# Check fault records
echo "SELECT id, station_id, description, severity, status, created_at FROM factory_fault_reports ORDER BY id DESC LIMIT 5;" | psql 2>/dev/null
echo "SELECT id, equipment_id, description, severity, status, created_at FROM equipment_faults ORDER BY id DESC LIMIT 5;" | psql 2>/dev/null
```

**Report:**
- Fault report form works?
- What data is captured?
- Where is fault stored in DB?

---

### STEP 11: END SHIFT FLOW
```bash
echo "═══ STEP 11: End Shift ═══"

# End shift step
grep -n "end-shift\|endShift\|vardiya.*kapat\|shift.*end" client/src/pages/fabrika/kiosk.tsx | head -15

# End shift API
grep -n "end-shift\|endShift\|closeShift" server/routes/factory.ts | head -10

# End shift summary — what data is shown?
grep -n "end-shift-summary\|shiftSummary\|vardiya.*özet" client/src/pages/fabrika/kiosk.tsx | head -10

# Test end shift
curl -s -X POST -b "connect.sid=$ADMIN_COOKIE" http://localhost:5000/api/factory/kiosk/end-shift \
  -H "Content-Type: application/json" \
  -d '{"stationId":1}' | head -50

# Check session records
echo "SELECT id, user_id, started_at, ended_at, status FROM factory_kiosk_sessions ORDER BY id DESC LIMIT 5;" | psql 2>/dev/null
```

**Report:**
- End shift works?
- What summary is shown?
- Is the session properly closed in DB?

---

### STEP 12: EDGE CASES & ERROR HANDLING
```bash
echo "═══ STEP 12: Edge Cases ═══"

# What happens if user refreshes the page during production?
# Is state persisted or lost?
grep -n "localStorage\|sessionStorage\|persist\|saved.*state\|restore.*state" client/src/pages/fabrika/kiosk.tsx | head -10

# What happens if internet drops during a production log?
grep -n "offline\|retry\|error.*handler\|network.*error\|fetch.*catch" client/src/pages/fabrika/kiosk.tsx | head -10

# What happens if two users try to use same station?
# Is there a station lock mechanism?
grep -n "lock\|occupied\|inUse\|concurrent" server/routes/factory.ts | head -10

# What happens if kiosk session expires (8 hour TTL) during work?
grep -n "expire\|timeout\|ttl\|session.*expire" client/src/pages/fabrika/kiosk.tsx | head -10

# What if production-entry is submitted with 0 quantity?
grep -n "validation\|validate\|required\|min.*0\|quantity.*check" client/src/pages/fabrika/kiosk.tsx | head -10

# What if the worker forgets to end shift?
# Is there automatic shift end at midnight or after X hours?
grep -n "auto.*end\|midnight\|max.*hours\|timeout.*shift" server/routes/factory.ts | head -10
```

**Report:**
- Page refresh: state persisted or lost?
- Offline handling: any retry logic?
- Concurrent station access: locked?
- Session expiry during work: handled?
- Quantity validation: 0 allowed?
- Forgotten shift end: auto-closed?

---

### STEP 13: KIOSK TOKEN & AUTH CHAIN
```bash
echo "═══ STEP 13: Auth Chain ═══"

# After login-by-username, is kiosk token used?
grep -n "kioskToken\|kiosk-token\|x-kiosk-token\|setKioskToken" client/src/pages/fabrika/kiosk.tsx | head -10

# Which endpoints use isKioskAuthenticated vs isAuthenticated?
echo "--- isKioskAuthenticated endpoints ---"
grep -n "isKioskAuthenticated" server/routes/factory.ts | head -20

echo "--- isAuthenticated endpoints ---"
grep -n "isAuthenticated[^K]" server/routes/factory.ts | head -20

# If user logged in via standard login (web session), do all kiosk endpoints work?
# Test key endpoints with admin web session
for ep in "/api/factory/kiosk/log-break" "/api/factory/kiosk/start-shift" "/api/factory/kiosk/end-shift"; do
  status=$(curl -s -o /dev/null -w "%{http_code}" -X POST -b "connect.sid=$ADMIN_COOKIE" -H "Content-Type: application/json" -d '{}' "http://localhost:5000$ep")
  echo "$ep → $status"
done

# If user logged in via kiosk flow (kiosk token only, no web session), do endpoints work?
# This is the REAL kiosk scenario — device-auth doesn't create web session
```

**Report:**
- Which auth method is actually used in practice?
- Are there endpoints that fail because of auth mismatch?
- Is the kiosk token properly stored and sent?

---

### STEP 14: DATABASE INTEGRITY CHECK
```bash
echo "═══ STEP 14: DB Integrity ═══"

# All factory-related tables and their row counts
for tbl in factory_production_runs factory_stations factory_products factory_kiosk_sessions factory_kiosk_session_events factory_break_logs factory_quality_records factory_kiosk_config; do
  count=$(echo "SELECT count(*) FROM $tbl;" | psql -t 2>/dev/null | tr -d ' ')
  echo "$tbl: $count rows"
done

# Any orphan records?
echo "--- Orphan production runs (no matching station) ---"
echo "SELECT count(*) FROM factory_production_runs WHERE station_id NOT IN (SELECT id FROM factory_stations);" | psql -t 2>/dev/null

# Any active sessions that should have been closed?
echo "--- Stale sessions ---"
echo "SELECT count(*) FROM factory_kiosk_sessions WHERE status='active' AND started_at < NOW() - INTERVAL '24 hours';" | psql -t 2>/dev/null

# Production runs without end time
echo "--- Unclosed production runs ---"  
echo "SELECT count(*) FROM factory_production_runs WHERE ended_at IS NULL AND started_at < NOW() - INTERVAL '24 hours';" | psql -t 2>/dev/null
```

---

## OUTPUT FORMAT

```
═══════════════════════════════════════════════════
FACTORY KIOSK PRODUCTION WORKFLOW — SIMULATION REPORT
═══════════════════════════════════════════════════

STEP-BY-STEP RESULTS:
  Step 1 (Device Auth):     PASS/FAIL — [details]
  Step 2 (Personnel List):  PASS/FAIL — [details]
  Step 3 (Login):           PASS/FAIL — [details]
  Step 4 (Start Shift):     PASS/FAIL — [details]
  Step 5 (Station Select):  PASS/FAIL — [details]
  Step 6 (Working Phases):  PASS/FAIL — [details]
  Step 7 (Production Entry):PASS/FAIL — [details]
  Step 8 (Stop Options):    PASS/FAIL — [details]
  Step 9 (Break Flow):      PASS/FAIL — [details]
  Step 10 (Fault Report):   PASS/FAIL — [details]
  Step 11 (End Shift):      PASS/FAIL — [details]
  Step 12 (Edge Cases):     PASS/FAIL — [details]
  Step 13 (Auth Chain):     PASS/FAIL — [details]
  Step 14 (DB Integrity):   PASS/FAIL — [details]

BUGS FOUND:
  [CRITICAL] ...
  [HIGH] ...
  [MEDIUM] ...
  [LOW] ...

MISSING FEATURES:
  1. ...
  2. ...

WORKING CORRECTLY:
  1. ...
  2. ...

═══════════════════════════════════════════════════
```

Be EXHAUSTIVE. Test every button, every API, every edge case. This is launch-critical.
```

---

Bu promptu **mola fix bittikten sonra** yapıştırın. Tüm üretim akışını adım adım simüle edecek ve her noktadaki sorunları bulacak. Sonucu paylaşın, gerekli fix'leri yazarım.