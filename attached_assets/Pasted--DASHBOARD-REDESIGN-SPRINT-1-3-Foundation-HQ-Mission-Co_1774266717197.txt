## DASHBOARD REDESIGN SPRINT 1/3 — Foundation + HQ Mission Control
## UPDATED: 22 Mart 2026 — Reflects all recent system changes

### Context
DOSPRESSO is getting a new "Mission Control" dashboard template alongside the existing "Classic" one.

RECENT SYSTEM CHANGES THAT AFFECT THIS SPRINT:
- QC system added: production_lots, factory_quality_checks, qc_tracker skill, auto-lot, /api/factory/qc/stats endpoint
- Payroll consolidated: payroll-calculation-service.ts (196 lines), POST /api/payroll/calculate-detailed, PDF export
- İK enhanced: employee_documents CRUD, disciplinary_reports CRUD, GET /api/hr/ik-dashboard (4 KPIs)
- AuthUser type exists: server/types/auth.ts — use it instead of (req.user as any)
- hashedPassword stripped from /api/auth/user response
- pdf-lib installed for PDF generation
- 19 Mr. Dobody agent skills (was 17)
- misc.ts MAY have been split into modular route files — check before assuming file paths
- schema.ts MAY have been split with barrel re-exports — imports should still work
- Kiosk QC mode MAY have been added — check kiosk.tsx for qc-pending/qc-inspect steps
- Default passwords: admin=env, others=0000 from seed

IMPORTANT: Do NOT delete or modify existing dashboard pages. Create NEW components. Toggle switches between old and new.

Read ALL skills:
- .agents/skills/dospresso-architecture/SKILL.md
- .agents/skills/dospresso-quality-gate/SKILL.md
- .agents/skills/dospresso-debug-guide/SKILL.md

### CRITICAL RULES
- Do NOT touch localAuth.ts, vite.ts, vite.config.ts, package.json
- Do NOT modify existing dashboard pages (hq-dashboard.tsx, sube-ozet.tsx, ceo-command-center.tsx) — only add layout check at top
- Do NOT change backend API endpoints — use EXISTING ones only
- Do NOT change scoring services, payroll, kiosk, or QC
- Use AuthUser type from server/types/auth.ts (NOT req.user as any)
- Use DOSPRESSO existing CSS variables and dark mode color palette
- Turkish UI, users.id is VARCHAR
- All thumbnails use existing profileImageUrl field

---

## STEP 0: DEEP INVESTIGATION — VERIFY CURRENT STATE

This is critical — system has changed significantly. Verify everything before building.

```bash
echo "══════════════════════════════════════════════"
echo "STEP 0: VERIFY CURRENT SYSTEM STATE"
echo "══════════════════════════════════════════════"

# === 0A: Was misc.ts split? ===
echo "--- misc.ts current size ---"
wc -l server/routes/misc.ts
echo "--- New route files (from split?) ---"
ls -la server/routes/*-routes.ts server/routes/*-route.ts 2>/dev/null
echo "--- Total route files ---"
find server/routes -name "*.ts" | wc -l

# === 0B: Was schema.ts split? ===
echo "--- schema.ts current size ---"
wc -l shared/schema.ts
echo "--- Schema barrel files? ---"
ls shared/schema/ 2>/dev/null
echo "--- Import still works? ---"
grep "from.*shared/schema\|from.*@shared/schema" server/routes/factory.ts | head -3

# === 0C: Dashboard pages and queries ===
echo "--- Dashboard pages ---"
find client/src/pages -name "*dashboard*" -o -name "*ozet*" -o -name "*command*" | sort

echo "--- HQ dashboard queries ---"
grep -n "useQuery\|queryKey" client/src/pages/hq-dashboard.tsx | head -20

echo "--- HQ dashboard size ---"
wc -l client/src/pages/hq-dashboard.tsx

echo "--- Sube dashboard queries ---"
grep -n "useQuery\|queryKey" client/src/pages/sube-ozet.tsx | head -20

# === 0D: NEW endpoints available for dashboard ===
echo "--- QC stats ---"
curl -s http://localhost:5000/api/factory/qc/stats 2>/dev/null | head -3 || echo "Need auth"

echo "--- İK dashboard ---"
grep -n "ik-dashboard" server/routes/hr.ts | head -3

echo "--- Payroll calculate-detailed ---"
grep -n "calculate-detailed" server/routes/payroll.ts | head -3

echo "--- PDF export ---"
grep -n "pdf" server/routes/payroll.ts | head -3

# === 0E: Mr. Dobody / Guidance data ===
echo "--- Guidance widget ---"
wc -l client/src/components/widgets/guidance-widget.tsx 2>/dev/null
grep -n "useQuery\|queryKey\|/api/agent" client/src/components/widgets/guidance-widget.tsx | head -10

echo "--- Agent skills count ---"
ls server/agent/skills/*.ts 2>/dev/null | wc -l

echo "--- QC tracker skill ---"
ls server/agent/skills/qc-tracker.ts 2>/dev/null && echo "EXISTS" || echo "NOT FOUND"

# === 0F: Activity/notification data (for live feed) ===
echo "--- Notification API ---"
grep -n "router.*notification" server/routes/misc.ts server/routes/notifications*.ts 2>/dev/null | head -5

echo "--- Recent notifications ---"
echo "SELECT count(*) FROM notifications WHERE created_at > NOW() - INTERVAL '7 days';" | psql 2>/dev/null

# === 0G: User preferences ===
echo "--- dashboard_layout column ---"
echo "SELECT column_name FROM information_schema.columns WHERE table_name='users' AND column_name='dashboard_layout';" | psql

echo "--- System config ---"
echo "SELECT key, value FROM ai_system_config WHERE key LIKE '%dashboard%';" | psql 2>/dev/null

# === 0H: Profile images ===
echo "--- Profile images ---"
echo "SELECT count(*) as with_photo FROM users WHERE profile_image_url IS NOT NULL AND profile_image_url != '';" | psql
echo "SELECT count(*) as total_active FROM users WHERE is_active = true;" | psql

# === 0I: Branch images ===
echo "SELECT column_name FROM information_schema.columns WHERE table_name='branches' AND (column_name LIKE '%image%' OR column_name LIKE '%photo%');" | psql

# === 0J: CSS variables ===
echo "--- DOSPRESSO colors ---"
grep -n "hsl\|#122549\|#cc1f1f\|#c9a84c" client/src/index.css | head -15

# === 0K: Role routing ===
echo "--- Dashboard routing ---"
grep -n "hq-dashboard\|sube-ozet\|ceo-command" client/src/App.tsx | head -15

# === 0L: AuthUser type ===
echo "--- AuthUser ---"
cat server/types/auth.ts 2>/dev/null

# === 0M: Task data ===
echo "--- Task endpoints ---"
grep -n "router.*task\|router.*gorev" server/routes/branch-tasks.ts server/routes/misc.ts 2>/dev/null | head -10

# === 0N: Kiosk QC mode already added? ===
echo "--- Kiosk QC ---"
grep -n "qc-pending\|qc-inspect\|qcMode\|qc_mode" client/src/pages/fabrika/kiosk.tsx 2>/dev/null | head -5

echo "══════════════════════════════════════════════"
echo "REPORT ALL FINDINGS BEFORE IMPLEMENTING"
echo "══════════════════════════════════════════════"
```

---

## PART 1: TOGGLE INFRASTRUCTURE

### 1A: Add dashboard_layout column (if not exists)

```bash
echo "SELECT column_name FROM information_schema.columns WHERE table_name='users' AND column_name='dashboard_layout';" | psql
```

If not exists:
```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS dashboard_layout VARCHAR(30) DEFAULT 'default';
```

Add to shared/schema.ts (or shared/schema/users.ts if split):
```typescript
dashboardLayout: varchar("dashboard_layout", { length: 30 }).default('default'),
```

### 1B: System default config

```bash
echo "SELECT * FROM ai_system_config WHERE key = 'default_dashboard_layout';" | psql 2>/dev/null
```

If not exists:
```sql
INSERT INTO ai_system_config (key, value, description) 
VALUES ('default_dashboard_layout', 'classic', 'Varsayılan dashboard görünümü')
ON CONFLICT (key) DO NOTHING;
```

### 1C: API endpoints

Add to appropriate route file (check if misc.ts was split — may need new file or add to existing):

```typescript
import { AuthUser } from '../types/auth';

// GET /api/me/dashboard-layout
router.get("/api/me/dashboard-layout", isAuthenticated, async (req, res) => {
  try {
    const user = req.user as AuthUser;
    let layout = user.dashboardLayout || 'default';
    
    if (layout === 'default') {
      const config = await db.select().from(aiSystemConfig)
        .where(eq(aiSystemConfig.key, 'default_dashboard_layout')).limit(1);
      layout = config[0]?.value || 'classic';
    }
    
    res.json({ layout, userPreference: user.dashboardLayout || 'default', available: ['classic', 'mission-control'] });
  } catch (error: unknown) {
    res.json({ layout: 'classic', userPreference: 'default', available: ['classic', 'mission-control'] });
  }
});

// PATCH /api/me/dashboard-layout
router.patch("/api/me/dashboard-layout", isAuthenticated, async (req, res) => {
  try {
    const user = req.user as AuthUser;
    const { layout } = req.body;
    
    if (!['default', 'classic', 'mission-control'].includes(layout)) {
      return res.status(400).json({ error: "Geçersiz layout" });
    }
    
    await db.update(users).set({ dashboardLayout: layout }).where(eq(users.id, user.id));
    res.json({ success: true, layout });
  } catch (error: unknown) {
    res.status(500).json({ error: error instanceof Error ? error.message : "Layout güncellenemedi" });
  }
});

// PATCH /api/admin/default-dashboard-layout (admin only)
router.patch("/api/admin/default-dashboard-layout", isAuthenticated, async (req, res) => {
  const user = req.user as AuthUser;
  if (!['admin', 'ceo', 'cgo'].includes(user.role)) {
    return res.status(403).json({ error: "Yetkiniz yok" });
  }
  const { layout } = req.body;
  if (!['classic', 'mission-control'].includes(layout)) {
    return res.status(400).json({ error: "Geçersiz layout" });
  }
  
  await db.insert(aiSystemConfig)
    .values({ key: 'default_dashboard_layout', value: layout })
    .onConflictDoUpdate({ target: aiSystemConfig.key, set: { value: layout } });
  
  res.json({ success: true, defaultLayout: layout });
});
```

### 1D: Frontend hook

Create `client/src/hooks/useDashboardLayout.ts`:

```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { apiRequest } from '@/lib/queryClient';

export function useDashboardLayout() {
  const queryClient = useQueryClient();
  
  const { data, isLoading } = useQuery({
    queryKey: ['/api/me/dashboard-layout'],
    staleTime: 300000,
  });
  
  const mutation = useMutation({
    mutationFn: (layout: string) => apiRequest('PATCH', '/api/me/dashboard-layout', { layout }),
    onSuccess: () => queryClient.invalidateQueries({ queryKey: ['/api/me/dashboard-layout'] }),
  });
  
  return {
    layout: (data as any)?.layout || 'classic',
    userPreference: (data as any)?.userPreference || 'default',
    available: (data as any)?.available || ['classic', 'mission-control'],
    setLayout: (layout: string) => mutation.mutate(layout),
    isLoading: isLoading || mutation.isPending,
  };
}
```

### 1E: LayoutToggle component

Create `client/src/components/mission-control/LayoutToggle.tsx`:
- Small button: current layout name + switch icon
- Click → toggles between classic/mission-control
- Shows in dashboard header (right side)

---

## PART 2: MR. DOBODY COLLAPSIBLE PANEL

Create `client/src/components/mission-control/DobodyPanel.tsx`

### Two states:
- **Rail (44px):** Avatar circle (32px, amber bg, "D") + red badge (critical count) + severity dots. Click → expand.
- **Panel (240px):** Header + severity pills + scrollable feed + search input. Click ← → collapse.
- **Mobile:** No side panel. DobodyMobileCard at top instead.
- **State in localStorage:** key `dobody-panel-expanded`

### Data source — use SAME query as existing guidance-widget.tsx:
```bash
grep -n "useQuery\|queryKey" client/src/components/widgets/guidance-widget.tsx | head -5
```
Use that exact queryKey. Don't create new endpoint.

### AI personality by role:
```
admin/ceo/cgo/coach → "Operasyon asistanı"
supervisor/mudur → "Şube asistanı"  
stajyer/barista/bar_buddy → "Kişisel eğitim koçu"
fabrika_mudur/fabrika_operator → "Üretim asistanı"
muhasebe_ik → "İK ve finans asistanı"
```

### Feed items:
- From guidance gaps data
- Sorted: critical → high → medium → low
- Max 10 items in panel
- Each item: severity label (colored uppercase 8px) + title (10px bold) + description (9px muted) + "Düzelt →" action link
- Action links use gap.deepLink to navigate (already working from previous sprint)

### NEW: QC-related items will appear from qc-tracker skill:
- "5 lot QC bekliyor" — navigates to /fabrika/kalite-kontrol
- "2 lot reddedildi" — navigates to /fabrika/kalite-kontrol

### Search input:
- Placeholder: "Mr. Dobody'ye sor..."
- Enter → navigate to /ai-asistan?q=... or toast "Yakında"

Also create `client/src/components/mission-control/DobodyMobileCard.tsx`:
- Compact card for mobile: avatar + name + pills + summary + "Tümünü gör →"

---

## PART 3: HQ MISSION CONTROL DASHBOARD

Create `client/src/components/mission-control/HQDashboard.tsx`

### Structure:

**3A: Header (40px)**
- Left: User avatar (28px, profileImageUrl or initials) + "Merhaba, {firstName}" + date
- Right: LayoutToggle + role badge

**3B: KPI Strip (48px, grid-cols-5)**

5 KPIs for HQ (UPDATED — includes QC):
1. **Şube** (count, neutral)
2. **SLA İhlali** (count, red if > 0)
3. **Açık Ticket** (count, amber if > 0)
4. **Aktif Kullanıcı** (count, green)
5. **QC Bekleyen** (pending lot count from /api/factory/qc/stats, amber if > 0) — NEW

Each KPI: number (20px) + label (8px) + optional sparkline (7 bars, 24px height) + trend arrow

Data sources:
- Şube/SLA/Ticket/Kullanıcı: from EXISTING HQ dashboard queries (check hq-dashboard.tsx useQuery hooks)
- QC: from /api/factory/qc/stats (new endpoint)

**3C: Scrollable content**

Section 1: **Görevler (Task Feed)**
- Header: "Bugünün görevleri" + count + "Tümünü gör →"
- Max 4 tasks with priority bars (red/amber/green)
- Data: existing task API

Section 2: **Şube Durumu (Branch Status)**
- Header: "Şube durumu" + count + "Tümünü gör →"
- 2-column grid of branch cards
- Each card: thumbnail (48px, initials if no image) + name + staff count + health bar + sparkline
- Data: existing branches + branch-health API

Section 3: **Fabrika + QC Özet** — NEW section
- Mini 2x2 grid: Bugün üretim, Fire oranı, QC bekleyen, QC onay oranı
- Data: existing factory stats + /api/factory/qc/stats

Section 4: **Canlı Akış (Live Feed)**
- Max 4 items: timestamp + colored dot + bold name + description
- Data: notifications or audit_log

Section 5: **İK Özet** — NEW section (conditional: only if user has İK access)
- Mini KPIs: Aktif personel, Özlük dosya, Süresi dolan belge, Açık tutanak
- Data: /api/hr/ik-dashboard

**3D: Quick Actions (4 icons)**
- İletişim (purple), Vardiya (teal), Fabrika (coral), Raporlar (blue)
- Click → navigate

### Mobile (< 768px):
- DobodyMobileCard at top
- KPI strip → 3 columns
- Branch cards → single column
- All sections stacked

---

## PART 4: INTEGRATE TOGGLE

Add layout check at TOP of existing dashboard pages (MINIMAL change):

```typescript
import { useDashboardLayout } from '@/hooks/useDashboardLayout';
import React from 'react';

export default function HQDashboard() {
  const { layout } = useDashboardLayout();
  
  if (layout === 'mission-control') {
    const HQMissionControl = React.lazy(() => import('@/components/mission-control/HQDashboard'));
    return (
      <React.Suspense fallback={<div className="flex items-center justify-center h-full"><p className="text-sm text-muted-foreground">Yükleniyor...</p></div>}>
        <HQMissionControl />
      </React.Suspense>
    );
  }
  
  // === EXISTING CLASSIC CODE BELOW — DO NOT MODIFY ===
```

Apply to: hq-dashboard.tsx, ceo-command-center.tsx
For sube-ozet.tsx: add check but return classic for now (Sprint 2 adds Supervisor MC)

---

## PART 5: ADMIN SETTINGS

Add "Dashboard Görünüm" card to admin settings page:
- Two option cards: "Klasik" (current) and "Mission Control" (new)
- Click → sets system default
- Note: "Her kullanıcı kendi tercihini değiştirebilir"

---

## FILE STRUCTURE

```
client/src/
  components/
    mission-control/
      DobodyPanel.tsx            — Collapsible AI panel
      DobodyMobileCard.tsx       — Mobile compact card
      DobodyFeedItem.tsx         — Single guidance item
      HQDashboard.tsx            — HQ Mission Control
      LayoutToggle.tsx           — Classic ↔ MC switch
      shared/
        KPICell.tsx              — Single KPI with optional sparkline
        Section.tsx              — Section wrapper (title + count + more)
        TaskFeedItem.tsx         — Task card with priority bar
        BranchCard.tsx           — Branch with thumbnail + health
        LiveFeedItem.tsx         — Activity feed row
        QuickActions.tsx         — Icon action buttons
        QCStatusMini.tsx         — QC summary widget (NEW)
        IKStatusMini.tsx         — İK summary widget (NEW)
  hooks/
    useDashboardLayout.ts        — Layout preference hook
```

---

## DESIGN SPECS

### Colors: Use existing DOSPRESSO dark theme CSS variables
### Spacing: Panel rail 44px, panel expanded 240px, header 40px, KPI strip 48px
### Typography: KPI number 20px/500, title 12px/500, body 10px, label 8px uppercase
### Thumbnails: Staff 40px circle with 2.5px ring, Branch 48px rounded-md
### Touch: Min 44px mobile, 36px desktop
### Transitions: Panel width 200ms ease, card hover bg 150ms
### Status colors: green #1D9E75, amber #EF9F27, red #E24B4A, purple #7F77DD, blue #378ADD

---

## DO NOT TOUCH:
- localAuth.ts, vite.ts, vite.config.ts, package.json, drizzle.config.ts
- Existing dashboard page logic (only add layout check)
- Scoring services, payroll, kiosk, QC endpoints
- Existing guidance widget (reuse its data, don't modify it)

## Testing:
1. App starts without errors
2. GET /api/me/dashboard-layout → returns layout
3. PATCH /api/me/dashboard-layout → updates
4. HQ default: classic (unchanged)
5. Toggle → Mission Control renders
6. Mr. Dobody rail (44px) visible
7. Click avatar → panel expands (240px) with guidance items
8. QC items appear in Mr. Dobody feed (from qc-tracker skill)
9. KPI strip: 5 metrics including QC pending
10. Tasks section with priority bars
11. Branch cards with health scores
12. Fabrika + QC section shows factory stats
13. İK section shows document/disciplinary stats (for authorized roles)
14. Live feed shows recent activity
15. Mobile: single column, DobodyMobileCard at top
16. Admin settings: can set default layout
17. Classic mode unchanged
18. No console errors

## After:
1. Report: new files created (list)
2. Report: existing files modified (should be minimal)
3. Report: QC data integration — what endpoint, what data shown
4. Report: İK data integration — what endpoint, what data shown
5. Report: KPI data sources (all 5 metrics)
6. Report: mobile behavior