**Replit'e gönder:**

```
Production "Internal Server Error" veriyor. Acil düzeltme gerekiyor.

═══ ADIM 1: GÜNCELLEMELERİ ÇEK ═══
git stash 2>/dev/null
git pull --rebase origin main
git stash pop 2>/dev/null
npm run build

Son commit: 135829c olmalı
Kontrol: git log --oneline -1

═══ ADIM 2: PRODUCTION DB FIX ═══
Neon bağlantı hatası + eksik tablolar production'ı çökertiyor.

# 1. module_flags index fix
psql $DATABASE_URL -c "DROP INDEX IF EXISTS uq_module_flags_key_scope_branch_role;"
psql $DATABASE_URL -c "ALTER TABLE module_flags ADD CONSTRAINT uq_module_flags_key_scope_branch_role UNIQUE (module_key, scope, branch_id, target_role);" 2>/dev/null || echo "constraint zaten var"

# 2. workshop_notes tablosu
psql $DATABASE_URL -c "CREATE TABLE IF NOT EXISTS workshop_notes (id SERIAL PRIMARY KEY, user_id VARCHAR NOT NULL, title VARCHAR(255) NOT NULL, content TEXT NOT NULL, section VARCHAR(50) NOT NULL DEFAULT 'Vizyon', created_at TIMESTAMP DEFAULT NOW(), updated_at TIMESTAMP DEFAULT NOW());"

# 3. Diğer eksik tablo kontrolü
psql $DATABASE_URL -c "SELECT tablename FROM pg_tables WHERE schemaname='public' ORDER BY tablename;" | wc -l

═══ ADIM 3: DUPLICATE IMPORT KONTROLÜ ═══
# server/index.ts'te duplicate import var mı?
grep -c "migrateCrmTaskTables" server/index.ts
# 1 olmalı. 2 ise duplicate'ı sil.

═══ ADIM 4: DEV TEST ═══
# Önce dev'de çalıştığını doğrula
npm run dev &
sleep 10
curl -s http://localhost:5000/api/health | head -20
# "HEALTHY" görmeli

═══ ADIM 5: REPUBLISH ═══
# Dev sağlıklıysa publish et
# Overview → Republish butonu
# "No, add constraint without deleting data" seç
# Build → Bundle → Promote bekle

═══ ADIM 6: PRODUCTION DOĞRULAMA ═══
# Deploy bittikten 30 saniye sonra:
curl -s https://dospressohq.replit.app/api/health
# "Internal Server Error" yerine JSON dönmeli

# Login test:
# admin / 133200

═══ HATA OLURSA ═══
Production loglarını paylaş:
# Logs tab → son 20 satır kopyala
# Özellikle "Error", "crash", "ECONNREFUSED", "ErrorEvent" içeren satırlar
```