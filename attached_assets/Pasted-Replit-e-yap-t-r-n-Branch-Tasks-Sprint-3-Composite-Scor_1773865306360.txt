Replit'e yapıştırın:

---

```
## Branch Tasks Sprint 3: Composite Score Integration

### Context
Branch Tasks Sprint 1-2 created the recurring task system with full UI. Now we integrate branch task completion into the composite score system. "Şube Görevleri" becomes a NEW separate component in composite score alongside checklist, akademi, pdks, görevler, crm.

Read these skills first:
- .agents/skills/dospresso-architecture/SKILL.md
- .agents/skills/dospresso-quality-gate/SKILL.md

### CRITICAL RULES
- Do NOT break existing score calculations — add the new component, don't replace existing ones
- Feature flag aware — if sube_gorevleri is disabled, exclude from score and recalculate proportionally
- Soft delete, Turkish error messages, users.id is VARCHAR
- Do NOT touch server/vite.ts, vite.config.ts, package.json, drizzle.config.ts

---

### Step 1: Understand Current Score System

Run these searches to find the scoring logic:
```bash
grep -rn "composite\|compositeScore\|health.*score\|branch.*score\|performance.*score" server/ --include="*.ts" | grep -v node_modules | head -30

grep -rn "composite\|compositeScore" server/services/ --include="*.ts" | head -15

grep -rn "branch-health-scoring\|branchHealth" server/ --include="*.ts" -l | head -10

# Find the actual score calculation function
grep -rn "function.*score\|function.*compute\|function.*calculate" server/services/branch-health-scoring.ts | head -10

# Find what components/weights exist
grep -rn "weight\|component\|factor" server/services/branch-health-scoring.ts | head -20

# Check if there's a separate employee/person score vs branch score
grep -rn "employee.*score\|person.*score\|user.*score\|staff.*score" server/ --include="*.ts" | head -15
```

### Step 2: Add Branch Tasks Score Component

Based on what you find in Step 1, add a new scoring component for branch recurring tasks.

#### 2A: Create score calculation function

Create or add to the appropriate service file:

```typescript
// Function to calculate branch task completion score for a branch or user
export async function calculateBranchTaskScore(
  branchId: number, 
  userId?: string,
  dateRange?: { start: Date; end: Date }
): Promise<{ score: number; details: { total: number; completed: number; overdue: number; rate: number } }> {
  // Default date range: last 30 days
  const start = dateRange?.start || new Date(Date.now() - 30 * 24 * 60 * 60 * 1000);
  const end = dateRange?.end || new Date();
  
  // Query branch_task_instances for this branch (and optionally user)
  // Count: total instances, completed instances, overdue instances
  // If userId provided: count instances where assignedToUserId=userId OR claimedByUserId=userId OR completedByUserId=userId
  // If no userId: count all instances for the branch
  
  // Score formula: (completed / total) * 100
  // Bonus: subtract penalty for overdue (e.g., -5 points per overdue task, min 0)
  // If total = 0, return score = 100 (no tasks = no penalty)
  
  return {
    score: Math.max(0, Math.min(100, completionRate - overduePenalty)),
    details: { total, completed, overdue, rate: completionRate }
  };
}
```

#### 2B: Integrate into Branch Health Score

Find the branch health score calculation (likely in server/services/branch-health-scoring.ts) and add the new component.

The current components are approximately:
- inspections (checklist/denetim)
- complaints (CRM)
- equipment (ekipman)
- training (akademi)
- opsHygiene (görevler)
- customerSatisfaction (CRM)

Add a new component:
```typescript
{
  key: "branchTasks",
  label: "Şube Görevleri",
  moduleKey: "sube_gorevleri", // for feature flag check
  weight: 0.12, // adjust so total weights still sum to ~1.0
  calculate: async (branchId) => {
    const result = await calculateBranchTaskScore(branchId);
    return result.score;
  }
}
```

IMPORTANT: When adding a new component with weight 0.12, reduce existing weights proportionally so total remains 1.0. For example if current total is 1.0, multiply each existing weight by 0.88 and add the new 0.12.

#### 2C: Feature Flag Integration

The branch health score already integrates with feature flags (from Sprint 3 of Feature Flag system). Verify that:
1. The new "branchTasks" component has moduleKey: "sube_gorevleri"
2. When sube_gorevleri is disabled for a branch → component is excluded → remaining weights recalculate proportionally
3. This should already work if the existing feature flag integration uses the moduleKey field

Check:
```bash
grep -rn "isModuleEnabled\|moduleKey\|module_flag" server/services/branch-health-scoring.ts | head -10
```

If the feature flag check is already there, just make sure the new component follows the same pattern.

### Step 3: Employee/User Score Integration

Check if there's a per-employee composite score (not just branch-level):
```bash
grep -rn "employee.*composite\|user.*composite\|staff.*score\|individual.*score" server/ --include="*.ts" | head -15
grep -rn "composite.*score" server/routes/ --include="*.ts" | head -15
```

If a per-employee score exists:
- Add branch task completion as a component
- Score = % of assigned/claimed tasks completed by this employee in the period
- Weight similar to other components

If no per-employee score exists, skip this step — branch-level is sufficient for now.

### Step 4: API Endpoint for Task Score

Add an endpoint to retrieve the branch task score:
```
GET /api/branch-tasks/score?branchId=5              — branch task score (last 30 days)
GET /api/branch-tasks/score/user/:userId?days=30    — user's task score
```

These endpoints should:
- Use isAuthenticated
- Branch scope: branch roles see only their branch, HQ sees any
- Return: { score, details: { total, completed, overdue, rate } }

### Step 5: Dashboard Score Display

Find where the branch health score is displayed on dashboards:
```bash
grep -rn "health.*score\|sağlık.*skor\|branch.*score" client/src/ --include="*.tsx" | head -15
```

If the dashboard shows a breakdown of score components, add "Şube Görevleri" to the breakdown display with its score and weight.

If the dashboard only shows a total score number, no UI change needed — the backend already includes the new component in the total.

### Step 6: Stats Enhancement

Update GET /api/branch-tasks/stats to include score data:
```typescript
// Add to the existing stats response:
{
  total, pending, claimed, completed, overdue, completionRate,
  // NEW:
  score: calculatedScore, // 0-100
  scoreDetails: { total, completed, overdue, rate }
}
```

This way the Görevler page Tab 2 can show the score without a separate API call.

---

### Testing Checklist

1. App starts without errors
2. Check branch health score includes new component:
   - GET the health score endpoint for a branch that has recurring task instances
   - Verify "branchTasks" component appears in the breakdown
   - Verify weight is ~0.12 and total weights sum to ~1.0
3. Complete some branch tasks for branch 5, then check score improves
4. Disable sube_gorevleri for branch 5 → health score excludes branchTasks component → remaining weights recalculate
5. Re-enable → component returns
6. GET /api/branch-tasks/score?branchId=5 → returns valid score
7. GET /api/branch-tasks/stats → includes score and scoreDetails
8. Dashboard health score display still works (no regression)

### After implementation:
1. Run 15-point quality gate
2. Update .agents/skills/dospresso-architecture/SKILL.md — add branchTasks as health score component
3. Update replit.md — add score integration section
4. Update .agents/skills/dospresso-debug-guide/SKILL.md — add note about branch task score troubleshooting
```

---

Direkt build başlatın! Bu son sprint — bitince Branch Tasks sistemi tamamen tamamlanmış olacak.