Evet ama iki ayrı sorun var — ikisi de lansman öncesi çözülmeli:

**Sorun 1:** Mr. Dobody deep link'leri kırık (10/17 — bu rapor)
**Sorun 2:** Menü görünürlüğü (Ayarlar butonu, İK erişimi — canlı test henüz gelmedi)

İkisini tek prompt'ta çözelim. Yapıştırın:

---

**"İki sorun var — ikisini birden çöz:**

## GÖREV 1: Kırık Deep Link'leri Düzelt

Audit'te 10 kırık deep link tespit edildi. system-completeness-service.ts'teki deep link'leri güncel route yapısına güncelle:

| # | Mevcut (kırık) | Doğru hedef |
|---|---------------|-------------|
| 1 | `/admin?tab=users&action=create&branchId=X` | `/admin/kullanicilar` |
| 2 | `/admin?tab=sla` | İlgili SLA sayfası (araştır, doğru route'u bul) |
| 3 | `/bordro?tab=parametreler` | `/bordrom` |
| 4 | `/crm?tab=feedback` | `/crm/ticket-talepler` veya doğru CRM path'i |
| 5 | `/satinalma?tab=tedarikciler` | Satınalma'nın doğru tab path'i (araştır) |
| 6 | `/satinalma?tab=siparisler` | Satınalma'nın doğru sipariş path'i |
| 7 | `/fabrika?tab=hedefler` | `/fabrika/hedefler` |
| 8 | `/akademi-hq?tab=certs` | Akademi HQ doğru path (araştır) |
| 9 | `/akademi-hq?tab=training` | Akademi HQ doğru path |
| 10 | agent-action-center.tsx INVALID_DEEP_LINKS listesi eksik | Kırık linkleri listeye ekle veya düzelt |

### Süreç:
1. Her kırık link için ÖNCE doğru route'u bul:
```bash
# Her hedef sayfa için gerçek route'u bul
grep -n "admin.*kullanici\|admin.*users" client/src/App.tsx
grep -n "bordro\|bordrom" client/src/App.tsx
grep -n "crm" client/src/App.tsx
grep -n "satinalma" client/src/App.tsx
grep -n "fabrika.*hedef" client/src/App.tsx client/src/pages/fabrika/index.tsx
grep -n "akademi-hq\|akademi.*hq" client/src/App.tsx

# Mega modül tab mapping'leri
grep -n "TAB_URL_MAP\|tabMap\|getTabFromPath\|getTabFromUrl" client/src/pages/admin-mega.tsx | head -10
grep -n "TAB_URL_MAP\|tabMap" client/src/pages/crm-mega.tsx | head -10
grep -n "TAB_URL_MAP\|tabMap" client/src/pages/satinalma-mega.tsx | head -10
```

2. system-completeness-service.ts'te deep link'leri güncelle
3. agent-action-center.tsx'te INVALID_DEEP_LINKS listesini güncelle (veya artık gerek kalmaz)

## GÖREV 2: Menü Canlı Doğrulama

Menü düzeltmesi (Task #50) gerçekten çalışıyor mu? Canlı test yap:

```bash
# Admin login + menü
curl -s -X POST http://localhost:5000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"utku","password":"0000"}' \
  -c /tmp/menu_test.txt > /dev/null 2>&1

echo "=== ADMIN MENÜ ==="
curl -s http://localhost:5000/api/me/menu -b /tmp/menu_test.txt | node -e "
const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));
let total=0;
d.forEach(s=>{
  console.log('['+s.label+']');
  if(s.items) s.items.forEach(i=>{console.log('  '+i.label+' → '+i.path);total++});
});
console.log('TOPLAM: '+total);
"

echo ""
echo "=== AYARLAR VAR MI? ==="
curl -s http://localhost:5000/api/me/menu -b /tmp/menu_test.txt | grep -i "ayarlar\|settings\|admin-panel\|kullanıcı"

echo ""
echo "=== PROFİL VAR MI? ==="
curl -s http://localhost:5000/api/me/menu -b /tmp/menu_test.txt | grep -i "profil"
```

Eğer Ayarlar/Profil GÖRÜNMÜYORSA → Task #50 düzeltmesi uygulanmamış, tekrar uygula.

## GÖREV 3: Kırık Link Doğrulama

Deep link düzeltmelerinden sonra, her düzeltilen link için frontend'de sayfa var mı kontrol et:

```bash
# Her düzeltilen deep link için
for path in "/admin/kullanicilar" "/bordrom" "/crm/ticket-talepler" "/fabrika/hedefler"; do
  short=$(echo "$path" | sed 's|^/||' | cut -d'/' -f1)
  if grep -q "$short" client/src/App.tsx; then
    echo "OK: $path → route var"
  else
    echo "KIRIK: $path → route YOK"
  fi
done
```

## Kurallar:
- Deep link düzeltmeleri sadece system-completeness-service.ts ve agent-action-center.tsx'te
- Menü sorunu sadece menu-service.ts'te
- Mevcut sayfa bileşenlerine dokunma
- Her düzeltmeden sonra sunucu çalışıyor mu kontrol et

## Test:
1. Mr. Dobody uyarılarındaki "Düzelt" butonları doğru sayfaya gidiyor
2. Admin menüsünde Ayarlar var
3. Tüm rollerde Profil linki var
4. Coach menüsünde İK var
5. E2E 26/26 PASS
6. Kırık deep link sayısı: 0

## Bittikten sonra rapor:
1. Düzeltilen deep link'ler listesi (eski → yeni)
2. Menü durumu (Ayarlar görünüyor mu, Profil var mı)
3. Kalan kırık link sayısı
4. E2E sonucu

**Başla.**"

---

Yapıştırın. Bu iki sorunu birden çözecek. Sonucu paylaşın.